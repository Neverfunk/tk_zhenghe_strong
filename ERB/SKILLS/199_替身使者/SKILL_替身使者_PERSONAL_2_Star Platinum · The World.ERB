;-----------------------------------
;基本値計算に先んじて処理するもの
;-----------------------------------
@SKILL_199_PERSONAL_2_EXIST
RETURN 1

@SKILL_199_PERSONAL_2_NAME
RESULTS = 白金之星·世界

;レベルは1-5まで
@SKILL_199_PERSONAL_2_LEVEL
RETURN 5

;対象選択
@SKILL_199_PERSONAL_2_SETTARGET(発動者, 発動番号, 発動側, 発動勢力, 発動部隊, 対象勢力, 対象部隊)
#DIM 発動者
#DIM 発動番号
#DIM スキル
#DIMS ジャンル
#DIM 発動側
#DIM 発動勢力
#DIM 発動部隊
#DIM 対象側
#DIM 対象勢力
#DIM 対象部隊
;自部隊対象
RETURN 1

;発動判定
@SKILL_199_PERSONAL_2_CAN_INVOKE(発動者, 発動番号, 発動側, 発動勢力, 発動部隊, 対象勢力, 対象部隊)
#DIM 発動者
#DIM 発動番号
#DIM スキル
#DIMS ジャンル
#DIM 発動側
#DIM 発動勢力
#DIM 発動部隊
#DIM 対象側
#DIM 対象勢力
#DIM 対象部隊
対象側 = !発動側
RETURN 1

;効果をここに記述
@SKILL_199_PERSONAL_2_INVOKE(発動者, 発動番号, 発動側, 発動勢力, 発動部隊, 対象勢力, 対象部隊)
#DIM 発動者
#DIM 発動番号
#DIM スキル
#DIMS ジャンル
#DIM 発動側
#DIM 発動勢力
#DIM 発動部隊
#DIM 対象側
#DIM 対象勢力
#DIM 対象部隊
対象側 = !発動側
PRINTFORMW 白金之星·世界！停止吧，时间！

FOR LOCAL, 0, BATTLE_COMMANDER_NUM:発動側
	IF LOCAL != 発動番号
		CALL BATTLE_KNOCKOUT(発動側, LOCAL)
	ENDIF
NEXT
FOR LOCAL, 0, BATTLE_COMMANDER_NUM:対象側
	CALL BATTLE_KNOCKOUT(対象側, LOCAL)
NEXT

RETURN 1

;

@SKILL_199_PERSONAL_2_EXPLANATION
RESULTS = 停止全世界的时间


@SKILL_199_PERSONAL_2_CANT_TELL

@SKILL_199_PERSONAL_2_RATE(発動者, 発動番号, 発動側, 発動勢力, 発動部隊, 対象勢力, 対象部隊)
#DIM 発動者
#DIM 発動番号
#DIM スキル
#DIMS ジャンル
#DIM 発動側
#DIM 発動勢力
#DIM 発動部隊
#DIM 対象側
#DIM 対象勢力
#DIM 対象部隊
RETURN 300
