;-------------------------------------------------
;「会いに行く」「閨に呼ぶ」「子育て」「捕虜会話」の口上
;-------------------------------------------------

;判定の難しい素質を判断するため、以下のCFLAGを追加・使用しています
;CFLAG:250　初キス判定用（『主人公との初めてのキス』判定に使う。既成事実Lv1に当たる）
;CFLAG:251　ファーストキス判定用（本キャラの『ファーストキス』判定に使う。キス経験済みだと表示されない）
;CFLAG:252　処女判定用（セックス時の処女判定に使う。処女喪失済みだと表示されない）
;CFLAG:253　処女（主人公）判定用（セックス時の主人公が処女かの判定に使う。処女喪失済みだと表示されない）
;CFLAG:254　童貞判定用（セックス時の主人公が童貞かの判定に使う。童貞喪失済みだと表示されない）

;成美口上用CFLAGメモ
;CFLAG:400 素質等変更処理


;=================================================
;素質変更処理。エレン口上を参考にしました
;=================================================
@CHANGE_TALENT_NARUMI
#DIM 消去行
$SHOW_LOOP
消去行 = 10
PRINTFORMDL 胸囲尺寸
CALL PRINTBUTTON_EX("{絶壁（デフォルト）}", 0, 1, TALENT:胸囲尺寸 == -2)
CALL PRINTBUTTON_EX("{貧乳}", 1, 1, TALENT:胸囲尺寸 == -1)
CALL PRINTBUTTON_EX("{並乳}", 2, 1, TALENT:胸囲尺寸 == 0)
CALL PRINTBUTTON_EX("{巨乳}", 3, 1, TALENT:胸囲尺寸 == 1)
PRINTL

PRINTFORMDL 臀囲尺寸
CALL PRINTBUTTON_EX("{巨尻（デフォルト）}", 5, 1, TALENT:臀囲尺寸 == 1)
CALL PRINTBUTTON_EX("{普尻}", 6, 1, TALENT:臀囲尺寸 == 0)
CALL PRINTBUTTON_EX("{薄尻}", 7, 1, TALENT:臀囲尺寸 == -1)
PRINTL

PRINTL
PRINTL
PRINTBUTTON "[决定了]", 999
INPUT
IF RESULT == 999
	RETURN
ELSEIF RESULT == 0
	TALENT:胸囲尺寸 = -2
ELSEIF RESULT == 1
	TALENT:胸囲尺寸 = -1
ELSEIF RESULT == 2
	TALENT:胸囲尺寸 = 0
ELSEIF RESULT == 3
	TALENT:胸囲尺寸 = 1
ELSEIF RESULT == 5
	TALENT:臀囲尺寸 = 1
ELSEIF RESULT == 6
	TALENT:臀囲尺寸 = 0
ELSEIF RESULT == 7
	TALENT:臀囲尺寸 = -1
ENDIF
CLEARLINE 消去行
GOTO SHOW_LOOP

;=================================================
;●「会いに行く」の開始時のセリフ
;=================================================
@KOJO_TRAIN_START_A1_K145
;[空虚]状態なら口上を表示しない
IF TALENT:空虚
	RETURN
ENDIF


;-------------------------------------------------
;初回
;-------------------------------------------------
IF CFLAG:200 == 0
	CFLAG:200 = 1
	;素質変更処理
	IF CFLAG:400 == 0
		CFLAG:400 = 1
		PRINTFORMDL 确认改变素质吗？
		CALL ASK_YN("是", "否")
		;TranslatedMark "はい", "いいえ"
		SIF RESULT == 0
			CALL CHANGE_TALENT_NARUMI
		PRINTW
		CALL SINGLE_DRAWLINE
	ENDIF
	;初対面の場合
	IF !CFLAG:17
		;対象が魔理沙か菫子の場合
		IF GROUPMATCH(MASTER, NAME_TO_CHARA("魔理沙")) || GROUPMATCH(MASTER, NAME_TO_CHARA("菫子"))
			PRINTFORMW "我还以为是谁呢，原来是%ANAME(MASTER)%啊，好久不见"
			;TranslatedMark 「誰かと思ったら%ANAME(MASTER)%じゃないの、久しぶり」
		;軟禁中に会いに行った場合
		ELSEIF CFLAG:29 == 1
			PRINTFORMW "总之先自我介绍一下，我是%NAME_FORMAL(TARGET)%"
			;TranslatedMark 「取り敢えず自己紹介しておくわ、私は%NAME_FORMAL(TARGET)%」
			PRINTFORMW "今天有什么事吗？"
			;TranslatedMark 「今日は何の用事なの？」
		;それ以外の場合
		ELSE
			PRINTFORMW "初次见面，感谢您的到来"
			;TranslatedMark 「初めまして、来てくれてありがとう」
			PRINTFORMW "我是%NAME_FORMAL(TARGET)%，请多关照"
			;TranslatedMark 「私は%NAME_FORMAL(TARGET)%よ、よろしくね」
		ENDIF
	;既に会ったことがある場合
	ELSE
		PRINTFORMW "大概是第一次以这样的形式见面吧"
		;TranslatedMark 「多分、こういう形で会うのは初めてね」
		PRINTFORMW "重新自我介绍一下，我是%NAME_FORMAL(TARGET)%"
		;TranslatedMark 「改めまして、私は%NAME_FORMAL(TARGET)%よ」
	ENDIF

;-------------------------------------------------
;開始時(1回のみ)
;-------------------------------------------------

;■正妻や親愛系で台詞が思いつかなければこの行から↓■
;正妻か妾
ELSEIF (CFLAG:200 < 7) && (TALENT:正妻 || TALENT:妾)
	;婚姻した次の回フラグ
	CFLAG:200 = 7
	;PRINTFORMW

;親愛か隷属
ELSEIF (CFLAG:200 < 6) && (TALENT:親愛 || TALENT:隷属)
	;親愛か隷属になった次の回フラグ
	CFLAG:200 = 6
	;PRINTFORMW
;■↑この行まで消しても良い■

;既成事実Lv3(初めてセックスをした次の回)
ELSEIF CFLAG:200 < 5 && TALENT:合意
	CFLAG:200 = 5
	;恋慕時の台詞。思いつかなければ「それ以外」と一緒でよい
	IF TALENT:恋慕
		;PRINTFORMW
	;それ以外
	ELSE
		;PRINTFORMW
	ENDIF

;既成事実Lv2(恋人になった次の回)
ELSEIF CFLAG:200 < 4 && TALENT:恋人
	CFLAG:200 = 4
	;恋慕時の台詞。思いつかなければ「それ以外」と一緒でよい
	IF TALENT:恋慕
		;PRINTFORMW
	;それ以外
	ELSE
		;PRINTFORMW
	ENDIF

;既成事実Lv1(初めてキスをした次の回)
ELSEIF CFLAG:200 < 3 && CFLAG:250 == 1
	CFLAG:200 = 3
	;PRINTFORMW

;既成事実Lv0で恋慕
ELSEIF CFLAG:200 < 2 && !TALENT:恋人 && TALENT:恋慕 && !TALENT:合意 && CFLAG:250 < 1
	CFLAG:200 = 2
	;PRINTFORMW

;-------------------------------------------------
;開始時(2回目以降)
;-------------------------------------------------
;正妻か妾　思いつかなければ親愛等のコピペでよい。というかしてないと正妻か妾になると台詞が表示されなくなる
ELSEIF TALENT:正妻 || TALENT:妾
	;ランダムで口上が変化する（使わない場合はすべて同じにすればよい）
	SELECTCASE RAND:3
		CASE 0
			;PRINTFORMW
		CASE 1
			;PRINTFORMW
		CASE 2
			;PRINTFORMW
	ENDSELECT

;親愛か隷属　思いつかなければ恋慕等のコピペでよい。というかしてないと親愛か隷属になると台詞が表示されなくなる
ELSEIF TALENT:親愛 || TALENT:隷属
	SELECTCASE RAND:3
		CASE 0
			;PRINTFORMW
		CASE 1
			;PRINTFORMW
		CASE 2
			;PRINTFORMW
	ENDSELECT

;既成事実Lv3(セックス済み)
ELSEIF TALENT:合意
	;恋慕
	IF TALENT:恋慕
		SELECTCASE RAND:3
			CASE 0
				;PRINTFORMW
			CASE 1
				;PRINTFORMW
			CASE 2
				;PRINTFORMW
		ENDSELECT
	;それ以外
	ELSE
		SELECTCASE RAND:2
			CASE 0
				;PRINTFORMW
			CASE 1
				;PRINTFORMW
		ENDSELECT
	ENDIF

;既成事実Lv2(恋人)
ELSEIF TALENT:恋人
	;恋慕
	IF TALENT:恋慕
		SELECTCASE RAND:3
			CASE 0
				;PRINTFORMW
			CASE 1
				;PRINTFORMW
			CASE 2
				;PRINTFORMW
		ENDSELECT
	;それ以外
	ELSE
		SELECTCASE RAND:2
			CASE 0
				;PRINTFORMW
			CASE 1
				;PRINTFORMW
		ENDSELECT
	ENDIF

;既成事実Lv1(キス済み)
ELSEIF CFLAG:250 == 1
	SELECTCASE RAND:2
		CASE 0
			;PRINTFORMW
		CASE 1
			;PRINTFORMW
	ENDSELECT

;既成事実Lv0で恋慕
ELSEIF !TALENT:恋人 && TALENT:恋慕 && !TALENT:合意 && CFLAG:250 < 1
	;PRINTFORMW

;既成事実Lv0
ELSEIF CFLAG:250 < 1
	;PRINTFORMW
ENDIF

;=================================================
;●「会いに行く」の終了時のセリフ
;=================================================
@KOJO_TRAIN_END_A1_K145
;[空虚]状態なら口上を表示しない
IF TALENT:空虚
	RETURN
ENDIF

;-------------------------------------------------
;行動不能の場合
;-------------------------------------------------
;酒酔いによる行動不能　現状死に分岐
IF TCVAR:53 == 1
	;PRINTFORMW
;快感のあまり気絶　現状死に分岐
ELSEIF TCVAR:52
	;PRINTFORMW
;疲労による行動不能
ELSEIF TCVAR:51
	;PRINTFORMW
ENDIF

;-------------------------------------------------
;終了時(1回のみ)
;-------------------------------------------------
;既成事実Lv3(初めてセックスをした)
IF CFLAG:201 < 5 && TALENT:合意
	CFLAG:201 = 5
	;行動不能ならフラグだけ立てて表示はしない
	IF !(TCVAR:51 || TCVAR:52 || TCVAR:53)
		;初めてセックスをしたときに恋慕かどうか。思いつかなければ一緒でよい
		IF TALENT:恋慕
			;PRINTFORMW
		ELSE
			;PRINTFORMW
		ENDIF
	ENDIF

;既成事実Lv2(恋人になった)
ELSEIF CFLAG:201 < 4 && TALENT:恋人
	CFLAG:201 = 4
	;行動不能ならフラグだけ立てて表示はしない
	IF !(TCVAR:51 || TCVAR:52 || TCVAR:53)
		;恋人になったときに恋慕かどうか。思いつかなければ一緒でよい
		IF TALENT:恋慕
			;PRINTFORMW
		ELSE
			;PRINTFORMW
		ENDIF
	ENDIF

;既成事実Lv1(初めてキスをした)
ELSEIF CFLAG:201 < 3 && CFLAG:250 == 1
	CFLAG:201 = 3
	;行動不能ならフラグだけ立てて表示はしない
	IF !(TCVAR:51 || TCVAR:52 || TCVAR:53)
		;PRINTFORMW
	ENDIF

;-------------------------------------------------
;終了時(2回目以降)
;-------------------------------------------------
;行動不能なら非表示
ELSEIF !(TCVAR:51 || TCVAR:52 || TCVAR:53)
	;正妻か妾　思いつかなければ親愛のコピペでよい。というかしてないと正妻か妾になると台詞が表示されなくなる
	IF TALENT:正妻 || TALENT:妾
		;終わり際、セックスしなかった場合はこちらを表示。気にならなければセックスした場合と同じコピペでよい
		IF !FLAG:ウフフフラグ
			SELECTCASE RAND:3
				CASE 0
					;PRINTFORMW
				CASE 1
					;PRINTFORMW
				CASE 2
					;PRINTFORMW
			ENDSELECT
		;終わり際、セックスした場合はこちらを表示
		ELSE
			SELECTCASE RAND:3
				CASE 0
					;PRINTFORMW
				CASE 1
					;PRINTFORMW
				CASE 2
					;PRINTFORMW
			ENDSELECT
		ENDIF

	;親愛か隷属　思いつかなければ恋慕のコピペでよい。というかしてないと親愛か隷属になると台詞が表示されなくなる
	ELSEIF TALENT:親愛 || TALENT:隷属
		;終わり際、セックスしなかった場合はこちらを表示。気にならなければセックスした場合と同じコピペでよい
		IF !FLAG:ウフフフラグ
			SELECTCASE RAND:3
				CASE 0
					;PRINTFORMW
				CASE 1
					;PRINTFORMW
				CASE 2
					;PRINTFORMW
			ENDSELECT
		;終わり際、セックスした場合はこちらを表示
		ELSE
			SELECTCASE RAND:3
				CASE 0
					;PRINTFORMW
				CASE 1
					;PRINTFORMW
				CASE 2
					;PRINTFORMW
			ENDSELECT
		ENDIF

	;既成事実Lv3（セックスの合意あり）
	ELSEIF TALENT:合意
		;恋慕
		IF TALENT:恋慕
			;終わり際、セックスしなかった場合はこちらを表示。気にならなければセックスした場合と同じコピペでよい
			IF !FLAG:ウフフフラグ
				SELECTCASE RAND:3
					CASE 0
						;PRINTFORMW
					CASE 1
						;PRINTFORMW
					CASE 2
						;PRINTFORMW
				ENDSELECT
			;終わり際、セックスした場合はこちらを表示
			ELSE
				SELECTCASE RAND:3
					CASE 0
						;PRINTFORMW
					CASE 1
						;PRINTFORMW
					CASE 2
						;PRINTFORMW
				ENDSELECT
			ENDIF
		;それ以外
		ELSE
			;終わり際、セックスしなかった場合はこちらを表示。気にならなければセックスした場合と同じコピペでよい
			IF !FLAG:ウフフフラグ
				SELECTCASE RAND:3
					CASE 0
						;PRINTFORMW
					CASE 1
						;PRINTFORMW
					CASE 2
						;PRINTFORMW
				ENDSELECT
			;終わり際、セックスした場合はこちらを表示
			ELSE
				SELECTCASE RAND:3
					CASE 0
						;PRINTFORMW
					CASE 1
						;PRINTFORMW
					CASE 2
						;PRINTFORMW
				ENDSELECT
			ENDIF
		ENDIF

	;既成事実Lv2(恋人)
	ELSEIF TALENT:恋人
		SELECTCASE RAND:2
			CASE 0
				;PRINTFORMW
			CASE 1
				;PRINTFORMW
		ENDSELECT

	;既成事実Lv1(キス済み)
	ELSEIF CFLAG:250 == 1
		SELECTCASE RAND:2
			CASE 0
				;PRINTFORMW
			CASE 1
				;PRINTFORMW
		ENDSELECT

	;既成事実Lv0で恋慕
	ELSEIF !TALENT:恋人 && TALENT:恋慕 && !TALENT:合意 && CFLAG:250 < 1
		;PRINTFORMW

	;既成事実Lv0
	ELSEIF CFLAG:250 < 1
		;PRINTFORMW

	ENDIF
ENDIF

;=================================================
;●「閨に呼ぶ」の開始時のセリフ ※単独で呼ばれた場合のみ表示される
;=================================================
@KOJO_TRAIN_START_A2_K145
;[空虚]状態なら口上を表示しない
IF TALENT:空虚
	RETURN
ENDIF
;正妻か妾
IF TALENT:正妻 || TALENT:妾
	;PRINTFORMW
;親愛か隷属
ELSEIF TALENT:親愛 || TALENT:隷属
	;PRINTFORMW
;恋慕
ELSEIF TALENT:恋慕
	;PRINTFORMW
;服従
ELSEIF TALENT:服従
	;PRINTFORMW
;それ以外
ELSE
	;PRINTFORMW
ENDIF

;=================================================
;●「閨に呼ぶ」の終了時のセリフ ※単独で呼ばれた場合のみ表示される
;=================================================
@KOJO_TRAIN_END_A2_K145
;[空虚]状態なら口上を表示しない
IF TALENT:空虚
	RETURN
ENDIF

;酒酔いによる行動不能　※現在死に分岐
IF TCVAR:53 == 1
	;PRINTFORMW
;快感のあまり気絶　※現在死に分岐
ELSEIF TCVAR:52
	;PRINTFORMW
;疲労による行動不能
ELSEIF TCVAR:51
	;PRINTFORMW
ENDIF

;正妻か妾
IF TALENT:正妻 || TALENT:妾
	;PRINTFORMW
;親愛か隷属
ELSEIF TALENT:親愛 || TALENT:隷属
	;PRINTFORMW
;恋慕
ELSEIF TALENT:恋慕
	;PRINTFORMW
;服従
ELSEIF TALENT:服従
	;PRINTFORMW
;それ以外
ELSE
	;PRINTFORMW
ENDIF

;=================================================
;●コマンド実行前(このキャラがターゲット側のとき)
;※地の文が表示される前に実行される。戻り値に1を設定する(RETURN 1)と地の文がカットされる
;  必要に応じてKOJO_COM_TARGETの代わりに使う
;=================================================
@KOJO_COM_BEFORE_TARGET_A_K145
;[空虚]状態の場合、口が塞がっている場合は口上を表示しない
IF TALENT:空虚 || IS_EQUIP_CONTINUE(TARGET, "口装着")
	RETURN 0
ENDIF

;ファーストキスフラグ（無接吻経験だったらCFLAG:251を1にセット）
SIF TALENT:無接吻経験
	CFLAG:251 = 1
;処女フラグ（処女だったらCFLAG:252を1にセット）
SIF TALENT:処女
	CFLAG:252 = 1
;主人公が処女フラグ（主人公が処女だったらCFLAG:253を1にセット）
SIF TALENT:MASTER:処女
	CFLAG:253 = 1
;主人公が童貞フラグ（主人公が童貞だったらCFLAG:254を1にセット）
SIF TALENT:MASTER:童貞
	CFLAG:254 = 1

RETURN 0

;=================================================
;●コマンド実行前(このキャラがプレイヤー側のとき)
;※地の文が表示される前に実行される。戻り値に1を設定する(RETURN 1)と地の文がカットされる
;  必要に応じてKOJO_COM_PLAYERの代わりに使う
;=================================================
@KOJO_COM_BEFORE_PLAYER_A_K145
;[空虚]状態の場合、口が塞がっている場合は口上を表示しない
IF TALENT:空虚 || IS_EQUIP_CONTINUE(TARGET, "口装着")
	RETURN 0
ENDIF

;ファーストキスフラグ（無接吻経験だったらCFLAG:251を1にセット）
SIF TALENT:無接吻経験
	CFLAG:251 = 1
;処女フラグ（処女だったらCFLAG:252を1にセット）
SIF TALENT:処女
	CFLAG:252 = 1
;主人公が処女フラグ（主人公が処女だったらCFLAG:253を1にセット）
SIF TALENT:MASTER:処女
	CFLAG:253 = 1
;主人公が童貞フラグ（主人公が童貞だったらCFLAG:254を1にセット）
SIF TALENT:MASTER:童貞
	CFLAG:254 = 1

RETURN 0

;=================================================
;●コマンド実行時(このキャラがプレイヤーでもターゲットでも呼び出される)
;  プレイヤー・ターゲットの区別がないコマンドはここに口上を記述する
;=================================================
@KOJO_COM_A_K145
;[空虚]状態の場合、口が塞がっている場合は口上を表示しない
IF TALENT:空虚 || IS_EQUIP_CONTINUE(TARGET, "口装着")
	RETURN 0
ENDIF

;-------------------------------------------------
;キス
;-------------------------------------------------
IF SELECTCOM == 20
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		CFLAG:250 = 1;初めてキスをしたフラグ
		;ファーストキス
		IF CFLAG:251 == 1
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;キス経験済み
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				SELECTCASE RAND:3
					CASE 0
						;PRINTFORMW
					CASE 1
						;PRINTFORMW
					CASE 2
						;PRINTFORMW
				ENDSELECT
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		CFLAG:250 = 1;初めてキスをしたフラグ
		;ファーストキス
		IF CFLAG:251 == 1
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;キス経験済み
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				SELECTCASE RAND:3
					CASE 0
						;PRINTFORMW
					CASE 1
						;PRINTFORMW
					CASE 2
						;PRINTFORMW
				ENDSELECT
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;貝合わせ
;-------------------------------------------------
IF SELECTCOM == 21
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;Ｃ感Lv3以上
				IF ABL:Ｃ感 >= 3
					;PRINTFORMW
				;Ｃ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;Ｃ感Lv3以上
				IF ABL:Ｃ感 >= 3
					;PRINTFORMW
				;Ｃ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;Ｃ感Lv3以上
				IF ABL:Ｃ感 >= 3
					;PRINTFORMW
				;Ｃ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;Ｃ感Lv3以上
				IF ABL:Ｃ感 >= 3
					;PRINTFORMW
				;Ｃ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;双頭バイブ
;-------------------------------------------------
IF SELECTCOM == 22
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;このキャラと主人公が共に処女
			IF CFLAG:252 == 1 && CFLAG:253 == 1
				;恋慕 or 服従
				IF TALENT:恋慕 || TALENT:服従
					;PRINTFORMW
				;それ以外
				ELSE
					;PRINTFORMW
				ENDIF
			;このキャラのみ処女
			ELSEIF CFLAG:252 == 1
				;恋慕 or 服従
				IF TALENT:恋慕 || TALENT:服従
					;PRINTFORMW
				;それ以外
				ELSE
					;PRINTFORMW
				ENDIF
			;主人公のみ処女
			ELSEIF CFLAG:253 == 1
				;恋慕 or 服従
				IF TALENT:恋慕 || TALENT:服従
					;PRINTFORMW
				;それ以外
				ELSE
					;PRINTFORMW
				ENDIF
			;このキャラと主人公が共に非処女
			ELSE
				;恋慕 or 服従
				IF TALENT:恋慕 || TALENT:服従
					;PRINTFORMW
				;それ以外
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;Ｖ感Lv3以上
				IF ABL:Ｖ感 >= 3
					;PRINTFORMW
				;Ｖ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;Ｖ感Lv3以上
				IF ABL:Ｖ感 >= 3
					;PRINTFORMW
				;Ｖ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;このキャラと主人公が共に処女
			IF CFLAG:252 == 1 && CFLAG:253 == 1
				;恋慕 or 服従
				IF TALENT:恋慕 || TALENT:服従
					;PRINTFORMW
				;それ以外
				ELSE
					;PRINTFORMW
				ENDIF
			;このキャラのみ処女
			ELSEIF CFLAG:252 == 1
				;恋慕 or 服従
				IF TALENT:恋慕 || TALENT:服従
					;PRINTFORMW
				;それ以外
				ELSE
					;PRINTFORMW
				ENDIF
			;主人公のみ処女
			ELSEIF CFLAG:253 == 1
				;恋慕 or 服従
				IF TALENT:恋慕 || TALENT:服従
					;PRINTFORMW
				;それ以外
				ELSE
					;PRINTFORMW
				ENDIF
			;このキャラと主人公が共に非処女
			ELSE
				;恋慕 or 服従
				IF TALENT:恋慕 || TALENT:服従
					;PRINTFORMW
				;それ以外
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;Ｖ感Lv3以上
				IF ABL:Ｖ感 >= 3
					;PRINTFORMW
				;Ｖ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;Ｖ感Lv3以上
				IF ABL:Ｖ感 >= 3
					;PRINTFORMW
				;Ｖ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;=================================================
;●コマンド実行時(このキャラがターゲット側のとき)
;=================================================
@KOJO_COM_TARGET_A_K145
;[空虚]状態の場合、口が塞がっている場合は口上を表示しない
IF TALENT:空虚 || IS_EQUIP_CONTINUE(TARGET, "口装着")
	RETURN 0
ENDIF

;-------------------------------------------------
;愛撫(ターゲット・される側)
;-------------------------------------------------
IF SELECTCOM == 0
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;胸愛撫(ターゲット・される側)
;-------------------------------------------------
IF SELECTCOM == 1
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;Ｂ感Lv3以上
				IF ABL:Ｂ感 >= 3
					;PRINTFORMW
				;Ｂ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;Ｂ感Lv3以上
				IF ABL:Ｂ感 >= 3
					;PRINTFORMW
				;Ｂ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;Ｂ感Lv3以上
				IF ABL:Ｂ感 >= 3
					;PRINTFORMW
				;Ｂ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;Ｂ感Lv3以上
				IF ABL:Ｂ感 >= 3
					;PRINTFORMW
				;Ｂ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF
;-------------------------------------------------
;クンニ(ターゲット・される側)
;-------------------------------------------------
IF SELECTCOM == 2
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;Ｃ感Lv3以上
				IF ABL:Ｃ感 >= 3
					;PRINTFORMW
				;Ｃ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;Ｃ感Lv3以上
				IF ABL:Ｃ感 >= 3
					;PRINTFORMW
				;Ｃ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;Ｃ感Lv3以上
				IF ABL:Ｃ感 >= 3
					;PRINTFORMW
				;Ｃ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;Ｃ感Lv3以上
				IF ABL:Ｃ感 >= 3
					;PRINTFORMW
				;Ｃ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;指挿入れ(ターゲット・される側)
;-------------------------------------------------
IF SELECTCOM == 3
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;処女
			IF TALENT:処女
				;恋慕 or 服従
				IF TALENT:恋慕 || TALENT:服従
					;PRINTFORMW
				;それ以外
				ELSE
					;PRINTFORMW
				ENDIF
			;非処女
			ELSE
				;恋慕 or 服従
				IF TALENT:恋慕 || TALENT:服従
					;PRINTFORMW
				;それ以外
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;Ｖ感Lv3以上
				IF ABL:Ｖ感 >= 3
					;PRINTFORMW
				;Ｖ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;Ｖ感Lv3以上
				IF ABL:Ｖ感 >= 3
					;PRINTFORMW
				;Ｖ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;処女
			IF TALENT:処女
				;恋慕 or 服従
				IF TALENT:恋慕 || TALENT:服従
					;PRINTFORMW
				;それ以外
				ELSE
					;PRINTFORMW
				ENDIF
			;非処女
			ELSE
				;恋慕 or 服従
				IF TALENT:恋慕 || TALENT:服従
					;PRINTFORMW
				;それ以外
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;Ｖ感Lv3以上
				IF ABL:Ｖ感 >= 3
					;PRINTFORMW
				;Ｖ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;Ｖ感Lv3以上
				IF ABL:Ｖ感 >= 3
					;PRINTFORMW
				;Ｖ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;アナル愛撫(ターゲット・される側)
;-------------------------------------------------
IF SELECTCOM == 4
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;Ａ感Lv3以上
				IF ABL:Ａ感 >= 3
					;PRINTFORMW
				;Ａ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;Ａ感Lv3以上
				IF ABL:Ａ感 >= 3
					;PRINTFORMW
				;Ａ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;Ａ感Lv3以上
				IF ABL:Ａ感 >= 3
					;PRINTFORMW
				;Ａ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;Ａ感Lv3以上
				IF ABL:Ａ感 >= 3
					;PRINTFORMW
				;Ａ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;泡踊り(ターゲット・される側)
;-------------------------------------------------
IF SELECTCOM == 5
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;尻尾挿入(ターゲット・挿入される側)
;-------------------------------------------------
IF SELECTCOM == 6
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;処女
			IF CFLAG:252 == 1
				;恋慕 or 服従
				IF TALENT:恋慕 || TALENT:服従
					;PRINTFORMW
				;それ以外
				ELSE
					;PRINTFORMW
				ENDIF
			;非処女
			ELSE
				;恋慕 or 服従
				IF TALENT:恋慕 || TALENT:服従
					;PRINTFORMW
				;それ以外
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;Ｖ感Lv3以上
				IF ABL:Ｖ感 >= 3
					;PRINTFORMW
				;Ｖ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;Ｖ感Lv3以上
				IF ABL:Ｖ感 >= 3
					;PRINTFORMW
				;Ｖ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;処女
			IF CFLAG:252 == 1
				;恋慕 or 服従
				IF TALENT:恋慕 || TALENT:服従
					;PRINTFORMW
				;それ以外
				ELSE
					;PRINTFORMW
				ENDIF
			;非処女
			ELSE
				;恋慕 or 服従
				IF TALENT:恋慕 || TALENT:服従
					;PRINTFORMW
				;それ以外
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;Ｖ感Lv3以上
				IF ABL:Ｖ感 >= 3
					;PRINTFORMW
				;Ｖ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;Ｖ感Lv3以上
				IF ABL:Ｖ感 >= 3
					;PRINTFORMW
				;Ｖ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;尻尾Ａ挿入(ターゲット・挿入される側)
;-------------------------------------------------
IF SELECTCOM == 7
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;Ａ感Lv3以上
				IF ABL:Ａ感 >= 3
					;PRINTFORMW
				;Ａ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;Ａ感Lv3以上
				IF ABL:Ａ感 >= 3
					;PRINTFORMW
				;Ａ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;Ａ感Lv3以上
				IF ABL:Ａ感 >= 3
					;PRINTFORMW
				;Ａ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;Ａ感Lv3以上
				IF ABL:Ａ感 >= 3
					;PRINTFORMW
				;Ａ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;アナル舐め(ターゲット・舐められる側)
;-------------------------------------------------
IF SELECTCOM == 8
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;Ａ感Lv3以上
				IF ABL:Ａ感 >= 3
					;PRINTFORMW
				;Ａ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;Ａ感Lv3以上
				IF ABL:Ａ感 >= 3
					;PRINTFORMW
				;Ａ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;Ａ感Lv3以上
				IF ABL:Ａ感 >= 3
					;PRINTFORMW
				;Ａ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;Ａ感Lv3以上
				IF ABL:Ａ感 >= 3
					;PRINTFORMW
				;Ａ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;乳首吸い(ターゲット・吸われる側)
;-------------------------------------------------
IF SELECTCOM == 9
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;Ｂ感Lv3以上
				IF ABL:Ｂ感 >= 3
					;PRINTFORMW
				;Ｂ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;Ｂ感Lv3以上
				IF ABL:Ｂ感 >= 3
					;PRINTFORMW
				;Ｂ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;Ｂ感Lv3以上
				IF ABL:Ｂ感 >= 3
					;PRINTFORMW
				;Ｂ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;Ｂ感Lv3以上
				IF ABL:Ｂ感 >= 3
					;PRINTFORMW
				;Ｂ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;手コキ(ターゲット・される側。要ペニス)
;-------------------------------------------------
IF SELECTCOM == 10
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				SELECTCASE RAND:3
					CASE 0
						;PRINTFORMW
					CASE 1
						;PRINTFORMW
					CASE 2
						;PRINTFORMW
				ENDSELECT
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				SELECTCASE RAND:3
					CASE 0
						;PRINTFORMW
					CASE 1
						;PRINTFORMW
					CASE 2
						;PRINTFORMW
				ENDSELECT
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;フェラチオ(ターゲット・しゃぶられる側。要ペニス)
;-------------------------------------------------
IF SELECTCOM == 11
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;パイズリ(ターゲット・される側。要ペニス)
;-------------------------------------------------
IF SELECTCOM == 12
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;素股(ターゲット・される側。要ペニス)
;-------------------------------------------------
IF SELECTCOM == 13
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;足コキ(ターゲット・される側。要ペニス)
;-------------------------------------------------
IF SELECTCOM == 14
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;尻コキ(ターゲット・される側。要ペニス)
;-------------------------------------------------
IF SELECTCOM == 15
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;オナホコキ(ターゲット・される側。要ペニス)
;-------------------------------------------------
IF SELECTCOM == 16
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;尻尾コキ(ターゲット・される側。要ペニス)
;-------------------------------------------------
IF SELECTCOM == 17
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;指フェラ(ターゲット・しゃぶられる側)
;-------------------------------------------------
IF SELECTCOM == 24
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;ニプルファック(ターゲット・ファックされる側)
;-------------------------------------------------
IF SELECTCOM == 25
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;正常位(ターゲット・挿れられる側)
;-------------------------------------------------
IF SELECTCOM == 30
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;処女
			IF CFLAG:252 == 1
				;主人公が童貞。特に無ければ「童貞でない」場合のコピペでよい
				IF CFLAG:254 == 1
					;恋慕 or 服従
					IF TALENT:恋慕 || TALENT:服従
						;PRINTFORMW
					;それ以外
					ELSE
						;PRINTFORMW
					ENDIF
				;主人公が童貞でない
				ELSE
					;恋慕 or 服従
					IF TALENT:恋慕 || TALENT:服従
						;PRINTFORMW
					;それ以外
					ELSE
						;PRINTFORMW
					ENDIF
				ENDIF
			;非処女
			ELSE
				;主人公が童貞。特に無ければ「童貞でない」場合のコピペでよい
				IF CFLAG:254 == 1
					;恋慕 or 服従
					IF TALENT:恋慕 || TALENT:服従
						;PRINTFORMW
					;それ以外
					ELSE
						;PRINTFORMW
					ENDIF
				;主人公が童貞でない
				ELSE
					;恋慕 or 服従
					IF TALENT:恋慕 || TALENT:服従
						;PRINTFORMW
					;それ以外
					ELSE
						;PRINTFORMW
					ENDIF
				ENDIF
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;Ｖ感Lv3以上
				IF ABL:Ｖ感 >= 3
					;PRINTFORMW
				;Ｖ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;Ｖ感Lv3以上
				IF ABL:Ｖ感 >= 3
					;PRINTFORMW
				;Ｖ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;処女
			IF CFLAG:252 == 1
				;主人公が童貞。特に無ければ「童貞でない」場合のコピペでよい
				IF CFLAG:254 == 1
					;恋慕 or 服従
					IF TALENT:恋慕 || TALENT:服従
						;PRINTFORMW
					;それ以外
					ELSE
						;PRINTFORMW
					ENDIF
				;主人公が童貞でない
				ELSE
					;恋慕 or 服従
					IF TALENT:恋慕 || TALENT:服従
						;PRINTFORMW
					;それ以外
					ELSE
						;PRINTFORMW
					ENDIF
				ENDIF
			;非処女
			ELSE
				;主人公が童貞。特に無ければ「童貞でない」場合のコピペでよい
				IF CFLAG:254 == 1
					;恋慕 or 服従
					IF TALENT:恋慕 || TALENT:服従
						;PRINTFORMW
					;それ以外
					ELSE
						;PRINTFORMW
					ENDIF
				;主人公が童貞でない
				ELSE
					;恋慕 or 服従
					IF TALENT:恋慕 || TALENT:服従
						;PRINTFORMW
					;それ以外
					ELSE
						;PRINTFORMW
					ENDIF
				ENDIF
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;Ｖ感Lv3以上
				IF ABL:Ｖ感 >= 3
					;PRINTFORMW
				;Ｖ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;Ｖ感Lv3以上
				IF ABL:Ｖ感 >= 3
					;PRINTFORMW
				;Ｖ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;後背位(ターゲット・挿れられる側)
;-------------------------------------------------
IF SELECTCOM == 31
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;処女
			IF CFLAG:252 == 1
				;主人公が童貞。特に無ければ「童貞でない」場合のコピペでよい
				IF CFLAG:254 == 1
					;恋慕 or 服従
					IF TALENT:恋慕 || TALENT:服従
						;PRINTFORMW
					;それ以外
					ELSE
						;PRINTFORMW
					ENDIF
				;主人公が童貞でない
				ELSE
					;恋慕 or 服従
					IF TALENT:恋慕 || TALENT:服従
						;PRINTFORMW
					;それ以外
					ELSE
						;PRINTFORMW
					ENDIF
				ENDIF
			;非処女
			ELSE
				;主人公が童貞。特に無ければ「童貞でない」場合のコピペでよい
				IF CFLAG:254 == 1
					;恋慕 or 服従
					IF TALENT:恋慕 || TALENT:服従
						;PRINTFORMW
					;それ以外
					ELSE
						;PRINTFORMW
					ENDIF
				;主人公が童貞でない
				ELSE
					;恋慕 or 服従
					IF TALENT:恋慕 || TALENT:服従
						;PRINTFORMW
					;それ以外
					ELSE
						;PRINTFORMW
					ENDIF
				ENDIF
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;Ｖ感Lv3以上
				IF ABL:Ｖ感 >= 3
					;PRINTFORMW
				;Ｖ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;Ｖ感Lv3以上
				IF ABL:Ｖ感 >= 3
					;PRINTFORMW
				;Ｖ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;処女
			IF CFLAG:252 == 1
				;主人公が童貞。特に無ければ「童貞でない」場合のコピペでよい
				IF CFLAG:254 == 1
					;恋慕 or 服従
					IF TALENT:恋慕 || TALENT:服従
						;PRINTFORMW
					;それ以外
					ELSE
						;PRINTFORMW
					ENDIF
				;主人公が童貞でない
				ELSE
					;恋慕 or 服従
					IF TALENT:恋慕 || TALENT:服従
						;PRINTFORMW
					;それ以外
					ELSE
						;PRINTFORMW
					ENDIF
				ENDIF
			;非処女
			ELSE
				;主人公が童貞。特に無ければ「童貞でない」場合のコピペでよい
				IF CFLAG:254 == 1
					;恋慕 or 服従
					IF TALENT:恋慕 || TALENT:服従
						;PRINTFORMW
					;それ以外
					ELSE
						;PRINTFORMW
					ENDIF
				;主人公が童貞でない
				ELSE
					;恋慕 or 服従
					IF TALENT:恋慕 || TALENT:服従
						;PRINTFORMW
					;それ以外
					ELSE
						;PRINTFORMW
					ENDIF
				ENDIF
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;Ｖ感Lv3以上
				IF ABL:Ｖ感 >= 3
					;PRINTFORMW
				;Ｖ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;Ｖ感Lv3以上
				IF ABL:Ｖ感 >= 3
					;PRINTFORMW
				;Ｖ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;対面座位(ターゲット・挿れられる側)
;-------------------------------------------------
IF SELECTCOM == 32
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;処女
			IF CFLAG:252 == 1
				;主人公が童貞。特に無ければ「童貞でない」場合のコピペでよい
				IF CFLAG:254 == 1
					;恋慕 or 服従
					IF TALENT:恋慕 || TALENT:服従
						;PRINTFORMW
					;それ以外
					ELSE
						;PRINTFORMW
					ENDIF
				;主人公が童貞でない
				ELSE
					;恋慕 or 服従
					IF TALENT:恋慕 || TALENT:服従
						;PRINTFORMW
					;それ以外
					ELSE
						;PRINTFORMW
					ENDIF
				ENDIF
			;非処女
			ELSE
				;主人公が童貞。特に無ければ「童貞でない」場合のコピペでよい
				IF CFLAG:254 == 1
					;恋慕 or 服従
					IF TALENT:恋慕 || TALENT:服従
						;PRINTFORMW
					;それ以外
					ELSE
						;PRINTFORMW
					ENDIF
				;主人公が童貞でない
				ELSE
					;恋慕 or 服従
					IF TALENT:恋慕 || TALENT:服従
						;PRINTFORMW
					;それ以外
					ELSE
						;PRINTFORMW
					ENDIF
				ENDIF
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;Ｖ感Lv3以上
				IF ABL:Ｖ感 >= 3
					;PRINTFORMW
				;Ｖ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;Ｖ感Lv3以上
				IF ABL:Ｖ感 >= 3
					;PRINTFORMW
				;Ｖ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;処女
			IF CFLAG:252 == 1
				;主人公が童貞。特に無ければ「童貞でない」場合のコピペでよい
				IF CFLAG:254 == 1
					;恋慕 or 服従
					IF TALENT:恋慕 || TALENT:服従
						;PRINTFORMW
					;それ以外
					ELSE
						;PRINTFORMW
					ENDIF
				;主人公が童貞でない
				ELSE
					;恋慕 or 服従
					IF TALENT:恋慕 || TALENT:服従
						;PRINTFORMW
					;それ以外
					ELSE
						;PRINTFORMW
					ENDIF
				ENDIF
			;非処女
			ELSE
				;主人公が童貞。特に無ければ「童貞でない」場合のコピペでよい
				IF CFLAG:254 == 1
					;恋慕 or 服従
					IF TALENT:恋慕 || TALENT:服従
						;PRINTFORMW
					;それ以外
					ELSE
						;PRINTFORMW
					ENDIF
				;主人公が童貞でない
				ELSE
					;恋慕 or 服従
					IF TALENT:恋慕 || TALENT:服従
						;PRINTFORMW
					;それ以外
					ELSE
						;PRINTFORMW
					ENDIF
				ENDIF
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;Ｖ感Lv3以上
				IF ABL:Ｖ感 >= 3
					;PRINTFORMW
				;Ｖ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;Ｖ感Lv3以上
				IF ABL:Ｖ感 >= 3
					;PRINTFORMW
				;Ｖ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;背面座位(ターゲット・挿れられる側)
;-------------------------------------------------
IF SELECTCOM == 33
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;処女
			IF CFLAG:252 == 1
				;主人公が童貞。特に無ければ「童貞でない」場合のコピペでよい
				IF CFLAG:254 == 1
					;恋慕 or 服従
					IF TALENT:恋慕 || TALENT:服従
						;PRINTFORMW
					;それ以外
					ELSE
						;PRINTFORMW
					ENDIF
				;主人公が童貞でない
				ELSE
					;恋慕 or 服従
					IF TALENT:恋慕 || TALENT:服従
						;PRINTFORMW
					;それ以外
					ELSE
						;PRINTFORMW
					ENDIF
				ENDIF
			;非処女
			ELSE
				;主人公が童貞。特に無ければ「童貞でない」場合のコピペでよい
				IF CFLAG:254 == 1
					;恋慕 or 服従
					IF TALENT:恋慕 || TALENT:服従
						;PRINTFORMW
					;それ以外
					ELSE
						;PRINTFORMW
					ENDIF
				;主人公が童貞でない
				ELSE
					;恋慕 or 服従
					IF TALENT:恋慕 || TALENT:服従
						;PRINTFORMW
					;それ以外
					ELSE
						;PRINTFORMW
					ENDIF
				ENDIF
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;Ｖ感Lv3以上
				IF ABL:Ｖ感 >= 3
					;PRINTFORMW
				;Ｖ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;Ｖ感Lv3以上
				IF ABL:Ｖ感 >= 3
					;PRINTFORMW
				;Ｖ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;処女
			IF CFLAG:252 == 1
				;主人公が童貞。特に無ければ「童貞でない」場合のコピペでよい
				IF CFLAG:254 == 1
					;恋慕 or 服従
					IF TALENT:恋慕 || TALENT:服従
						;PRINTFORMW
					;それ以外
					ELSE
						;PRINTFORMW
					ENDIF
				;主人公が童貞でない
				ELSE
					;恋慕 or 服従
					IF TALENT:恋慕 || TALENT:服従
						;PRINTFORMW
					;それ以外
					ELSE
						;PRINTFORMW
					ENDIF
				ENDIF
			;非処女
			ELSE
				;主人公が童貞。特に無ければ「童貞でない」場合のコピペでよい
				IF CFLAG:254 == 1
					;恋慕 or 服従
					IF TALENT:恋慕 || TALENT:服従
						;PRINTFORMW
					;それ以外
					ELSE
						;PRINTFORMW
					ENDIF
				;主人公が童貞でない
				ELSE
					;恋慕 or 服従
					IF TALENT:恋慕 || TALENT:服従
						;PRINTFORMW
					;それ以外
					ELSE
						;PRINTFORMW
					ENDIF
				ENDIF
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;Ｖ感Lv3以上
				IF ABL:Ｖ感 >= 3
					;PRINTFORMW
				;Ｖ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;Ｖ感Lv3以上
				IF ABL:Ｖ感 >= 3
					;PRINTFORMW
				;Ｖ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;騎乗位(ターゲット・挿れられる側)
;-------------------------------------------------
IF SELECTCOM == 34
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;処女
			IF CFLAG:252 == 1
				;主人公が童貞。特に無ければ「童貞でない」場合のコピペでよい
				IF CFLAG:254 == 1
					;恋慕 or 服従
					IF TALENT:恋慕 || TALENT:服従
						;PRINTFORMW
					;それ以外
					ELSE
						;PRINTFORMW
					ENDIF
				;主人公が童貞でない
				ELSE
					;恋慕 or 服従
					IF TALENT:恋慕 || TALENT:服従
						;PRINTFORMW
					;それ以外
					ELSE
						;PRINTFORMW
					ENDIF
				ENDIF
			;非処女
			ELSE
				;主人公が童貞。特に無ければ「童貞でない」場合のコピペでよい
				IF CFLAG:254 == 1
					;恋慕 or 服従
					IF TALENT:恋慕 || TALENT:服従
						;PRINTFORMW
					;それ以外
					ELSE
						;PRINTFORMW
					ENDIF
				;主人公が童貞でない
				ELSE
					;恋慕 or 服従
					IF TALENT:恋慕 || TALENT:服従
						;PRINTFORMW
					;それ以外
					ELSE
						;PRINTFORMW
					ENDIF
				ENDIF
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;Ｖ感Lv3以上
				IF ABL:Ｖ感 >= 3
					;PRINTFORMW
				;Ｖ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;Ｖ感Lv3以上
				IF ABL:Ｖ感 >= 3
					;PRINTFORMW
				;Ｖ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;処女
			IF CFLAG:252 == 1
				;主人公が童貞。特に無ければ「童貞でない」場合のコピペでよい
				IF CFLAG:254 == 1
					;恋慕 or 服従
					IF TALENT:恋慕 || TALENT:服従
						;PRINTFORMW
					;それ以外
					ELSE
						;PRINTFORMW
					ENDIF
				;主人公が童貞でない
				ELSE
					;恋慕 or 服従
					IF TALENT:恋慕 || TALENT:服従
						;PRINTFORMW
					;それ以外
					ELSE
						;PRINTFORMW
					ENDIF
				ENDIF
			;非処女
			ELSE
				;主人公が童貞。特に無ければ「童貞でない」場合のコピペでよい
				IF CFLAG:254 == 1
					;恋慕 or 服従
					IF TALENT:恋慕 || TALENT:服従
						;PRINTFORMW
					;それ以外
					ELSE
						;PRINTFORMW
					ENDIF
				;主人公が童貞でない
				ELSE
					;恋慕 or 服従
					IF TALENT:恋慕 || TALENT:服従
						;PRINTFORMW
					;それ以外
					ELSE
						;PRINTFORMW
					ENDIF
				ENDIF
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;Ｖ感Lv3以上
				IF ABL:Ｖ感 >= 3
					;PRINTFORMW
				;Ｖ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;Ｖ感Lv3以上
				IF ABL:Ｖ感 >= 3
					;PRINTFORMW
				;Ｖ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;背面騎乗位(ターゲット・挿れられる側)
;-------------------------------------------------
IF SELECTCOM == 35
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;処女
			IF CFLAG:252 == 1
				;主人公が童貞。特に無ければ「童貞でない」場合のコピペでよい
				IF CFLAG:254 == 1
					;恋慕 or 服従
					IF TALENT:恋慕 || TALENT:服従
						;PRINTFORMW
					;それ以外
					ELSE
						;PRINTFORMW
					ENDIF
				;主人公が童貞でない
				ELSE
					;恋慕 or 服従
					IF TALENT:恋慕 || TALENT:服従
						;PRINTFORMW
					;それ以外
					ELSE
						;PRINTFORMW
					ENDIF
				ENDIF
			;非処女
			ELSE
				;主人公が童貞。特に無ければ「童貞でない」場合のコピペでよい
				IF CFLAG:254 == 1
					;恋慕 or 服従
					IF TALENT:恋慕 || TALENT:服従
						;PRINTFORMW
					;それ以外
					ELSE
						;PRINTFORMW
					ENDIF
				;主人公が童貞でない
				ELSE
					;恋慕 or 服従
					IF TALENT:恋慕 || TALENT:服従
						;PRINTFORMW
					;それ以外
					ELSE
						;PRINTFORMW
					ENDIF
				ENDIF
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;Ｖ感Lv3以上
				IF ABL:Ｖ感 >= 3
					;PRINTFORMW
				;Ｖ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;Ｖ感Lv3以上
				IF ABL:Ｖ感 >= 3
					;PRINTFORMW
				;Ｖ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;処女
			IF CFLAG:252 == 1
				;恋慕 or 服従
				IF TALENT:恋慕 || TALENT:服従
					;PRINTFORMW
				;それ以外
				ELSE
					;PRINTFORMW
				ENDIF
			;非処女
			ELSE
				;恋慕 or 服従
				IF TALENT:恋慕 || TALENT:服従
					;PRINTFORMW
				;それ以外
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;Ｖ感Lv3以上
				IF ABL:Ｖ感 >= 3
					;PRINTFORMW
				;Ｖ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;Ｖ感Lv3以上
				IF ABL:Ｖ感 >= 3
					;PRINTFORMW
				;Ｖ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;二本挿入(ターゲット・挿れられる側)
;-------------------------------------------------
IF SELECTCOM == 36
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;Ｖ拡張経験50以上
				IF EXP:Ｖ拡張経験 >= 50
					;PRINTFORMW
				;Ｖ拡張経験50未満
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;Ｖ拡張経験50以上
				IF EXP:Ｖ拡張経験 >= 50
					;PRINTFORMW
				;Ｖ拡張経験50未満
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;Ｖ拡張経験50以上
				IF EXP:Ｖ拡張経験 >= 50
					;PRINTFORMW
				;Ｖ拡張経験50未満
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;Ｖ拡張経験50以上
				IF EXP:Ｖ拡張経験 >= 50
					;PRINTFORMW
				;Ｖ拡張経験50未満
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;側位(ターゲット・挿れられる側)
;-------------------------------------------------
IF SELECTCOM == 37
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;処女
			IF CFLAG:252 == 1
				;主人公が童貞。特に無ければ「童貞でない」場合のコピペでよい
				IF CFLAG:254 == 1
					;恋慕 or 服従
					IF TALENT:恋慕 || TALENT:服従
						;PRINTFORMW
					;それ以外
					ELSE
						;PRINTFORMW
					ENDIF
				;主人公が童貞でない
				ELSE
					;恋慕 or 服従
					IF TALENT:恋慕 || TALENT:服従
						;PRINTFORMW
					;それ以外
					ELSE
						;PRINTFORMW
					ENDIF
				ENDIF
			;非処女
			ELSE
				;主人公が童貞。特に無ければ「童貞でない」場合のコピペでよい
				IF CFLAG:254 == 1
					;恋慕 or 服従
					IF TALENT:恋慕 || TALENT:服従
						;PRINTFORMW
					;それ以外
					ELSE
						;PRINTFORMW
					ENDIF
				;主人公が童貞でない
				ELSE
					;恋慕 or 服従
					IF TALENT:恋慕 || TALENT:服従
						;PRINTFORMW
					;それ以外
					ELSE
						;PRINTFORMW
					ENDIF
				ENDIF
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;Ｖ感Lv3以上
				IF ABL:Ｖ感 >= 3
					;PRINTFORMW
				;Ｖ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;Ｖ感Lv3以上
				IF ABL:Ｖ感 >= 3
					;PRINTFORMW
				;Ｖ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;処女
			IF CFLAG:252 == 1
				;主人公が童貞。特に無ければ「童貞でない」場合のコピペでよい
				IF CFLAG:254 == 1
					;恋慕 or 服従
					IF TALENT:恋慕 || TALENT:服従
						;PRINTFORMW
					;それ以外
					ELSE
						;PRINTFORMW
					ENDIF
				;主人公が童貞でない
				ELSE
					;恋慕 or 服従
					IF TALENT:恋慕 || TALENT:服従
						;PRINTFORMW
					;それ以外
					ELSE
						;PRINTFORMW
					ENDIF
				ENDIF
			;非処女
			ELSE
				;主人公が童貞。特に無ければ「童貞でない」場合のコピペでよい
				IF CFLAG:254 == 1
					;恋慕 or 服従
					IF TALENT:恋慕 || TALENT:服従
						;PRINTFORMW
					;それ以外
					ELSE
						;PRINTFORMW
					ENDIF
				;主人公が童貞でない
				ELSE
					;恋慕 or 服従
					IF TALENT:恋慕 || TALENT:服従
						;PRINTFORMW
					;それ以外
					ELSE
						;PRINTFORMW
					ENDIF
				ENDIF
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;Ｖ感Lv3以上
				IF ABL:Ｖ感 >= 3
					;PRINTFORMW
				;Ｖ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;Ｖ感Lv3以上
				IF ABL:Ｖ感 >= 3
					;PRINTFORMW
				;Ｖ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;屈曲位(ターゲット・挿れられる側)
;-------------------------------------------------
IF SELECTCOM == 38
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;処女
			IF CFLAG:252 == 1
				;主人公が童貞。特に無ければ「童貞でない」場合のコピペでよい
				IF CFLAG:254 == 1
					;恋慕 or 服従
					IF TALENT:恋慕 || TALENT:服従
						;PRINTFORMW
					;それ以外
					ELSE
						;PRINTFORMW
					ENDIF
				;主人公が童貞でない
				ELSE
					;恋慕 or 服従
					IF TALENT:恋慕 || TALENT:服従
						;PRINTFORMW
					;それ以外
					ELSE
						;PRINTFORMW
					ENDIF
				ENDIF
			;非処女
			ELSE
				;主人公が童貞。特に無ければ「童貞でない」場合のコピペでよい
				IF CFLAG:254 == 1
					;恋慕 or 服従
					IF TALENT:恋慕 || TALENT:服従
						;PRINTFORMW
					;それ以外
					ELSE
						;PRINTFORMW
					ENDIF
				;主人公が童貞でない
				ELSE
					;恋慕 or 服従
					IF TALENT:恋慕 || TALENT:服従
						;PRINTFORMW
					;それ以外
					ELSE
						;PRINTFORMW
					ENDIF
				ENDIF
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;Ｖ感Lv3以上
				IF ABL:Ｖ感 >= 3
					;PRINTFORMW
				;Ｖ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;Ｖ感Lv3以上
				IF ABL:Ｖ感 >= 3
					;PRINTFORMW
				;Ｖ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;処女
			IF CFLAG:252 == 1
				;主人公が童貞。特に無ければ「童貞でない」場合のコピペでよい
				IF CFLAG:254 == 1
					;恋慕 or 服従
					IF TALENT:恋慕 || TALENT:服従
						;PRINTFORMW
					;それ以外
					ELSE
						;PRINTFORMW
					ENDIF
				;主人公が童貞でない
				ELSE
					;恋慕 or 服従
					IF TALENT:恋慕 || TALENT:服従
						;PRINTFORMW
					;それ以外
					ELSE
						;PRINTFORMW
					ENDIF
				ENDIF
			;非処女
			ELSE
				;主人公が童貞。特に無ければ「童貞でない」場合のコピペでよい
				IF CFLAG:254 == 1
					;恋慕 or 服従
					IF TALENT:恋慕 || TALENT:服従
						;PRINTFORMW
					;それ以外
					ELSE
						;PRINTFORMW
					ENDIF
				;主人公が童貞でない
				ELSE
					;恋慕 or 服従
					IF TALENT:恋慕 || TALENT:服従
						;PRINTFORMW
					;それ以外
					ELSE
						;PRINTFORMW
					ENDIF
				ENDIF
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;Ｖ感Lv3以上
				IF ABL:Ｖ感 >= 3
					;PRINTFORMW
				;Ｖ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;Ｖ感Lv3以上
				IF ABL:Ｖ感 >= 3
					;PRINTFORMW
				;Ｖ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;駅弁(ターゲット・挿れられる側)
;-------------------------------------------------
IF SELECTCOM == 39
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;処女
			IF CFLAG:252 == 1
				;主人公が童貞。特に無ければ「童貞でない」場合のコピペでよい
				IF CFLAG:254 == 1
					;恋慕 or 服従
					IF TALENT:恋慕 || TALENT:服従
						;PRINTFORMW
					;それ以外
					ELSE
						;PRINTFORMW
					ENDIF
				;主人公が童貞でない
				ELSE
					;恋慕 or 服従
					IF TALENT:恋慕 || TALENT:服従
						;PRINTFORMW
					;それ以外
					ELSE
						;PRINTFORMW
					ENDIF
				ENDIF
			;非処女
			ELSE
				;主人公が童貞。特に無ければ「童貞でない」場合のコピペでよい
				IF CFLAG:254 == 1
					;恋慕 or 服従
					IF TALENT:恋慕 || TALENT:服従
						;PRINTFORMW
					;それ以外
					ELSE
						;PRINTFORMW
					ENDIF
				;主人公が童貞でない
				ELSE
					;恋慕 or 服従
					IF TALENT:恋慕 || TALENT:服従
						;PRINTFORMW
					;それ以外
					ELSE
						;PRINTFORMW
					ENDIF
				ENDIF
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;Ｖ感Lv3以上
				IF ABL:Ｖ感 >= 3
					;PRINTFORMW
				;Ｖ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;Ｖ感Lv3以上
				IF ABL:Ｖ感 >= 3
					;PRINTFORMW
				;Ｖ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;処女
			IF CFLAG:252 == 1
				;主人公が童貞。特に無ければ「童貞でない」場合のコピペでよい
				IF CFLAG:254 == 1
					;恋慕 or 服従
					IF TALENT:恋慕 || TALENT:服従
						;PRINTFORMW
					;それ以外
					ELSE
						;PRINTFORMW
					ENDIF
				;主人公が童貞でない
				ELSE
					;恋慕 or 服従
					IF TALENT:恋慕 || TALENT:服従
						;PRINTFORMW
					;それ以外
					ELSE
						;PRINTFORMW
					ENDIF
				ENDIF
			;非処女
			ELSE
				;主人公が童貞。特に無ければ「童貞でない」場合のコピペでよい
				IF CFLAG:254 == 1
					;恋慕 or 服従
					IF TALENT:恋慕 || TALENT:服従
						;PRINTFORMW
					;それ以外
					ELSE
						;PRINTFORMW
					ENDIF
				;主人公が童貞でない
				ELSE
					;恋慕 or 服従
					IF TALENT:恋慕 || TALENT:服従
						;PRINTFORMW
					;それ以外
					ELSE
						;PRINTFORMW
					ENDIF
				ENDIF
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;Ｖ感Lv3以上
				IF ABL:Ｖ感 >= 3
					;PRINTFORMW
				;Ｖ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;Ｖ感Lv3以上
				IF ABL:Ｖ感 >= 3
					;PRINTFORMW
				;Ｖ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;背面駅弁(ターゲット・挿れられる側)
;-------------------------------------------------
IF SELECTCOM == 52
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;処女
			IF CFLAG:252 == 1
				;主人公が童貞。特に無ければ「童貞でない」場合のコピペでよい
				IF CFLAG:254 == 1
					;恋慕 or 服従
					IF TALENT:恋慕 || TALENT:服従
						;PRINTFORMW
					;それ以外
					ELSE
						;PRINTFORMW
					ENDIF
				;主人公が童貞でない
				ELSE
					;恋慕 or 服従
					IF TALENT:恋慕 || TALENT:服従
						;PRINTFORMW
					;それ以外
					ELSE
						;PRINTFORMW
					ENDIF
				ENDIF
			;非処女
			ELSE
				;主人公が童貞。特に無ければ「童貞でない」場合のコピペでよい
				IF CFLAG:254 == 1
					;恋慕 or 服従
					IF TALENT:恋慕 || TALENT:服従
						;PRINTFORMW
					;それ以外
					ELSE
						;PRINTFORMW
					ENDIF
				;主人公が童貞でない
				ELSE
					;恋慕 or 服従
					IF TALENT:恋慕 || TALENT:服従
						;PRINTFORMW
					;それ以外
					ELSE
						;PRINTFORMW
					ENDIF
				ENDIF
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;Ｖ感Lv3以上
				IF ABL:Ｖ感 >= 3
					;PRINTFORMW
				;Ｖ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;Ｖ感Lv3以上
				IF ABL:Ｖ感 >= 3
					;PRINTFORMW
				;Ｖ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;処女
			IF CFLAG:252 == 1
				;主人公が童貞。特に無ければ「童貞でない」場合のコピペでよい
				IF CFLAG:254 == 1
					;恋慕 or 服従
					IF TALENT:恋慕 || TALENT:服従
						;PRINTFORMW
					;それ以外
					ELSE
						;PRINTFORMW
					ENDIF
				;主人公が童貞でない
				ELSE
					;恋慕 or 服従
					IF TALENT:恋慕 || TALENT:服従
						;PRINTFORMW
					;それ以外
					ELSE
						;PRINTFORMW
					ENDIF
				ENDIF
			;非処女
			ELSE
				;主人公が童貞。特に無ければ「童貞でない」場合のコピペでよい
				IF CFLAG:254 == 1
					;恋慕 or 服従
					IF TALENT:恋慕 || TALENT:服従
						;PRINTFORMW
					;それ以外
					ELSE
						;PRINTFORMW
					ENDIF
				;主人公が童貞でない
				ELSE
					;恋慕 or 服従
					IF TALENT:恋慕 || TALENT:服従
						;PRINTFORMW
					;それ以外
					ELSE
						;PRINTFORMW
					ENDIF
				ENDIF
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;Ｖ感Lv3以上
				IF ABL:Ｖ感 >= 3
					;PRINTFORMW
				;Ｖ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;Ｖ感Lv3以上
				IF ABL:Ｖ感 >= 3
					;PRINTFORMW
				;Ｖ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;立ちバック(ターゲット・挿れられる側)
;-------------------------------------------------
IF SELECTCOM == 160
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;処女
			IF CFLAG:252 == 1
				;主人公が童貞。特に無ければ「童貞でない」場合のコピペでよい
				IF CFLAG:254 == 1
					;恋慕 or 服従
					IF TALENT:恋慕 || TALENT:服従
						;PRINTFORMW
					;それ以外
					ELSE
						;PRINTFORMW
					ENDIF
				;主人公が童貞でない
				ELSE
					;恋慕 or 服従
					IF TALENT:恋慕 || TALENT:服従
						;PRINTFORMW
					;それ以外
					ELSE
						;PRINTFORMW
					ENDIF
				ENDIF
			;非処女
			ELSE
				;主人公が童貞。特に無ければ「童貞でない」場合のコピペでよい
				IF CFLAG:254 == 1
					;恋慕 or 服従
					IF TALENT:恋慕 || TALENT:服従
						;PRINTFORMW
					;それ以外
					ELSE
						;PRINTFORMW
					ENDIF
				;主人公が童貞でない
				ELSE
					;恋慕 or 服従
					IF TALENT:恋慕 || TALENT:服従
						;PRINTFORMW
					;それ以外
					ELSE
						;PRINTFORMW
					ENDIF
				ENDIF
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;Ｖ感Lv3以上
				IF ABL:Ｖ感 >= 3
					;PRINTFORMW
				;Ｖ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;Ｖ感Lv3以上
				IF ABL:Ｖ感 >= 3
					;PRINTFORMW
				;Ｖ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;処女
			IF CFLAG:252 == 1
				;主人公が童貞。特に無ければ「童貞でない」場合のコピペでよい
				IF CFLAG:254 == 1
					;恋慕 or 服従
					IF TALENT:恋慕 || TALENT:服従
						;PRINTFORMW
					;それ以外
					ELSE
						;PRINTFORMW
					ENDIF
				;主人公が童貞でない
				ELSE
					;恋慕 or 服従
					IF TALENT:恋慕 || TALENT:服従
						;PRINTFORMW
					;それ以外
					ELSE
						;PRINTFORMW
					ENDIF
				ENDIF
			;非処女
			ELSE
				;主人公が童貞。特に無ければ「童貞でない」場合のコピペでよい
				IF CFLAG:254 == 1
					;恋慕 or 服従
					IF TALENT:恋慕 || TALENT:服従
						;PRINTFORMW
					;それ以外
					ELSE
						;PRINTFORMW
					ENDIF
				;主人公が童貞でない
				ELSE
					;恋慕 or 服従
					IF TALENT:恋慕 || TALENT:服従
						;PRINTFORMW
					;それ以外
					ELSE
						;PRINTFORMW
					ENDIF
				ENDIF
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;Ｖ感Lv3以上
				IF ABL:Ｖ感 >= 3
					;PRINTFORMW
				;Ｖ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;Ｖ感Lv3以上
				IF ABL:Ｖ感 >= 3
					;PRINTFORMW
				;Ｖ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;Ａ正常位(ターゲット・挿れられる側)
;-------------------------------------------------
IF SELECTCOM == 40
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;Ａ感Lv3以上
				IF ABL:Ａ感 >= 3
					;PRINTFORMW
				;Ａ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;Ａ感Lv3以上
				IF ABL:Ａ感 >= 3
					;PRINTFORMW
				;Ａ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;Ａ感Lv3以上
				IF ABL:Ａ感 >= 3
					;PRINTFORMW
				;Ａ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;Ａ感Lv3以上
				IF ABL:Ａ感 >= 3
					;PRINTFORMW
				;Ａ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;Ａ後背位(ターゲット・挿れられる側)
;-------------------------------------------------
IF SELECTCOM == 41
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;Ａ感Lv3以上
				IF ABL:Ａ感 >= 3
					;PRINTFORMW
				;Ａ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;Ａ感Lv3以上
				IF ABL:Ａ感 >= 3
					;PRINTFORMW
				;Ａ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;Ａ感Lv3以上
				IF ABL:Ａ感 >= 3
					;PRINTFORMW
				;Ａ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;Ａ感Lv3以上
				IF ABL:Ａ感 >= 3
					;PRINTFORMW
				;Ａ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;Ａ対面座位(ターゲット・挿れられる側)
;-------------------------------------------------
IF SELECTCOM == 42
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;Ａ感Lv3以上
				IF ABL:Ａ感 >= 3
					;PRINTFORMW
				;Ａ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;Ａ感Lv3以上
				IF ABL:Ａ感 >= 3
					;PRINTFORMW
				;Ａ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;Ａ感Lv3以上
				IF ABL:Ａ感 >= 3
					;PRINTFORMW
				;Ａ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;Ａ感Lv3以上
				IF ABL:Ａ感 >= 3
					;PRINTFORMW
				;Ａ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;Ａ背面座位(ターゲット・挿れられる側)
;-------------------------------------------------
IF SELECTCOM == 43
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;Ａ感Lv3以上
				IF ABL:Ａ感 >= 3
					;PRINTFORMW
				;Ａ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;Ａ感Lv3以上
				IF ABL:Ａ感 >= 3
					;PRINTFORMW
				;Ａ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;Ａ感Lv3以上
				IF ABL:Ａ感 >= 3
					;PRINTFORMW
				;Ａ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;Ａ感Lv3以上
				IF ABL:Ａ感 >= 3
					;PRINTFORMW
				;Ａ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;Ａ騎乗位(ターゲット・挿れられる側)
;-------------------------------------------------
IF SELECTCOM == 44
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;Ａ感Lv3以上
				IF ABL:Ａ感 >= 3
					;PRINTFORMW
				;Ａ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;Ａ感Lv3以上
				IF ABL:Ａ感 >= 3
					;PRINTFORMW
				;Ａ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;Ａ感Lv3以上
				IF ABL:Ａ感 >= 3
					;PRINTFORMW
				;Ａ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;Ａ感Lv3以上
				IF ABL:Ａ感 >= 3
					;PRINTFORMW
				;Ａ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;Ａ背面騎乗位(ターゲット・挿れられる側)
;-------------------------------------------------
IF SELECTCOM == 45
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;Ａ感Lv3以上
				IF ABL:Ａ感 >= 3
					;PRINTFORMW
				;Ａ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;Ａ感Lv3以上
				IF ABL:Ａ感 >= 3
					;PRINTFORMW
				;Ａ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;Ａ感Lv3以上
				IF ABL:Ａ感 >= 3
					;PRINTFORMW
				;Ａ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;Ａ感Lv3以上
				IF ABL:Ａ感 >= 3
					;PRINTFORMW
				;Ａ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;Ａ二本挿入(ターゲット・挿れられる側)
;-------------------------------------------------
IF SELECTCOM == 46
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;Ａ拡張経験50以上
				IF EXP:Ａ拡張経験 >= 50
					;PRINTFORMW
				;Ａ拡張経験50未満
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;Ａ拡張経験50以上
				IF EXP:Ａ拡張経験 >= 50
					;PRINTFORMW
				;Ａ拡張経験50未満
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;Ａ拡張経験50以上
				IF EXP:Ａ拡張経験 >= 50
					;PRINTFORMW
				;Ａ拡張経験50未満
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;Ａ拡張経験50以上
				IF EXP:Ａ拡張経験 >= 50
					;PRINTFORMW
				;Ａ拡張経験50未満
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;Ａ側位(ターゲット・挿れられる側)
;-------------------------------------------------
IF SELECTCOM == 47
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;Ａ感Lv3以上
				IF ABL:Ａ感 >= 3
					;PRINTFORMW
				;Ａ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;Ａ感Lv3以上
				IF ABL:Ａ感 >= 3
					;PRINTFORMW
				;Ａ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;Ａ感Lv3以上
				IF ABL:Ａ感 >= 3
					;PRINTFORMW
				;Ａ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;Ａ感Lv3以上
				IF ABL:Ａ感 >= 3
					;PRINTFORMW
				;Ａ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;Ａ屈曲位(ターゲット・挿れられる側)
;-------------------------------------------------
IF SELECTCOM == 48
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;Ａ感Lv3以上
				IF ABL:Ａ感 >= 3
					;PRINTFORMW
				;Ａ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;Ａ感Lv3以上
				IF ABL:Ａ感 >= 3
					;PRINTFORMW
				;Ａ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;Ａ感Lv3以上
				IF ABL:Ａ感 >= 3
					;PRINTFORMW
				;Ａ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;Ａ感Lv3以上
				IF ABL:Ａ感 >= 3
					;PRINTFORMW
				;Ａ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;Ａ駅弁(ターゲット・挿れられる側)
;-------------------------------------------------
IF SELECTCOM == 49
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;Ａ感Lv3以上
				IF ABL:Ａ感 >= 3
					;PRINTFORMW
				;Ａ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;Ａ感Lv3以上
				IF ABL:Ａ感 >= 3
					;PRINTFORMW
				;Ａ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;Ａ感Lv3以上
				IF ABL:Ａ感 >= 3
					;PRINTFORMW
				;Ａ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;Ａ感Lv3以上
				IF ABL:Ａ感 >= 3
					;PRINTFORMW
				;Ａ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;Ａ背面駅弁(ターゲット・挿れられる側)
;-------------------------------------------------
IF SELECTCOM == 53
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;Ａ感Lv3以上
				IF ABL:Ａ感 >= 3
					;PRINTFORMW
				;Ａ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;Ａ感Lv3以上
				IF ABL:Ａ感 >= 3
					;PRINTFORMW
				;Ａ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;Ａ感Lv3以上
				IF ABL:Ａ感 >= 3
					;PRINTFORMW
				;Ａ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;Ａ感Lv3以上
				IF ABL:Ａ感 >= 3
					;PRINTFORMW
				;Ａ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;Ａ立ちバック(ターゲット・挿れられる側)
;-------------------------------------------------
IF SELECTCOM == 161
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;Ａ感Lv3以上
				IF ABL:Ａ感 >= 3
					;PRINTFORMW
				;Ａ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;Ａ感Lv3以上
				IF ABL:Ａ感 >= 3
					;PRINTFORMW
				;Ａ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;Ａ感Lv3以上
				IF ABL:Ａ感 >= 3
					;PRINTFORMW
				;Ａ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;Ａ感Lv3以上
				IF ABL:Ａ感 >= 3
					;PRINTFORMW
				;Ａ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;ペニバン装着(ターゲット・される側)
;-------------------------------------------------
IF SELECTCOM == 50
	;初めて(主導権を考慮しない)
	IF IS_COM_FIRST(1)
		;恋慕 or 服従
		IF TALENT:恋慕 || TALENT:服従
			;PRINTFORMW
		;それ以外
		ELSE
			;PRINTFORMW
		ENDIF
	;二回目以降
	ELSE
		;恋慕 or 服従
		IF TALENT:恋慕 || TALENT:服従
			;PRINTFORMW
		;それ以外
		ELSE
			;PRINTFORMW
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;酒恥肉倫(ターゲット・される側)
;-------------------------------------------------
IF SELECTCOM == 51
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;順番に挿入(ターゲット・挿れられる側)
;-------------------------------------------------
IF SELECTCOM == 55
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;順番にＡ挿入(ターゲット・挿れられる側)
;-------------------------------------------------
IF SELECTCOM == 56
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;代わる代わる挿入(ターゲット・挿れられる側)
;-------------------------------------------------
IF SELECTCOM == 57
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;代わる代わるＡ挿入(ターゲット・挿れられる側)
;-------------------------------------------------
IF SELECTCOM == 58
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;ローター(ターゲット・される側)
;-------------------------------------------------
IF SELECTCOM == 60
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;Ｃ感Lv3以上
				IF ABL:Ｃ感 >= 3
					;PRINTFORMW
				;Ｃ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;Ｃ感Lv3以上
				IF ABL:Ｃ感 >= 3
					;PRINTFORMW
				;Ｃ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;Ｃ感Lv3以上
				IF ABL:Ｃ感 >= 3
					;PRINTFORMW
				;Ｃ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;Ｃ感Lv3以上
				IF ABL:Ｃ感 >= 3
					;PRINTFORMW
				;Ｃ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;バイブ(ターゲット・挿れられる側)
;-------------------------------------------------
IF SELECTCOM == 61
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;処女
			IF CFLAG:252 == 1
				;恋慕 or 服従
				IF TALENT:恋慕 || TALENT:服従
					;PRINTFORMW
				;それ以外
				ELSE
					;PRINTFORMW
				ENDIF
			;非処女
			ELSE
				;恋慕 or 服従
				IF TALENT:恋慕 || TALENT:服従
					;PRINTFORMW
				;それ以外
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;Ｖ感Lv3以上
				IF ABL:Ｖ感 >= 3
					;PRINTFORMW
				;Ｖ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;Ｖ感Lv3以上
				IF ABL:Ｖ感 >= 3
					;PRINTFORMW
				;Ｖ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;処女
			IF CFLAG:252 == 1
				;恋慕 or 服従
				IF TALENT:恋慕 || TALENT:服従
					;PRINTFORMW
				;それ以外
				ELSE
					;PRINTFORMW
				ENDIF
			;非処女
			ELSE
				;恋慕 or 服従
				IF TALENT:恋慕 || TALENT:服従
					;PRINTFORMW
				;それ以外
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;Ｖ感Lv3以上
				IF ABL:Ｖ感 >= 3
					;PRINTFORMW
				;Ｖ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;Ｖ感Lv3以上
				IF ABL:Ｖ感 >= 3
					;PRINTFORMW
				;Ｖ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;アナルバイブ(ターゲット・挿れられる側)
;-------------------------------------------------
IF SELECTCOM == 62
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;Ａ感Lv3以上
				IF ABL:Ａ感 >= 3
					;PRINTFORMW
				;Ａ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;Ａ感Lv3以上
				IF ABL:Ａ感 >= 3
					;PRINTFORMW
				;Ａ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;Ａ感Lv3以上
				IF ABL:Ａ感 >= 3
					;PRINTFORMW
				;Ａ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;Ａ感Lv3以上
				IF ABL:Ａ感 >= 3
					;PRINTFORMW
				;Ａ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;クリキャップ(ターゲット・される側)
;-------------------------------------------------
IF SELECTCOM == 63
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;Ｃ感Lv3以上
				IF ABL:Ｃ感 >= 3
					;PRINTFORMW
				;Ｃ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;Ｃ感Lv3以上
				IF ABL:Ｃ感 >= 3
					;PRINTFORMW
				;Ｃ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;Ｃ感Lv3以上
				IF ABL:Ｃ感 >= 3
					;PRINTFORMW
				;Ｃ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;Ｃ感Lv3以上
				IF ABL:Ｃ感 >= 3
					;PRINTFORMW
				;Ｃ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;ニプルキャップ(ターゲット・される側)
;-------------------------------------------------
IF SELECTCOM == 64
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;Ｂ感Lv3以上
				IF ABL:Ｂ感 >= 3
					;PRINTFORMW
				;Ｂ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;Ｂ感Lv3以上
				IF ABL:Ｂ感 >= 3
					;PRINTFORMW
				;Ｂ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;Ｂ感Lv3以上
				IF ABL:Ｂ感 >= 3
					;PRINTFORMW
				;Ｂ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;Ｂ感Lv3以上
				IF ABL:Ｂ感 >= 3
					;PRINTFORMW
				;Ｂ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;Ｖ拡張バルーン(ターゲット・される側)
;-------------------------------------------------
IF SELECTCOM == 65
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;処女
			IF CFLAG:252 == 1
				;恋慕 or 服従
				IF TALENT:恋慕 || TALENT:服従
					;PRINTFORMW
				;それ以外
				ELSE
					;PRINTFORMW
				ENDIF
			;非処女
			ELSE
				;恋慕 or 服従
				IF TALENT:恋慕 || TALENT:服従
					;PRINTFORMW
				;それ以外
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;Ｖ感Lv3以上
				IF ABL:Ｖ感 >= 3
					;PRINTFORMW
				;Ｖ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;Ｖ感Lv3以上
				IF ABL:Ｖ感 >= 3
					;PRINTFORMW
				;Ｖ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;処女
			IF CFLAG:252 == 1
				;恋慕 or 服従
				IF TALENT:恋慕 || TALENT:服従
					;PRINTFORMW
				;それ以外
				ELSE
					;PRINTFORMW
				ENDIF
			;非処女
			ELSE
				;恋慕 or 服従
				IF TALENT:恋慕 || TALENT:服従
					;PRINTFORMW
				;それ以外
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;Ｖ感Lv3以上
				IF ABL:Ｖ感 >= 3
					;PRINTFORMW
				;Ｖ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;Ｖ感Lv3以上
				IF ABL:Ｖ感 >= 3
					;PRINTFORMW
				;Ｖ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;Ａ拡張バルーン(ターゲット・される側)
;-------------------------------------------------
IF SELECTCOM == 66
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;Ａ感Lv3以上
				IF ABL:Ａ感 >= 3
					;PRINTFORMW
				;Ａ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;Ａ感Lv3以上
				IF ABL:Ａ感 >= 3
					;PRINTFORMW
				;Ａ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;Ａ感Lv3以上
				IF ABL:Ａ感 >= 3
					;PRINTFORMW
				;Ａ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;Ａ感Lv3以上
				IF ABL:Ａ感 >= 3
					;PRINTFORMW
				;Ａ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;ローション(ターゲット・される側)
;-------------------------------------------------
IF SELECTCOM == 70
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;媚薬(ターゲット・される側)
;-------------------------------------------------
IF SELECTCOM == 71
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				PRINTFORMW
			;それ以外
			ELSE
				PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				PRINTFORMW
			;それ以外
			ELSE
				PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				PRINTFORMW
			;それ以外
			ELSE
				PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				PRINTFORMW
			;それ以外
			ELSE
				PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;排卵誘発剤(ターゲット・される側)
;-------------------------------------------------
IF SELECTCOM == 72
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;絶倫丸(ターゲット・される側)
;-------------------------------------------------
IF SELECTCOM == 73
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;麻薬(ターゲット・される側)
;-------------------------------------------------
IF SELECTCOM == 74
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;コンドーム(ターゲット・される側)
;-------------------------------------------------
IF SELECTCOM == 75
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;桃源香(ターゲット・される側)
;-------------------------------------------------
IF SELECTCOM == 76
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;脅す(ターゲット・される側)
;-------------------------------------------------
IF SELECTCOM == 80
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				IF GETBIT(TALENT:淫乱系, 素質_淫乱_マゾ)
					;PRINTFORMW
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				IF GETBIT(TALENT:淫乱系, 素質_淫乱_マゾ)
					;PRINTFORMW
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				IF GETBIT(TALENT:淫乱系, 素質_淫乱_マゾ)
					;PRINTFORMW
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				IF GETBIT(TALENT:淫乱系, 素質_淫乱_マゾ)
					;PRINTFORMW
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;スパンキング(ターゲット・される側)
;-------------------------------------------------
IF SELECTCOM == 81
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				IF GETBIT(TALENT:淫乱系, 素質_淫乱_マゾ)
					;PRINTFORMW
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				IF GETBIT(TALENT:淫乱系, 素質_淫乱_マゾ)
					;PRINTFORMW
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				IF GETBIT(TALENT:淫乱系, 素質_淫乱_マゾ)
					;PRINTFORMW
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				IF GETBIT(TALENT:淫乱系, 素質_淫乱_マゾ)
					;PRINTFORMW
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;鞭(ターゲット・される側)
;-------------------------------------------------
IF SELECTCOM == 82
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				IF GETBIT(TALENT:淫乱系, 素質_淫乱_マゾ)
					;PRINTFORMW
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				IF GETBIT(TALENT:淫乱系, 素質_淫乱_マゾ)
					;PRINTFORMW
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				IF GETBIT(TALENT:淫乱系, 素質_淫乱_マゾ)
					;PRINTFORMW
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				IF GETBIT(TALENT:淫乱系, 素質_淫乱_マゾ)
					;PRINTFORMW
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;針(ターゲット・される側)
;-------------------------------------------------
IF SELECTCOM == 83
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				IF GETBIT(TALENT:淫乱系, 素質_淫乱_マゾ)
					;PRINTFORMW
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				IF GETBIT(TALENT:淫乱系, 素質_淫乱_マゾ)
					;PRINTFORMW
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				IF GETBIT(TALENT:淫乱系, 素質_淫乱_マゾ)
					;PRINTFORMW
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				IF GETBIT(TALENT:淫乱系, 素質_淫乱_マゾ)
					;PRINTFORMW
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;目隠し(ターゲット・される側)
;-------------------------------------------------
IF SELECTCOM == 84
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				IF GETBIT(TALENT:淫乱系, 素質_淫乱_マゾ)
					;PRINTFORMW
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				IF GETBIT(TALENT:淫乱系, 素質_淫乱_マゾ)
					;PRINTFORMW
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				IF GETBIT(TALENT:淫乱系, 素質_淫乱_マゾ)
					;PRINTFORMW
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				IF GETBIT(TALENT:淫乱系, 素質_淫乱_マゾ)
					;PRINTFORMW
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;縄(ターゲット・される側)
;-------------------------------------------------
IF SELECTCOM == 85
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				IF GETBIT(TALENT:淫乱系, 素質_淫乱_マゾ)
					;PRINTFORMW
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				IF GETBIT(TALENT:淫乱系, 素質_淫乱_マゾ)
					;PRINTFORMW
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				IF GETBIT(TALENT:淫乱系, 素質_淫乱_マゾ)
					;PRINTFORMW
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				IF GETBIT(TALENT:淫乱系, 素質_淫乱_マゾ)
					;PRINTFORMW
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;口枷(ターゲット・される側)
;-------------------------------------------------
IF SELECTCOM == 86
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				IF GETBIT(TALENT:淫乱系, 素質_淫乱_マゾ)
					;PRINTFORMW
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				IF GETBIT(TALENT:淫乱系, 素質_淫乱_マゾ)
					;PRINTFORMW
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				IF GETBIT(TALENT:淫乱系, 素質_淫乱_マゾ)
					;PRINTFORMW
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				IF GETBIT(TALENT:淫乱系, 素質_淫乱_マゾ)
					;PRINTFORMW
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;フィストファック(ターゲット・される側)
;-------------------------------------------------
IF SELECTCOM == 87
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;処女
			IF CFLAG:252 == 1
				;恋慕 or 服従
				IF TALENT:恋慕 || TALENT:服従
					;PRINTFORMW
				;それ以外
				ELSE
					;PRINTFORMW
				ENDIF
			;非処女
			ELSE
				;恋慕 or 服従
				IF TALENT:恋慕 || TALENT:服従
					;PRINTFORMW
				;それ以外
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;Ｖ感Lv5以上
				IF ABL:Ｖ感 >= 5
					;PRINTFORMW
				;Ｖ感Lv5未満
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;Ｖ感Lv5以上
				IF ABL:Ｖ感 >= 5
					;PRINTFORMW
				;Ｖ感Lv5未満
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;処女
			IF CFLAG:252 == 1
				;恋慕 or 服従
				IF TALENT:恋慕 || TALENT:服従
					;PRINTFORMW
				;それ以外
				ELSE
					;PRINTFORMW
				ENDIF
			;非処女
			ELSE
				;恋慕 or 服従
				IF TALENT:恋慕 || TALENT:服従
					;PRINTFORMW
				;それ以外
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;Ｖ感Lv5以上
				IF ABL:Ｖ感 >= 5
					;PRINTFORMW
				;Ｖ感Lv5未満
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;Ｖ感Lv5以上
				IF ABL:Ｖ感 >= 5
					;PRINTFORMW
				;Ｖ感Lv5未満
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;アナルフィスト(ターゲット・される側)
;-------------------------------------------------
IF SELECTCOM == 88
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;Ａ感Lv5以上
				IF ABL:Ａ感 >= 5
					;PRINTFORMW
				;Ａ感Lv5未満
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;Ａ感Lv5以上
				IF ABL:Ａ感 >= 5
					;PRINTFORMW
				;Ａ感Lv5未満
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;Ａ感Lv5以上
				IF ABL:Ａ感 >= 5
					;PRINTFORMW
				;Ａ感Lv5未満
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;Ａ感Lv5以上
				IF ABL:Ａ感 >= 5
					;PRINTFORMW
				;Ａ感Lv5未満
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;平手打ち(ターゲット・される側)
;-------------------------------------------------
IF SELECTCOM == 89
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				IF GETBIT(TALENT:淫乱系, 素質_淫乱_マゾ)
					;PRINTFORMW
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				IF GETBIT(TALENT:淫乱系, 素質_淫乱_マゾ)
					;PRINTFORMW
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				IF GETBIT(TALENT:淫乱系, 素質_淫乱_マゾ)
					;PRINTFORMW
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				IF GETBIT(TALENT:淫乱系, 素質_淫乱_マゾ)
					;PRINTFORMW
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;イラマチオ(ターゲット・しゃぶられる側。要ペニス)
;-------------------------------------------------
IF SELECTCOM == 90
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;ピアシング(ターゲット・される側)
;-------------------------------------------------
IF SELECTCOM == 91
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				IF GETBIT(TALENT:淫乱系, 素質_淫乱_マゾ)
					;PRINTFORMW
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				IF GETBIT(TALENT:淫乱系, 素質_淫乱_マゾ)
					;PRINTFORMW
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				IF GETBIT(TALENT:淫乱系, 素質_淫乱_マゾ)
					;PRINTFORMW
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				IF GETBIT(TALENT:淫乱系, 素質_淫乱_マゾ)
					;PRINTFORMW
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;タトゥー(ターゲット・される側)
;-------------------------------------------------
IF SELECTCOM == 92
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				IF GETBIT(TALENT:淫乱系, 素質_淫乱_マゾ)
					;PRINTFORMW
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				IF GETBIT(TALENT:淫乱系, 素質_淫乱_マゾ)
					;PRINTFORMW
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				IF GETBIT(TALENT:淫乱系, 素質_淫乱_マゾ)
					;PRINTFORMW
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				IF GETBIT(TALENT:淫乱系, 素質_淫乱_マゾ)
					;PRINTFORMW
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;鼻フック(ターゲット・される側)
;-------------------------------------------------
IF SELECTCOM == 93
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				IF GETBIT(TALENT:淫乱系, 素質_淫乱_マゾ)
					;PRINTFORMW
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				IF GETBIT(TALENT:淫乱系, 素質_淫乱_マゾ)
					;PRINTFORMW
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				IF GETBIT(TALENT:淫乱系, 素質_淫乱_マゾ)
					;PRINTFORMW
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				IF GETBIT(TALENT:淫乱系, 素質_淫乱_マゾ)
					;PRINTFORMW
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;首絞め(ターゲット・される側)
;-------------------------------------------------
IF SELECTCOM == 94
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				IF GETBIT(TALENT:淫乱系, 素質_淫乱_マゾ)
					;PRINTFORMW
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				IF GETBIT(TALENT:淫乱系, 素質_淫乱_マゾ)
					;PRINTFORMW
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				IF GETBIT(TALENT:淫乱系, 素質_淫乱_マゾ)
					;PRINTFORMW
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				IF GETBIT(TALENT:淫乱系, 素質_淫乱_マゾ)
					;PRINTFORMW
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;腹パン(ターゲット・される側)
;-------------------------------------------------
IF SELECTCOM == 97
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				IF GETBIT(TALENT:淫乱系, 素質_淫乱_マゾ)
					;PRINTFORMW
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				IF GETBIT(TALENT:淫乱系, 素質_淫乱_マゾ)
					;PRINTFORMW
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				IF GETBIT(TALENT:淫乱系, 素質_淫乱_マゾ)
					;PRINTFORMW
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				IF GETBIT(TALENT:淫乱系, 素質_淫乱_マゾ)
					;PRINTFORMW
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;遠隔調教(ターゲット・される側)
;-------------------------------------------------
IF SELECTCOM == 98
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				IF GETBIT(TALENT:淫乱系, 素質_淫乱_淫乱)
					;PRINTFORMW
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				IF GETBIT(TALENT:淫乱系, 素質_淫乱_淫乱)
					;PRINTFORMW
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				IF GETBIT(TALENT:淫乱系, 素質_淫乱_淫乱)
					;PRINTFORMW
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				IF GETBIT(TALENT:淫乱系, 素質_淫乱_淫乱)
					;PRINTFORMW
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;自慰(ターゲット・される側)
;-------------------------------------------------
IF SELECTCOM == 100
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;顔面騎乗(ターゲット・される側)
;-------------------------------------------------
IF SELECTCOM == 101
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;プレイヤーが膣がない(玉舐め)
				IF !HAS_VAGINA(MPLY:0)
					;PRINTFORMW
				;プレイヤーに膣がある(秘部舐め)
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;プレイヤーが膣がない(玉舐め)
				IF !HAS_VAGINA(MPLY:0)
					;PRINTFORMW
				;プレイヤーに膣がある(秘部舐め)
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;プレイヤーが膣がない(玉舐め)
				IF !HAS_VAGINA(MPLY:0)
					;PRINTFORMW
				;プレイヤーに膣がある(秘部舐め)
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;プレイヤーが膣がない(玉舐め)
				IF !HAS_VAGINA(MPLY:0)
					;PRINTFORMW
				;プレイヤーに膣がある(秘部舐め)
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;プレイヤーが膣がない(玉舐め)
				IF !HAS_VAGINA(MPLY:0)
					;PRINTFORMW
				;プレイヤーに膣がある(秘部舐め)
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;プレイヤーが膣がない(玉舐め)
				IF !HAS_VAGINA(MPLY:0)
					;PRINTFORMW
				;プレイヤーに膣がある(秘部舐め)
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;プレイヤーが膣がない(玉舐め)
				IF !HAS_VAGINA(MPLY:0)
					;PRINTFORMW
				;プレイヤーに膣がある(秘部舐め)
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;プレイヤーが膣がない(玉舐め)
				IF !HAS_VAGINA(MPLY:0)
					;PRINTFORMW
				;プレイヤーに膣がある(秘部舐め)
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;Ａ顔面騎乗(ターゲット・される側)
;-------------------------------------------------
IF SELECTCOM == 102
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;電気按摩(ターゲット・される側)
;-------------------------------------------------
IF SELECTCOM == 103
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;足を舐める(ターゲット・される側)
;-------------------------------------------------
IF SELECTCOM == 104
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;Ｗ顔面騎乗(ターゲット・される側)
;-------------------------------------------------
IF SELECTCOM == 105
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;亀頭責め手コキ(ターゲット・される側。要ペニス)
;-------------------------------------------------
IF SELECTCOM == 106
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;亀頭責めフェラ(ターゲット・しゃぶられる側。要ペニス)
;-------------------------------------------------
IF SELECTCOM == 107
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;尻尾セルフ挿入(ターゲット・される側)
;-------------------------------------------------
IF SELECTCOM == 108
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;尻尾セルフＡ挿入(ターゲット・される側)
;-------------------------------------------------
IF SELECTCOM == 109
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;土下座(ターゲット・される側)
;-------------------------------------------------
IF SELECTCOM == 110
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;おねだり(ターゲット・される側)
;-------------------------------------------------
IF SELECTCOM == 111
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;露出プレイ(ターゲット・される側)
;-------------------------------------------------
IF SELECTCOM == 112
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;陰毛管理(ターゲット・される側)
;-------------------------------------------------
IF SELECTCOM == 113
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;身体に落書き(ターゲット・される側)
;-------------------------------------------------
IF SELECTCOM == 114
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;放尿する(ターゲット・される側)
;-------------------------------------------------
IF SELECTCOM == 130
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;尿を掛ける(ターゲット・される側)
;-------------------------------------------------
IF SELECTCOM == 131
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;口内放尿(ターゲット・される側)
;-------------------------------------------------
IF SELECTCOM == 132
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;利尿剤(ターゲット・される側)
;-------------------------------------------------
IF SELECTCOM == 140
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;浣腸(ターゲット・される側)
;-------------------------------------------------
IF SELECTCOM == 141
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;排泄Lv2以上
				IF ABL:排泄 >= 2
					;PRINTFORMW
				;排泄Lv2未満
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;排泄Lv2以上
				IF ABL:排泄 >= 2
					;PRINTFORMW
				;排泄Lv2未満
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;排泄Lv2以上
				IF ABL:排泄 >= 2
					;PRINTFORMW
				;排泄Lv2未満
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;排泄Lv2以上
				IF ABL:排泄 >= 2
					;PRINTFORMW
				;排泄Lv2未満
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;浣腸＆プラグ(ターゲット・される側)
;-------------------------------------------------
IF SELECTCOM == 142
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;排泄Lv2以上
				IF ABL:排泄 >= 2
					;PRINTFORMW
				;排泄Lv2未満
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;排泄Lv2以上
				IF ABL:排泄 >= 2
					;PRINTFORMW
				;排泄Lv2未満
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;排泄Lv2以上
				IF ABL:排泄 >= 2
					;PRINTFORMW
				;排泄Lv2未満
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;排泄Lv2以上
				IF ABL:排泄 >= 2
					;PRINTFORMW
				;排泄Lv2未満
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;放屁する(ターゲット・される側)
;-------------------------------------------------
IF SELECTCOM == 150
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;顔騎放屁(ターゲット・される側)
;-------------------------------------------------
IF SELECTCOM == 151
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;触手召喚(ターゲット・される側)
;-------------------------------------------------
IF SELECTCOM == 200
	;初めて(主導権を考慮しない)
	IF IS_COM_FIRST(1)
		;恋慕 or 服従
		IF TALENT:恋慕 || TALENT:服従
			;PRINTFORMW
		;それ以外
		ELSE
			;PRINTFORMW
		ENDIF
	;二回目以降
	ELSE
		;恋慕 or 服従
		IF TALENT:恋慕 || TALENT:服従
			;触手Lv3以上
			IF ABL:触手 >= 3
				;PRINTFORMW
			ELSE
				;PRINTFORMW
			ENDIF
		;それ以外
		ELSE
			;触手Lv3以上
			IF ABL:触手 >= 3
				;PRINTFORMW
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;触手挿入(ターゲット・挿れられる側)
;-------------------------------------------------
IF SELECTCOM == 201
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;処女
			IF CFLAG:252 == 1
				;恋慕 or 服従
				IF TALENT:恋慕 || TALENT:服従
					;PRINTFORMW
				;それ以外
				ELSE
					;PRINTFORMW
				ENDIF
			;非処女
			ELSE
				;恋慕 or 服従
				IF TALENT:恋慕 || TALENT:服従
					;PRINTFORMW
				;それ以外
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;触手Lv3以上
				IF ABL:触手 >= 3
					;PRINTFORMW
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;触手Lv3以上
				IF ABL:触手 >= 3
					;PRINTFORMW
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;処女
			IF CFLAG:252 == 1
				;恋慕 or 服従
				IF TALENT:恋慕 || TALENT:服従
					;PRINTFORMW
				;それ以外
				ELSE
					;PRINTFORMW
				ENDIF
			;非処女
			ELSE
				;恋慕 or 服従
				IF TALENT:恋慕 || TALENT:服従
					;PRINTFORMW
				;それ以外
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;触手Lv3以上
				IF ABL:触手 >= 3
					;PRINTFORMW
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;触手Lv3以上
				IF ABL:触手 >= 3
					;PRINTFORMW
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;触手Ａ挿入(ターゲット・挿れられる側)
;-------------------------------------------------
IF SELECTCOM == 202
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;触手Lv3以上
				IF ABL:触手 >= 3
					;PRINTFORMW
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;触手Lv3以上
				IF ABL:触手 >= 3
					;PRINTFORMW
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;触手Lv3以上
				IF ABL:触手 >= 3
					;PRINTFORMW
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;触手Lv3以上
				IF ABL:触手 >= 3
					;PRINTFORMW
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;触手クリ責め(ターゲット・される側)
;-------------------------------------------------
IF SELECTCOM == 203
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;触手Lv3以上
				IF ABL:触手 >= 3
					;PRINTFORMW
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;触手Lv3以上
				IF ABL:触手 >= 3
					;PRINTFORMW
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;触手Lv3以上
				IF ABL:触手 >= 3
					;PRINTFORMW
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;触手Lv3以上
				IF ABL:触手 >= 3
					;PRINTFORMW
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;触手胸愛撫(ターゲット・される側)
;-------------------------------------------------
IF SELECTCOM == 204
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;触手Lv3以上
				IF ABL:触手 >= 3
					;PRINTFORMW
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;触手Lv3以上
				IF ABL:触手 >= 3
					;PRINTFORMW
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;触手Lv3以上
				IF ABL:触手 >= 3
					;PRINTFORMW
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;触手Lv3以上
				IF ABL:触手 >= 3
					;PRINTFORMW
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;触手オナホ(ターゲット・される側。要ペニス)
;-------------------------------------------------
IF SELECTCOM == 205
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;触手Lv3以上
				IF ABL:触手 >= 3
					;PRINTFORMW
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;触手Lv3以上
				IF ABL:触手 >= 3
					;PRINTFORMW
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;触手Lv3以上
				IF ABL:触手 >= 3
					;PRINTFORMW
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;触手Lv3以上
				IF ABL:触手 >= 3
					;PRINTFORMW
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;触手コキ(ターゲット・される側。要ペニス)
;-------------------------------------------------
IF SELECTCOM == 206
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;触手Lv3以上
				IF ABL:触手 >= 3
					;PRINTFORMW
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;触手Lv3以上
				IF ABL:触手 >= 3
					;PRINTFORMW
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;触手Lv3以上
				IF ABL:触手 >= 3
					;PRINTFORMW
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;触手Lv3以上
				IF ABL:触手 >= 3
					;PRINTFORMW
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;触手搾乳(ターゲット・される側)
;-------------------------------------------------
IF SELECTCOM == 207
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;触手Lv3以上
				IF ABL:触手 >= 3
					;PRINTFORMW
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;触手Lv3以上
				IF ABL:触手 >= 3
					;PRINTFORMW
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;触手Lv3以上
				IF ABL:触手 >= 3
					;PRINTFORMW
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;触手Lv3以上
				IF ABL:触手 >= 3
					;PRINTFORMW
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;触手緊縛(ターゲット・される側)
;-------------------------------------------------
IF SELECTCOM == 208
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				IF GETBIT(TALENT:淫乱系, 素質_淫乱_マゾ)
					;PRINTFORMW
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				IF GETBIT(TALENT:淫乱系, 素質_淫乱_マゾ)
					;PRINTFORMW
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				IF GETBIT(TALENT:淫乱系, 素質_淫乱_マゾ)
					;PRINTFORMW
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				IF GETBIT(TALENT:淫乱系, 素質_淫乱_マゾ)
					;PRINTFORMW
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;触手口辱(ターゲット・される側)
;-------------------------------------------------
IF SELECTCOM == 210
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				IF GETBIT(TALENT:淫乱系, 素質_淫乱_マゾ)
					;PRINTFORMW
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				IF GETBIT(TALENT:淫乱系, 素質_淫乱_マゾ)
					;PRINTFORMW
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				IF GETBIT(TALENT:淫乱系, 素質_淫乱_マゾ)
					;PRINTFORMW
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				IF GETBIT(TALENT:淫乱系, 素質_淫乱_マゾ)
					;PRINTFORMW
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;触手オナニー(ターゲット・される側)
;-------------------------------------------------
IF SELECTCOM == 211
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;触手Lv3以上
				IF ABL:触手 >= 3
					;PRINTFORMW
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;触手Lv3以上
				IF ABL:触手 >= 3
					;PRINTFORMW
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;触手Lv3以上
				IF ABL:触手 >= 3
					;PRINTFORMW
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;触手Lv3以上
				IF ABL:触手 >= 3
					;PRINTFORMW
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;触手産卵(ターゲット・される側)
;-------------------------------------------------
IF SELECTCOM == 212
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;妊娠中
				IF TALENT:妊娠
					;PRINTFORMW
				;触手Lv3以上
				ELSEIF ABL:触手 >= 3
					;PRINTFORMW
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;妊娠中
				IF TALENT:妊娠
					;PRINTFORMW
				;触手Lv3以上
				ELSEIF ABL:触手 >= 3
					;PRINTFORMW
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;妊娠中
				IF TALENT:妊娠
					;PRINTFORMW
				;触手Lv3以上
				ELSEIF ABL:触手 >= 3
					;PRINTFORMW
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;妊娠中
				IF TALENT:妊娠
					;PRINTFORMW
				;触手Lv3以上
				ELSEIF ABL:触手 >= 3
					;PRINTFORMW
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;触手Ａ産卵(ターゲット・される側)
;-------------------------------------------------
IF SELECTCOM == 213
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;触手Lv3以上
				IF ABL:触手 >= 3
					;PRINTFORMW
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;触手Lv3以上
				IF ABL:触手 >= 3
					;PRINTFORMW
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;触手Lv3以上
				IF ABL:触手 >= 3
					;PRINTFORMW
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;触手Lv3以上
				IF ABL:触手 >= 3
					;PRINTFORMW
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;おまかせする
;-------------------------------------------------
IF SELECTCOM == 291
	;初めて(主導権を考慮しない)
	IF IS_COM_FIRST(1)
		;恋慕 or 服従
		IF TALENT:恋慕 || TALENT:服従
			;PRINTFORMW
		;それ以外
		ELSE
			;PRINTFORMW
		ENDIF
	;二回目以降
	ELSE
		;恋慕 or 服従
		IF TALENT:恋慕 || TALENT:服従
			;PRINTFORMW
		;それ以外
		ELSE
			;PRINTFORMW
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;なにもしない
;-------------------------------------------------
IF SELECTCOM == 292
	;初めて(主導権を考慮しない)
	IF IS_COM_FIRST(1)
		;恋慕 or 服従
		IF TALENT:恋慕 || TALENT:服従
			;PRINTFORMW
		;それ以外
		ELSE
			;PRINTFORMW
		ENDIF
	;二回目以降
	ELSE
		;恋慕 or 服従
		IF TALENT:恋慕 || TALENT:服従
			;PRINTFORMW
		;それ以外
		ELSE
			;PRINTFORMW
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;忠誠を誓わせる(ターゲット・される側)
;-------------------------------------------------
IF SELECTCOM == 299
	;初めて(主人公がプレイヤーで主導権があるとき限定)
	IF IS_COM_FIRST(1)
		;恋慕 or 服従
		IF TALENT:恋慕 || TALENT:服従
			;PRINTFORMW
		;それ以外
		ELSE
			;PRINTFORMW
		ENDIF
	;二回目以降
	ELSE
		;恋慕 or 服従
		IF TALENT:恋慕 || TALENT:服従
			;PRINTFORMW
		;それ以外
		ELSE
			;PRINTFORMW
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;会話
;-------------------------------------------------
IF SELECTCOM == 300
	;初めて(主導権を考慮しない)
	IF IS_COM_FIRST(1)
		PRINTFORMW "嗯……该从哪里开始说呢？"
		;TranslatedMark 「ええと……何から話す？」
	;二回目以降
	;愚痴聞きの場合
	ELSEIF TFLAG:17 == 1
		IF (TALENT:恋慕 || TALENT:服従 || TALENT:親友 || TALENT:恋人) && RAND:2
			PRINTFORMW "正好，有想让%ANAME(MASTER)%听的事"
			;TranslatedMark 「丁度良かった、%ANAME(MASTER)%にこそ聞いて欲しいことがあるの」
			PRINTFORML ……
			PRINTFORML ～（・　ω・）～
			;TranslatedMark ～（・　ω・）～
			PRINTFORMW ……
			PRINTFORML "——，能爽快地说出来，心情也平静下来了呢"
			;TranslatedMark 「――と、洗いざらい話せてだいぶ落ち着いたわ」
			PRINTFORMW "谢谢你愿意听我说完"
			;TranslatedMark 「全部聞いてくれてありがとう♪」
		ELSE
			SELECTCASE RAND:8
			CASE 1
				;MAPによって分岐する発言……の筈。
				IF GET_CITYNUMBER("ハノイ") == -1 && GET_CITYNUMBER("サイゴン") == -1
					PRINTFORMW "最近，有个妖怪说“在越南见过”"
					;TranslatedMark 「最近、ある妖怪から『ベトナムで見たことある』って言われたの」
					PRINTFORMW "我应该没去过那样的地方，真是不可思议啊"
					;TranslatedMark 「私ってそんな所に行ったこと無い筈なのに不思議ねぇ」
					;※クッキー☆界隈以外で成美ちゃんをベトコン扱いするのは止めましょう
				ELSE
					PRINTFORMW "……虽然这么说"
					;TranslatedMark 「……ということなんだけど」
				ENDIF
			CASE 2
				PRINTFORML "我和真正的地藏菩萨一样
				;TranslatedMark 「私は本物の地蔵菩薩と同じく、
				PRINTFORMW 　都是在佛教衰微的时代,自我期许拯救众生的角色。"
				;TranslatedMark 『無仏の時代に衆生を救済する』という役割を自らに課していてね？」
				PRINTFORML "例如，在孩子遇到困难的时候，会主动伸出手，积极地帮助
				;TranslatedMark 「例えば子供が困ってたら積極的に助けることが多いけど、
				PRINTFORMW 　在各种妖怪中,天狗经常对会人类小孩捉弄作怪…"
				;TranslatedMark 妖怪の中でも天狗は人間の子供を狙って悪戯することが多くて……」
				PRINTFORMW "所以，天狗和其他妖怪相比，会更加难相处一点"
				;TranslatedMark 「だから、天狗には他の妖怪と比べて苦手意識が余計にあるのよ」
			CASEELSE
				PRINTFORMW "你能帮我问一下吗？其实呢，之前……"
				;TranslatedMark 「ちょっと聞いてくれる？　実はね、この前……」
			ENDSELECT
		ENDIF
	;悩み聞きの場合
	ELSEIF TFLAG:17 == 2
		IF (TALENT:恋慕 || TALENT:服従 || TALENT:親友 || TALENT:恋人) && RAND:2
			PRINTFORMW "正好，有想让%ANAME(MASTER)%听的事"
			;TranslatedMark 「丁度良かった、%ANAME(MASTER)%にこそ聞いて欲しいことがあるの」
			PRINTFORML ……
			PRINTFORML ～（・ω　・）～
			;TranslatedMark ～（・ω　・）～
			PRINTFORMW ……
			PRINTFORML "——说了很多话，心情也变得轻松了一点"
			;TranslatedMark 「――と、色々話せて気持ちが少し楽になったわ」
			PRINTFORMW "谢谢你能听我说完"
			;TranslatedMark 「全部聞いてくれてありがとう♪」
		ELSE
			SELECTCASE RAND:8
			CASE 1
				PRINTFORML "我原本是那边的地藏菩萨的石像
				;TranslatedMark 「私って元々は石像の方のお地蔵さんだから、
				PRINTFORMW 　和所谓的真正的地藏菩萨完全不同呢"
				;TranslatedMark いわゆる本物の地蔵菩薩とは全然違うのよね」
				PRINTFORMW "即便如此，似乎也和付丧神不同……"
				;TranslatedMark 「だからといって付喪神とも違う存在みたいだし……」
				PRINTFORMW "最后，我到底是什么人呢？这样想着，就有点寂寞了"
				;TranslatedMark 「結局、私は何者なの？　と考えてたら少し寂しくなったわ」
			CASE 2
				PRINTFORMW "现在正好很寂寞"
				;TranslatedMark 「今、丁度寂しかったところだったのよ」
				PRINTFORML "我一个人独处的时间居多
				;TranslatedMark 「私って一人でいることが多いけど、
				PRINTFORMW 　一个人的话果然很无聊……"
				;TranslatedMark いーっつも一人だと流石につまらないというか……」
			CASEELSE
				PRINTFORMW "你能帮我问一下吗？其实最近……"
				;TranslatedMark 「ちょっと聞いてくれる？　実は最近……」
			ENDSELECT
		ENDIF
	ELSE
		;恋慕 or 服従 or 親友
		IF (TALENT:恋慕 || TALENT:服従 || TALENT:親友) && RAND:2
			IF RAND:3 == 0
				PRINTFORML "——啊，我也知道那个！"
				;TranslatedMark 「――あ、それって私も知ってるわ！」
				PRINTFORMW "顺便说一下，这个故事是对其他孩子保密的，其实呢……"
				;TranslatedMark 「ついでに、この話って他の子へは内緒なんだけど、実はね……」
			ELSEIF RAND:2 == 0
				PRINTFORMW "啊，幻想乡也有那么厉害的家伙啊"
				;TranslatedMark 「へえ、幻想郷にはそんな凄い奴もいるのねぇ」
			ELSE
				PRINTFORML "我，虽说原本是地藏，但是因为是石像的缘故，所以
				;TranslatedMark 「私、元々が地蔵だとは言え石像の方だから、
				PRINTFORML 　其实佛法并不像真的那么拿手"
				;TranslatedMark 実のところ仏法については本物ほど得意なわけではないのよ」
				PRINTFORMW "相反，倒是擅长魔法，这也让人觉得很讽刺吧"
				;TranslatedMark 「その代わりに魔法が得意というのも何だか皮肉を感じるわー」
			ENDIF
		ELSE
			SELECTCASE RAND:8
			CASE 1
				PRINTFORMW "这样啊，你也很辛苦啊"
				;TranslatedMark 「そっかー、貴方も結構大変なのね」
			CASE 2
				PRINTFORML "这是从其他孩子那里听说的"
				;TranslatedMark 「コレは他の子から聞いた話なんだけどね」
				IF RAND:3 == 0
					PRINTFORML "付丧神在我们的势力中有几名？
					;TranslatedMark 「付喪神ってうちの勢力に何名居たかしら？
					PRINTFORML 　我听说严格来说是不一样的……"
					;TranslatedMark 私の場合は厳密に言えば違うと聞いたけど……」
					PRINTFORML "现在的博丽巫女使用的“针”
					;TranslatedMark 「今の博麗の巫女が使っている"針"も、
					PRINTFORMW 　听说它也曾一度“付丧神”化，闹出了很大的卵子"
					;TranslatedMark 一度だけ付喪神化しかけて大変なことになったらしいわ」
				ELSEIF RAND:2 == 0
					PRINTFORML "在月之都，与死亡相关的力量和生命力都被视为“污秽”
					;TranslatedMark 「月の都では死に関する力や生命力が"穢れ"として扱われていて、
					PRINTFORML 　为了去除那个“污秽”好像有很多各种各样的技术和兵器"
					;TranslatedMark その"穢れ"を取り除く為に色々な技術や兵器が沢山有るらしいの」
					PRINTFORMW "因为我最擅长生命操作，所以在各种意义上都不投缘呢"
					;TranslatedMark 「私は生命操作が一番得意だから、色々な意味で相性悪いわねぇ」
				ELSE
					PRINTFORML "外面世界的某个地方有一个叫做“地藏流”的活动
					;TranslatedMark 「外の世界のどこかには『地蔵流し』という行事が有るんだけど、
					PRINTFORML 　我听说那不是流放石像地藏的仪式,而是流放绘有地藏图像的佛教符图的仪式。"
					;TranslatedMark 石像の地蔵じゃなくて、地蔵が描かれたお札を流す行事らしいのよ」
					PRINTFORMW "我想这意味着如果不是用专门的船只,是无法将石像流放掉的吧。"
					;TranslatedMark 「石像を流すのは本格的な船じゃないと出来ないから、ってことかしら」
				ENDIF
			CASEELSE
				IF RAND:3 == 0
					PRINTFORMW "是啊，我也有那样的事"
					;TranslatedMark 「そうなのよねー、私にもそういうことあるかも」
				ELSEIF RAND:2 == 0
					PRINTFORML "现在的形势？还不错呢"
					;TranslatedMark 「今の情勢の話？　良いわよー」
					PRINTFORMW "对谁来说都很重要"
					;TranslatedMark 「誰にとっても重要な話なんだもの」
				ELSE
					PRINTFORMW "……接下来要说什么呢"
					;TranslatedMark 「……次は何について話そうかしら」
				ENDIF
			ENDSELECT
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;武術訓練
;-------------------------------------------------
IF SELECTCOM == 301
	;初めて(主導権を考慮しない)
	IF IS_COM_FIRST(1)
		;PRINTFORMW
	;二回目以降
	ELSE
		PRINTFORML %ANAME(MASTER)%和%ANAME(TARGET)%通过弹幕游戏进行武术训练…。
		;TranslatedMark %ANAME(MASTER)%と%ANAME(TARGET)%は、弾幕ごっこを通して武術訓練をしている…。
		;恋慕 or 服従 or 親友
		IF (TALENT:恋慕 || TALENT:服従 || TALENT:親友) && RAND:2
			SELECTCASE RAND:3
			CASE 1
				PRINTFORML 魔符「作宠物的巨大弹生命体」
				;TranslatedMark 『魔符「ペットの巨大弾生命体」』
				IF RAND:3 == 0
					PRINTFORMW "呵呵，意外的很可爱吧？"
					;TranslatedMark 「ふふふ、意外と可愛いでしょ？」
				ELSEIF RAND:2 == 0
					PRINTFORMW ～（・ω　・）～
					;TranslatedMark ～（・ω　・）～
				ELSE
					PRINTFORMW ～（・ω　・）～
					;TranslatedMark ～（・　ω・）～
				ENDIF
			CASE 2
				PRINTFORML 地藏「业火救济」
				;TranslatedMark 『地蔵「業火救済」』
				IF RAND:2 == 0
					PRINTFORMW "火焰啊飞舞吧！"
					;TranslatedMark 「炎よ舞い上がれ！」
				ELSE
					PRINTFORMW "无论是过去还是未来，我都会努力向前！"
					;TranslatedMark 「前からも後ろからもドンドン行くわ！」
				ENDIF
			CASEELSE
				PRINTFORML 魔符「即席菩提」
				;TranslatedMark 『魔符「即席菩提」』
				PRINTFORMW "即使密度变浓，基本原理也是一样的"
				;TranslatedMark 「密度が濃くなっても基本は同じよー」
			ENDSELECT
		ELSE
			SELECTCASE RAND:4
			CASE 1
				PRINTFORML 魔符「Bullet Golem」（弹丸魔像）
				;TranslatedMark 『魔符「バレットゴーレム」』
				IF RAND:3 == 0
					PRINTFORMW "得到了被%SPLIT_RAND("マスタースパーク/夢想封印", 1)%也不会消失的认证"
					;TranslatedMark 「%SPLIT_RAND("マスタースパーク/夢想封印", 1)%でも消滅しないというお墨付きを頂いているわ」
				ELSEIF RAND:2 == 0
					PRINTFORMW ～（・　ω・）～
					;TranslatedMark ～（・ω　・）～
				ELSE
					PRINTFORMW ～（・　ω・）～
					;TranslatedMark ～（・　ω・）～
				ENDIF
			CASE 2
				PRINTFORML 地藏「Criminal Salvation」（罪业救赎）
				;TranslatedMark 『地蔵「クリミナルサルヴェイション」』
				IF RAND:2 == 0
					PRINTFORMW "火焰啊飞舞吧！"
					;TranslatedMark 「炎よ舞い上がれ！」
				ELSE
					PRINTFORMW "无论是过去还是未来，我都会努力向前！"
					;TranslatedMark 「前からも後ろからもドンドン行くわ！」
				ENDIF
			CASEELSE
				PRINTFORML 魔符「Instant Bodhi」（顷刻菩提）
				;TranslatedMark 『魔符「インスタントボーディ」』
				PRINTFORMW "我觉得这样的话就可以从容地避开"
				;TranslatedMark 「この程度なら余裕で避けられると思うわ」
			ENDSELECT
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;護身術
;-------------------------------------------------
IF SELECTCOM == 302
	;初めて(主導権を考慮しない)
	IF IS_COM_FIRST(1)
		;PRINTFORMW
	;二回目以降
	ELSE
		;恋慕 or 服従 or 親友
		IF TALENT:恋慕 || TALENT:服従 || TALENT:親友
			SELECTCASE RAND:3
			CASE 1
				;PRINTFORMW 「」
			CASE 2
				;PRINTFORMW 「」
			CASEELSE
				;PRINTFORMW 「」
			ENDSELECT
		ELSE
			SELECTCASE RAND:3
			CASE 1
				;PRINTFORMW 「」
			CASE 2
				;PRINTFORMW 「」
			CASEELSE
				;PRINTFORMW 「」
			ENDSELECT
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;兵法学習
;-------------------------------------------------
IF SELECTCOM == 303
	;初めて(主導権を考慮しない)
	IF IS_COM_FIRST(1)
		;PRINTFORMW
	;二回目以降
	ELSE
		;恋慕 or 服従 or 親友
		IF TALENT:恋慕 || TALENT:服従 || TALENT:親友
			SELECTCASE RAND:3
			CASE 1
				;PRINTFORMW 「」
			CASE 2
				;PRINTFORMW 「」
			CASEELSE
				;PRINTFORMW 「」
			ENDSELECT
		ELSE
			SELECTCASE RAND:3
			CASE 1
				;PRINTFORMW 「」
			CASE 2
				;PRINTFORMW 「」
			CASEELSE
				;PRINTFORMW 「」
			ENDSELECT
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;政務
;-------------------------------------------------
IF SELECTCOM == 304
	;初めて(主導権を考慮しない)
	IF IS_COM_FIRST(1)
		PRINTFORMW "多谢你的帮助。得救了！"
		;TranslatedMark 「手伝ってくれるのね。助かるわ！」
	;二回目以降
	ELSE
		;恋慕 or 服従 or 親友
		IF TALENT:恋慕 || TALENT:服従 || TALENT:親友
			SELECTCASE RAND:3
			CASE 1
				;PRINTFORMW 「」
			CASE 2
				;PRINTFORMW 「」
			CASEELSE
				;PRINTFORMW 「」
			ENDSELECT
		ELSE
			SELECTCASE RAND:3
			CASE 1
				;PRINTFORMW 「」
			CASE 2
				;PRINTFORMW 「」
			CASEELSE
				;PRINTFORMW 「」
			ENDSELECT
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;警邏
;-------------------------------------------------
IF SELECTCOM == 305
	;初めて(主導権を考慮しない)
	IF IS_COM_FIRST(1)
		;PRINTFORMW
	;二回目以降
	ELSE
		;恋慕 or 服従 or 親友
		IF TALENT:恋慕 || TALENT:服従 || TALENT:親友
			SELECTCASE RAND:3
			CASE 1
				;PRINTFORMW 「」
			CASE 2
				;PRINTFORMW 「」
			CASEELSE
				;PRINTFORMW 「」
			ENDSELECT
		ELSE
			SELECTCASE RAND:3
			CASE 1
				;PRINTFORMW 「」
			CASE 2
				;PRINTFORMW 「」
			CASEELSE
				;PRINTFORMW 「」
			ENDSELECT
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;歌唱訓練
;-------------------------------------------------
IF SELECTCOM == 306
	;初めて(主導権を考慮しない)
	IF IS_COM_FIRST(1)
		;PRINTFORMW
	;二回目以降
	ELSE
		;恋慕 or 服従 or 親友
		IF TALENT:恋慕 || TALENT:服従 || TALENT:親友
			SELECTCASE RAND:3
			CASE 1
				;PRINTFORMW 「」
			CASE 2
				;PRINTFORMW 「」
			CASEELSE
				;PRINTFORMW 「」
			ENDSELECT
		ELSE
			SELECTCASE RAND:3
			CASE 1
				;PRINTFORMW 「」
			CASE 2
				;PRINTFORMW 「」
			CASEELSE
				;PRINTFORMW 「」
			ENDSELECT
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;料理
;-------------------------------------------------
IF SELECTCOM == 307
	;初めて(主導権を考慮しない)
	IF IS_COM_FIRST(1)
		;PRINTFORMW
	;二回目以降
	ELSE
		;恋慕 or 服従 or 親友
		IF TALENT:恋慕 || TALENT:服従 || TALENT:親友
			SELECTCASE RAND:3
			CASE 1
				;PRINTFORMW 「」
			CASE 2
				;PRINTFORMW 「」
			CASEELSE
				;PRINTFORMW 「」
			ENDSELECT
		ELSE
			SELECTCASE RAND:3
			CASE 1
				;PRINTFORMW 「」
			CASE 2
				;PRINTFORMW 「」
			CASEELSE
				;PRINTFORMW 「」
			ENDSELECT
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;猥談
;-------------------------------------------------
IF SELECTCOM == 309
	;成功
	IF TFLAG:17
		;このキャラが主導権を持っている
		IF IS_INITIATIVE(TARGET)
			;初めて
			IF IS_COM_FIRST()
				;PRINTFORMW
			;二回目以降
			ELSE
				;恋慕 or 服従 or 親友
				IF TALENT:恋慕 || TALENT:服従 || TALENT:親友
					;PRINTFORMW
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		;このキャラが主導権を持っていない
		ELSE
			;初めて
			IF IS_COM_FIRST()
				;PRINTFORMW
			;二回目以降
			ELSE
				;恋慕 or 服従 or 親友
				IF TALENT:恋慕 || TALENT:服従 || TALENT:親友
					;PRINTFORMW
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	;失敗
	ELSE
		;PRINTFORMW
	ENDIF
ENDIF

;-------------------------------------------------
;酒を飲む
;-------------------------------------------------
IF SELECTCOM == 310
	;初めて(主導権を考慮しない)
	IF IS_COM_FIRST(1)
		;PRINTFORMW
	;二回目以降
	ELSE
		;恋慕 or 服従 or 親友
		IF TALENT:恋慕 || TALENT:服従 || TALENT:親友
			;PRINTFORMW
		ELSE
			;PRINTFORMW
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;蜂蜜水
;-------------------------------------------------
IF SELECTCOM == 311
	;初めて(主導権を考慮しない)
	IF IS_COM_FIRST(1)
		;PRINTFORMW
	;二回目以降
	ELSE
		;恋慕 or 服従 or 親友
		IF TALENT:恋慕 || TALENT:服従 || TALENT:親友
			;PRINTFORMW
		;それ以外
		ELSE
			;PRINTFORMW
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;スキンシップ(ターゲット・される側)
;-------------------------------------------------
IF SELECTCOM == 320
	;温泉なら裸のスキンシップ
	IF TFLAG:54 == 7
		;このキャラが主導権を持っている
		IF IS_INITIATIVE(TARGET)
			;初めて
			IF IS_COM_FIRST()
				;既成事実Lv2以上
				IF TALENT:恋人 || TALENT:合意
					;PRINTFORMW
				ELSE
					;PRINTFORMW
				ENDIF
			;二回目以降
			ELSE
				;恋慕 or 服従 or 親友
				IF TALENT:恋慕 || TALENT:服従 || TALENT:親友
					;PRINTFORMW
				;既成事実Lv2以上
				ELSEIF TALENT:恋人 || TALENT:合意
					;PRINTFORMW
				;それ以外
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		;このキャラが主導権を持っていない
		ELSE
			;初めて
			IF IS_COM_FIRST()
				;既成事実Lv2以上
				IF TALENT:恋人 || TALENT:合意
					;PRINTFORMW
				ELSE
					;PRINTFORMW
				ENDIF
			;二回目以降
			ELSE
				;恋慕 or 服従 or 親友
				IF TALENT:恋慕 || TALENT:服従 || TALENT:親友
					;PRINTFORMW
				;既成事実Lv2以上
				ELSEIF TALENT:恋人 || TALENT:合意
					;PRINTFORMW
				;それ以外
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	ELSE
		;このキャラが主導権を持っている　ここから普通のスキンシップ
		IF IS_INITIATIVE(TARGET)
			;初めて
			IF IS_COM_FIRST()
				;既成事実Lv2以上
				IF TALENT:恋人 || TALENT:合意
					;PRINTFORMW
				ELSE
					;PRINTFORMW
				ENDIF
			;二回目以降
			ELSE
				;恋慕 or 服従 or 親友
				IF TALENT:恋慕 || TALENT:服従 || TALENT:親友
					;PRINTFORMW
				;既成事実Lv2以上
				ELSEIF TALENT:恋人 || TALENT:合意
					;PRINTFORMW
				;それ以外
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		;このキャラが主導権を持っていない
		ELSE
			;初めて
			IF IS_COM_FIRST()
				;既成事実Lv2以上
				IF TALENT:恋人 || TALENT:合意
					;PRINTFORMW
				ELSE
					;PRINTFORMW
				ENDIF
			;二回目以降
			ELSE
				;恋慕 or 服従 or 親友
				IF TALENT:恋慕 || TALENT:服従 || TALENT:親友
					;PRINTFORMW
				;既成事実Lv2以上
				ELSEIF TALENT:恋人 || TALENT:合意
					;PRINTFORMW
				;それ以外
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;頭を撫でる(ターゲット・される側)
;-------------------------------------------------
IF SELECTCOM == 321
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;既成事実Lv2以上
			IF TALENT:恋人 || TALENT:合意
				;PRINTFORMW
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従 or 親友
			IF TALENT:恋慕 || TALENT:服従 || TALENT:親友
				;PRINTFORMW
			;既成事実Lv2以上
			ELSEIF TALENT:恋人 || TALENT:合意
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;既成事実Lv2以上
			IF TALENT:恋人 || TALENT:合意
				;PRINTFORMW
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従 or 親友
			IF TALENT:恋慕 || TALENT:服従 || TALENT:親友
				;PRINTFORMW
			;既成事実Lv2以上
			ELSEIF TALENT:恋人 || TALENT:合意
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;髪を梳く(ターゲット・される側)
;-------------------------------------------------
IF SELECTCOM == 322
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;既成事実Lv2以上
			IF TALENT:恋人 || TALENT:合意
				;PRINTFORMW
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従 or 親友
			IF TALENT:恋慕 || TALENT:服従 || TALENT:親友
				;PRINTFORMW
			;既成事実Lv2以上
			ELSEIF TALENT:恋人 || TALENT:合意
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;既成事実Lv2以上
			IF TALENT:恋人 || TALENT:合意
				;PRINTFORMW
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従 or 親友
			IF TALENT:恋慕 || TALENT:服従 || TALENT:親友
				;PRINTFORMW
			;既成事実Lv2以上
			ELSEIF TALENT:恋人 || TALENT:合意
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;櫛で梳かす(ターゲット・される側)
;-------------------------------------------------
IF SELECTCOM == 323
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;既成事実Lv2以上
			IF TALENT:恋人 || TALENT:合意
				;PRINTFORMW
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従 or 親友
			IF TALENT:恋慕 || TALENT:服従 || TALENT:親友
				;PRINTFORMW
			;既成事実Lv2以上
			ELSEIF TALENT:恋人 || TALENT:合意
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;既成事実Lv2以上
			IF TALENT:恋人 || TALENT:合意
				;PRINTFORMW
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従 or 親友
			IF TALENT:恋慕 || TALENT:服従 || TALENT:親友
				;PRINTFORMW
			;既成事実Lv2以上
			ELSEIF TALENT:恋人 || TALENT:合意
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;甘い言葉(ターゲット・される側)
;-------------------------------------------------
IF SELECTCOM == 324
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;既成事実Lv2以上
			IF TALENT:恋人 || TALENT:合意
				;PRINTFORMW
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従 or 親友
			IF TALENT:恋慕 || TALENT:服従 || TALENT:親友
				;PRINTFORMW
			;既成事実Lv2以上
			ELSEIF TALENT:恋人 || TALENT:合意
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;既成事実Lv2以上
			IF TALENT:恋人 || TALENT:合意
				;PRINTFORMW
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従 or 親友
			IF TALENT:恋慕 || TALENT:服従 || TALENT:親友
				;PRINTFORMW
			;既成事実Lv2以上
			ELSEIF TALENT:恋人 || TALENT:合意
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;見つめ合う
;-------------------------------------------------
IF SELECTCOM == 325
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;既成事実Lv2以上
			IF TALENT:恋人 || TALENT:合意
				;PRINTFORMW
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従 or 親友
			IF TALENT:恋慕 || TALENT:服従 || TALENT:親友
				;PRINTFORMW
			;既成事実Lv2以上
			ELSEIF TALENT:恋人 || TALENT:合意
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;既成事実Lv2以上
			IF TALENT:恋人 || TALENT:合意
				;PRINTFORMW
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従 or 親友
			IF TALENT:恋慕 || TALENT:服従 || TALENT:親友
				;PRINTFORMW
			;既成事実Lv2以上
			ELSEIF TALENT:恋人 || TALENT:合意
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;お腹の音を聴く
;-------------------------------------------------
IF SELECTCOM == 326
	;父親が主人公で確定している場合
	IF CFLAG:23 == GET_ID(MASTER)
		;妊娠８期以降(ぼてっ)
		IF CFLAG:18 >= 8
			;恋慕 or 服従 or 親友
			IF TALENT:恋慕 || TALENT:服従 || TALENT:親友
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;妊娠６期以降(ぽっこり)
		ELSEIF CFLAG:18 >= 6
			;恋慕 or 服従 or 親友
			IF TALENT:恋慕 || TALENT:服従 || TALENT:親友
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;妊娠５期以前
		ELSE
			;恋慕 or 服従 or 親友
			IF TALENT:恋慕 || TALENT:服従 || TALENT:親友
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;父親不明、もしくは他の士官の子
	ELSE
		;妊娠８期以降(ぼてっ)
		IF CFLAG:18 >= 8
			;恋慕 or 服従 or 親友
			IF TALENT:恋慕 || TALENT:服従 || TALENT:親友
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;妊娠６期以降(ぽっこり)
		ELSEIF CFLAG:18 >= 6
			;恋慕 or 服従 or 親友
			IF TALENT:恋慕 || TALENT:服従 || TALENT:親友
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;妊娠５期以前
		ELSE
			;恋慕 or 服従 or 親友
			IF TALENT:恋慕 || TALENT:服従 || TALENT:親友
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;日常フェラ(ターゲット・される側。要ペニス)
;-------------------------------------------------
IF SELECTCOM == 330
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;既成事実Lv2以上
			IF TALENT:恋人 || TALENT:合意
				;PRINTFORMW
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従 or 親友
			IF TALENT:恋慕 || TALENT:服従 || TALENT:親友
				;PRINTFORMW
			;既成事実Lv2以上
			ELSEIF TALENT:恋人 || TALENT:合意
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;既成事実Lv2以上
			IF TALENT:恋人 || TALENT:合意
				;PRINTFORMW
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従 or 親友
			IF TALENT:恋慕 || TALENT:服従 || TALENT:親友
				;PRINTFORMW
			;既成事実Lv2以上
			ELSEIF TALENT:恋人 || TALENT:合意
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;バードキス(ターゲット・される側)
;-------------------------------------------------
IF SELECTCOM == 340
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			CFLAG:250 = 1;初めてキスをしたフラグ
			;既成事実Lv2以上
			IF TALENT:恋人 || TALENT:合意
				;ファーストキス
				IF CFLAG:251 == 1
					;PRINTFORMW
				;キス経験済み
				ELSE
					;PRINTFORMW
				ENDIF
			ELSE
				;ファーストキス
				IF CFLAG:251 == 1
					;PRINTFORMW
				;キス経験済み
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従 or 親友
			IF TALENT:恋慕 || TALENT:服従 || TALENT:親友
				;PRINTFORMW
			;既成事実Lv2以上
			ELSEIF TALENT:恋人 || TALENT:合意
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			CFLAG:250 = 1;初めてキスをしたフラグ
			;既成事実Lv2以上
			IF TALENT:恋人 || TALENT:合意
				;ファーストキス
				IF CFLAG:251 == 1
					;PRINTFORMW
				;キス経験済み
				ELSE
					;PRINTFORMW
				ENDIF
			ELSE
				;ファーストキス
				IF CFLAG:251 == 1
					;PRINTFORMW
				;キス経験済み
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従 or 親友
			IF TALENT:恋慕 || TALENT:服従 || TALENT:親友
				;PRINTFORMW
			;既成事実Lv2以上
			ELSEIF TALENT:恋人 || TALENT:合意
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;ソフトキス(ターゲット・される側)
;-------------------------------------------------
IF SELECTCOM == 341
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			CFLAG:250 = 1;初めてキスをしたフラグ
			;既成事実Lv2以上
			IF TALENT:恋人 || TALENT:合意
				;ファーストキス
				IF CFLAG:251 == 1
					;PRINTFORMW
				;キス経験済み
				ELSE
					;PRINTFORMW
				ENDIF
			ELSE
				;ファーストキス
				IF CFLAG:251 == 1
					;PRINTFORMW
				;キス経験済み
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従 or 親友
			IF TALENT:恋慕 || TALENT:服従 || TALENT:親友
				;PRINTFORMW
			;既成事実Lv2以上
			ELSEIF TALENT:恋人 || TALENT:合意
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			CFLAG:250 = 1;初めてキスをしたフラグ
			;既成事実Lv2以上
			IF TALENT:恋人 || TALENT:合意
				;ファーストキス
				IF CFLAG:251 == 1
					;PRINTFORMW
				;キス経験済み
				ELSE
					;PRINTFORMW
				ENDIF
			ELSE
				;ファーストキス
				IF CFLAG:251 == 1
					;PRINTFORMW
				;キス経験済み
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従 or 親友
			IF TALENT:恋慕 || TALENT:服従 || TALENT:親友
				;PRINTFORMW
			;既成事実Lv2以上
			ELSEIF TALENT:恋人 || TALENT:合意
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;ディープキス(ターゲット・される側)
;-------------------------------------------------
IF SELECTCOM == 342
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			CFLAG:250 = 1;初めてキスをしたフラグ
			;既成事実Lv2以上
			IF TALENT:恋人 || TALENT:合意
				;ファーストキス
				IF CFLAG:251 == 1
					;PRINTFORMW
				;キス経験済み
				ELSE
					;PRINTFORMW
				ENDIF
			ELSE
				;ファーストキス
				IF CFLAG:251 == 1
					;PRINTFORMW
				;キス経験済み
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従 or 親友
			IF TALENT:恋慕 || TALENT:服従 || TALENT:親友
				;PRINTFORMW
			;既成事実Lv2以上
			ELSEIF TALENT:恋人 || TALENT:合意
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			CFLAG:250 = 1;初めてキスをしたフラグ
			;既成事実Lv2以上
			IF TALENT:恋人 || TALENT:合意
				;ファーストキス
				IF CFLAG:251 == 1
					;PRINTFORMW
				;キス経験済み
				ELSE
					;PRINTFORMW
				ENDIF
			ELSE
				;ファーストキス
				IF CFLAG:251 == 1
					;PRINTFORMW
				;キス経験済み
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従 or 親友
			IF TALENT:恋慕 || TALENT:服従 || TALENT:親友
				;PRINTFORMW
			;既成事実Lv2以上
			ELSEIF TALENT:恋人 || TALENT:合意
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
ENDIF


;-------------------------------------------------
;食事
;-------------------------------------------------
IF SELECTCOM == 351
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;既成事実Lv2以上
			IF TALENT:恋人 || TALENT:合意
				;PRINTFORMW
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従 or 親友
			IF TALENT:恋慕 || TALENT:服従 || TALENT:親友
				;PRINTFORMW
			;既成事実Lv2以上
			ELSEIF TALENT:恋人 || TALENT:合意
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;既成事実Lv2以上
			IF TALENT:恋人 || TALENT:合意
				;PRINTFORMW
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従 or 親友
			IF TALENT:恋慕 || TALENT:服従 || TALENT:親友
				;PRINTFORMW
			;既成事実Lv2以上
			ELSEIF TALENT:恋人 || TALENT:合意
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;本屋に入る　現状、死に分岐
;-------------------------------------------------
IF SELECTCOM == 352
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;既成事実Lv2以上
			IF TALENT:恋人 || TALENT:合意
				;PRINTFORMW
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従 or 親友
			IF TALENT:恋慕 || TALENT:服従 || TALENT:親友
				;PRINTFORMW
			;既成事実Lv2以上
			ELSEIF TALENT:恋人 || TALENT:合意
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;既成事実Lv2以上
			IF TALENT:恋人 || TALENT:合意
				;PRINTFORMW
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従 or 親友
			IF TALENT:恋慕 || TALENT:服従 || TALENT:親友
				;PRINTFORMW
			;既成事実Lv2以上
			ELSEIF TALENT:恋人 || TALENT:合意
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;水浴び
;-------------------------------------------------
IF SELECTCOM == 354
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;既成事実Lv2以上
			IF TALENT:恋人 || TALENT:合意
				;PRINTFORMW
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従 or 親友
			IF TALENT:恋慕 || TALENT:服従 || TALENT:親友
				;PRINTFORMW
			;既成事実Lv2以上
			ELSEIF TALENT:恋人 || TALENT:合意
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;既成事実Lv2以上
			IF TALENT:恋人 || TALENT:合意
				;PRINTFORMW
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従 or 親友
			IF TALENT:恋慕 || TALENT:服従 || TALENT:親友
				;PRINTFORMW
			;既成事実Lv2以上
			ELSEIF TALENT:恋人 || TALENT:合意
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;泳ぐ
;-------------------------------------------------
IF SELECTCOM == 355
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;既成事実Lv2以上
			IF TALENT:恋人 || TALENT:合意
				;PRINTFORMW
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従 or 親友
			IF TALENT:恋慕 || TALENT:服従 || TALENT:親友
				;PRINTFORMW
			;既成事実Lv2以上
			ELSEIF TALENT:恋人 || TALENT:合意
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;既成事実Lv2以上
			IF TALENT:恋人 || TALENT:合意
				;PRINTFORMW
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従 or 親友
			IF TALENT:恋慕 || TALENT:服従 || TALENT:親友
				;PRINTFORMW
			;既成事実Lv2以上
			ELSEIF TALENT:恋人 || TALENT:合意
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;花冠を作る(ターゲット・される側)
;-------------------------------------------------
IF SELECTCOM == 356
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;既成事実Lv2以上
			IF TALENT:恋人 || TALENT:合意
				;PRINTFORMW
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従 or 親友
			IF TALENT:恋慕 || TALENT:服従 || TALENT:親友
				;PRINTFORMW
			;既成事実Lv2以上
			ELSEIF TALENT:恋人 || TALENT:合意
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;既成事実Lv2以上
			IF TALENT:恋人 || TALENT:合意
				;PRINTFORMW
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従 or 親友
			IF TALENT:恋慕 || TALENT:服従 || TALENT:親友
				;PRINTFORMW
			;既成事実Lv2以上
			ELSEIF TALENT:恋人 || TALENT:合意
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;狩りをする
;-------------------------------------------------
IF SELECTCOM == 357
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;既成事実Lv2以上
			IF TALENT:恋人 || TALENT:合意
				;PRINTFORMW
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従 or 親友
			IF TALENT:恋慕 || TALENT:服従 || TALENT:親友
				;PRINTFORMW
			;既成事実Lv2以上
			ELSEIF TALENT:恋人 || TALENT:合意
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;既成事実Lv2以上
			IF TALENT:恋人 || TALENT:合意
				;PRINTFORMW
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従 or 親友
			IF TALENT:恋慕 || TALENT:服従 || TALENT:親友
				;PRINTFORMW
			;既成事実Lv2以上
			ELSEIF TALENT:恋人 || TALENT:合意
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;背中を流す(ターゲット・される側)
;-------------------------------------------------
IF SELECTCOM == 358
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;既成事実Lv2以上
			IF TALENT:恋人 || TALENT:合意
				;PRINTFORMW
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従 or 親友
			IF TALENT:恋慕 || TALENT:服従 || TALENT:親友
				;PRINTFORMW
			;既成事実Lv2以上
			ELSEIF TALENT:恋人 || TALENT:合意
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;既成事実Lv2以上
			IF TALENT:恋人 || TALENT:合意
				;PRINTFORMW
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従 or 親友
			IF TALENT:恋慕 || TALENT:服従 || TALENT:親友
				;PRINTFORMW
			;既成事実Lv2以上
			ELSEIF TALENT:恋人 || TALENT:合意
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;二人で出掛ける(ターゲット・される側)
;-------------------------------------------------
IF SELECTCOM == 390
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;既成事実Lv2以上
			IF TALENT:恋人 || TALENT:合意
				;PRINTFORMW
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従 or 親友
			IF TALENT:恋慕 || TALENT:服従 || TALENT:親友
				;PRINTFORMW
			;既成事実Lv2以上
			ELSEIF TALENT:恋人 || TALENT:合意
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;既成事実Lv2以上
			IF TALENT:恋人 || TALENT:合意
				;PRINTFORMW
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従 or 親友
			IF TALENT:恋慕 || TALENT:服従 || TALENT:親友
				;PRINTFORMW
			;既成事実Lv2以上
			ELSEIF TALENT:恋人 || TALENT:合意
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;移動(ターゲット・される側)
;-------------------------------------------------
IF SELECTCOM == 391
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;既成事実Lv2以上
			IF TALENT:恋人 || TALENT:合意
				;PRINTFORMW
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従 or 親友
			IF TALENT:恋慕 || TALENT:服従 || TALENT:親友
				;PRINTFORMW
			;既成事実Lv2以上
			ELSEIF TALENT:恋人 || TALENT:合意
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;既成事実Lv2以上
			IF TALENT:恋人 || TALENT:合意
				;PRINTFORMW
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従 or 親友
			IF TALENT:恋慕 || TALENT:服従 || TALENT:親友
				;PRINTFORMW
			;既成事実Lv2以上
			ELSEIF TALENT:恋人 || TALENT:合意
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;帰る(ターゲット・される側)
;-------------------------------------------------
IF SELECTCOM == 392
	;初めて(主導権を考慮しない)
	IF IS_COM_FIRST(1)
		;既成事実Lv2以上
		IF TALENT:恋人 || TALENT:合意
			;PRINTFORMW
		ELSE
			;PRINTFORMW
		ENDIF
	;二回目以降
	ELSE
		;恋慕 or 服従 or 親友
		IF TALENT:恋慕 || TALENT:服従 || TALENT:親友
			;PRINTFORMW
		;既成事実Lv2以上
		ELSEIF TALENT:恋人 || TALENT:合意
			;PRINTFORMW
		;それ以外
		ELSE
			;PRINTFORMW
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;なりゆきまかせ
;-------------------------------------------------
IF SELECTCOM == 393
	;初めて(主導権を考慮しない)
	IF IS_COM_FIRST(1)
		;既成事実Lv2以上
		IF TALENT:恋人 || TALENT:合意
			;PRINTFORMW
		ELSE
			;PRINTFORMW
		ENDIF
	;二回目以降
	ELSE
		;恋慕 or 服従 or 親友
		IF TALENT:恋慕 || TALENT:服従 || TALENT:親友
			;PRINTFORMW
		;既成事実Lv2以上
		ELSEIF TALENT:恋人 || TALENT:合意
			;PRINTFORMW
		;それ以外
		ELSE
			;PRINTFORMW
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;告白する(ターゲット・される側)　告白描写はKOJO_EVENTで書いておけばいいので、ここの箇所は特に必要ないかも
;-------------------------------------------------
IF SELECTCOM == 399
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;既成事実Lv2以上
			IF TALENT:恋人 || TALENT:合意
				;PRINTFORMW
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従 or 親友
			IF TALENT:恋慕 || TALENT:服従 || TALENT:親友
				;PRINTFORMW
			;既成事実Lv2以上
			ELSEIF TALENT:恋人 || TALENT:合意
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;既成事実Lv2以上
			IF TALENT:恋人 || TALENT:合意
				;PRINTFORMW
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従 or 親友
			IF TALENT:恋慕 || TALENT:服従 || TALENT:親友
				;PRINTFORMW
			;既成事実Lv2以上
			ELSEIF TALENT:恋人 || TALENT:合意
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;武術を教える(ターゲット・される側)　子供に指導する際の台詞
;-------------------------------------------------
IF SELECTCOM == 500
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;兵法を教える(ターゲット・される側)　子供に指導する際の台詞
;-------------------------------------------------
IF SELECTCOM == 501
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;政治を教える(ターゲット・される側)　子供に指導する際の台詞
;-------------------------------------------------
IF SELECTCOM == 502
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;歌の練習(ターゲット・される側)　子供に指導する際の台詞
;-------------------------------------------------
IF SELECTCOM == 503
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;料理を作る(ターゲット・される側)　子供に指導する際の台詞
;-------------------------------------------------
IF SELECTCOM == 504
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;性の知識を教える(ターゲット・される側)　子供に指導する際の台詞
;-------------------------------------------------
IF SELECTCOM == 550
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;恋慕 or 服従 or 親友
		IF TALENT:恋慕 || TALENT:服従 || TALENT:親友
			;PRINTFORMW
		;それ以外
		ELSE
			;PRINTFORMW
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;恋慕 or 服従 or 親友
		IF TALENT:恋慕 || TALENT:服従 || TALENT:親友
			;PRINTFORMW
		;それ以外
		ELSE
			;PRINTFORMW
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;自国に誘う(ターゲット・される側)
;-------------------------------------------------
IF SELECTCOM == 551
	;初めて
	IF IS_COM_FIRST()
		;恋慕 or 服従
		IF TALENT:恋慕 || TALENT:服従
			;PRINTFORMW
		;それ以外
		ELSE
			;PRINTFORMW
		ENDIF
	;二回目以降
	ELSE
		;恋慕 or 服従
		IF TALENT:恋慕 || TALENT:服従
			;PRINTFORMW
		;依存度300以上
		ELSEIF CFLAG:3 >= 300
			;PRINTFORMW
		;それ以外
		ELSE
			;PRINTFORMW
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;記憶を断ち切らせる(ターゲット・される側)
;-------------------------------------------------
IF SELECTCOM == 554
	;初めて
	IF IS_COM_FIRST()
		;恋慕 or 服従
		IF TALENT:恋慕 || TALENT:服従
			;PRINTFORMW
		;それ以外
		ELSE
			;PRINTFORMW
		ENDIF
	;二回目以降
	ELSE
		;恋慕 or 服従
		IF TALENT:恋慕 || TALENT:服従
			;PRINTFORMW
		;依存度300以上
		ELSEIF CFLAG:3 >= 300
			;PRINTFORMW
		;それ以外
		ELSE
			;PRINTFORMW
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;=================================================
;●コマンド実行時(このキャラがプレイヤー側のとき)
;=================================================
@KOJO_COM_PLAYER_A_K145
;[空虚]状態の場合、口が塞がっている場合は口上を表示しない
IF TALENT:空虚 || IS_EQUIP_CONTINUE(TARGET, "口装着")
	RETURN 0
ENDIF

;-------------------------------------------------
;愛撫(プレイヤー・する側)
;-------------------------------------------------
IF SELECTCOM == 0
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;胸愛撫(プレイヤー・する側)
;-------------------------------------------------
IF SELECTCOM == 1
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;クンニ(プレイヤー・舐める側)
;-------------------------------------------------
IF SELECTCOM == 2
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;指挿入れ(プレイヤー・入れる側)
;-------------------------------------------------
IF SELECTCOM == 3
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;主人公が処女
			IF TALENT:MASTER:処女
				;恋慕 or 服従
				IF TALENT:恋慕 || TALENT:服従
					;PRINTFORMW
				;それ以外
				ELSE
					;PRINTFORMW
				ENDIF
			;主人公が非処女
			ELSE
				;恋慕 or 服従
				IF TALENT:恋慕 || TALENT:服従
					;PRINTFORMW
				;それ以外
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;主人公が処女
			IF TALENT:MASTER:処女
				;恋慕 or 服従
				IF TALENT:恋慕 || TALENT:服従
					;PRINTFORMW
				;それ以外
				ELSE
					;PRINTFORMW
				ENDIF
			;主人公が非処女
			ELSE
				;恋慕 or 服従
				IF TALENT:恋慕 || TALENT:服従
					;PRINTFORMW
				;それ以外
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;アナル愛撫(プレイヤー・する側)
;-------------------------------------------------
IF SELECTCOM == 4
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;泡踊り(プレイヤー・する側)
;-------------------------------------------------
IF SELECTCOM == 5
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;尻尾挿入(プレイヤー・挿れる側)
;-------------------------------------------------
IF SELECTCOM == 6
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;主人公が処女
			IF CFLAG:253 == 1
				;恋慕 or 服従
				IF TALENT:恋慕 || TALENT:服従
					;PRINTFORMW
				;それ以外
				ELSE
					;PRINTFORMW
				ENDIF
			;主人公が非処女
			ELSE
				;恋慕 or 服従
				IF TALENT:恋慕 || TALENT:服従
					;PRINTFORMW
				;それ以外
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;主人公が処女
			IF CFLAG:253 == 1
				;恋慕 or 服従
				IF TALENT:恋慕 || TALENT:服従
					;PRINTFORMW
				;それ以外
				ELSE
					;PRINTFORMW
				ENDIF
			;主人公が非処女
			ELSE
				;恋慕 or 服従
				IF TALENT:恋慕 || TALENT:服従
					;PRINTFORMW
				;それ以外
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;尻尾Ａ挿入(プレイヤー・挿れる側)
;-------------------------------------------------
IF SELECTCOM == 7
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;アナル舐め(プレイヤー・する側)
;-------------------------------------------------
IF SELECTCOM == 8
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;Ａ感Lv3以上
				IF ABL:Ａ感 >= 3
					;PRINTFORMW
				;Ａ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;Ａ感Lv3以上
				IF ABL:Ａ感 >= 3
					;PRINTFORMW
				;Ａ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;Ａ感Lv3以上
				IF ABL:Ａ感 >= 3
					;PRINTFORMW
				;Ａ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;Ａ感Lv3以上
				IF ABL:Ａ感 >= 3
					;PRINTFORMW
				;Ａ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;乳首吸い(プレイヤー・吸う側)
;-------------------------------------------------
IF SELECTCOM == 9
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;Ｂ感Lv3以上
				IF ABL:Ｂ感 >= 3
					;PRINTFORMW
				;Ｂ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;Ｂ感Lv3以上
				IF ABL:Ｂ感 >= 3
					;PRINTFORMW
				;Ｂ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;Ｂ感Lv3以上
				IF ABL:Ｂ感 >= 3
					;PRINTFORMW
				;Ｂ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;Ｂ感Lv3以上
				IF ABL:Ｂ感 >= 3
					;PRINTFORMW
				;Ｂ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;手コキ(プレイヤー・シゴく側)
;-------------------------------------------------
IF SELECTCOM == 10
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;フェラチオ(プレイヤー・しゃぶる側)
;-------------------------------------------------
IF SELECTCOM == 11
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;パイズリ(プレイヤー・する側)
;-------------------------------------------------
IF SELECTCOM == 12
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;素股(プレイヤー・する側)
;-------------------------------------------------
IF SELECTCOM == 13
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;足コキ(プレイヤー・シゴく側)
;-------------------------------------------------
IF SELECTCOM == 14
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;尻コキ(プレイヤー・シゴく側)
;-------------------------------------------------
IF SELECTCOM == 15
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;オナホコキ(プレイヤー・シゴく側)
;-------------------------------------------------
IF SELECTCOM == 16
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;尻尾コキ(プレイヤー・シゴく側)
;-------------------------------------------------
IF SELECTCOM == 17
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF
;-------------------------------------------------
;指フェラ(プレイヤー・しゃぶる側)
;-------------------------------------------------
IF SELECTCOM == 24
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;ニプルファック(プレイヤー・ファックする側。要ペニス)
;-------------------------------------------------
IF SELECTCOM == 25
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;正常位(プレイヤー・挿れる側。要ペニス)
;-------------------------------------------------
IF SELECTCOM == 30
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;主人公が処女
			IF CFLAG:253 == 1
				;恋慕 or 服従
				IF TALENT:恋慕 || TALENT:服従
					;PRINTFORMW
				;それ以外
				ELSE
					;PRINTFORMW
				ENDIF
			;主人公が非処女
			ELSE
				;恋慕 or 服従
				IF TALENT:恋慕 || TALENT:服従
					;PRINTFORMW
				;それ以外
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;主人公が処女
			IF CFLAG:253 == 1
				;恋慕 or 服従
				IF TALENT:恋慕 || TALENT:服従
					;PRINTFORMW
				;それ以外
				ELSE
					;PRINTFORMW
				ENDIF
			;主人公が非処女
			ELSE
				;恋慕 or 服従
				IF TALENT:恋慕 || TALENT:服従
					;PRINTFORMW
				;それ以外
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;後背位(プレイヤー・挿れる側。要ペニス)
;-------------------------------------------------
IF SELECTCOM == 31
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;主人公が処女
			IF CFLAG:253 == 1
				;恋慕 or 服従
				IF TALENT:恋慕 || TALENT:服従
					;PRINTFORMW
				;それ以外
				ELSE
					;PRINTFORMW
				ENDIF
			;主人公が非処女
			ELSE
				;恋慕 or 服従
				IF TALENT:恋慕 || TALENT:服従
					;PRINTFORMW
				;それ以外
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;主人公が処女
			IF CFLAG:253 == 1
				;恋慕 or 服従
				IF TALENT:恋慕 || TALENT:服従
					;PRINTFORMW
				;それ以外
				ELSE
					;PRINTFORMW
				ENDIF
			;主人公が非処女
			ELSE
				;恋慕 or 服従
				IF TALENT:恋慕 || TALENT:服従
					;PRINTFORMW
				;それ以外
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;対面座位(プレイヤー・挿れる側。要ペニス)
;-------------------------------------------------
IF SELECTCOM == 32
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;主人公が処女
			IF CFLAG:253 == 1
				;恋慕 or 服従
				IF TALENT:恋慕 || TALENT:服従
					;PRINTFORMW
				;それ以外
				ELSE
					;PRINTFORMW
				ENDIF
			;主人公が非処女
			ELSE
				;恋慕 or 服従
				IF TALENT:恋慕 || TALENT:服従
					;PRINTFORMW
				;それ以外
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;主人公が処女
			IF CFLAG:253 == 1
				;恋慕 or 服従
				IF TALENT:恋慕 || TALENT:服従
					;PRINTFORMW
				;それ以外
				ELSE
					;PRINTFORMW
				ENDIF
			;主人公が非処女
			ELSE
				;恋慕 or 服従
				IF TALENT:恋慕 || TALENT:服従
					;PRINTFORMW
				;それ以外
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;背面座位(プレイヤー・挿れる側。要ペニス)
;-------------------------------------------------
IF SELECTCOM == 33
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;主人公が処女
			IF CFLAG:253 == 1
				;恋慕 or 服従
				IF TALENT:恋慕 || TALENT:服従
					;PRINTFORMW
				;それ以外
				ELSE
					;PRINTFORMW
				ENDIF
			;主人公が非処女
			ELSE
				;恋慕 or 服従
				IF TALENT:恋慕 || TALENT:服従
					;PRINTFORMW
				;それ以外
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;主人公が処女
			IF CFLAG:253 == 1
				;恋慕 or 服従
				IF TALENT:恋慕 || TALENT:服従
					;PRINTFORMW
				;それ以外
				ELSE
					;PRINTFORMW
				ENDIF
			;主人公が非処女
			ELSE
				;恋慕 or 服従
				IF TALENT:恋慕 || TALENT:服従
					;PRINTFORMW
				;それ以外
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;騎乗位(プレイヤー・挿れる側。要ペニス)
;-------------------------------------------------
IF SELECTCOM == 34
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;主人公が処女
			IF CFLAG:253 == 1
				;恋慕 or 服従
				IF TALENT:恋慕 || TALENT:服従
					;PRINTFORMW
				;それ以外
				ELSE
					;PRINTFORMW
				ENDIF
			;主人公が非処女
			ELSE
				;恋慕 or 服従
				IF TALENT:恋慕 || TALENT:服従
					;PRINTFORMW
				;それ以外
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;主人公が処女
			IF CFLAG:253 == 1
				;恋慕 or 服従
				IF TALENT:恋慕 || TALENT:服従
					;PRINTFORMW
				;それ以外
				ELSE
					;PRINTFORMW
				ENDIF
			;主人公が非処女
			ELSE
				;恋慕 or 服従
				IF TALENT:恋慕 || TALENT:服従
					;PRINTFORMW
				;それ以外
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;背面騎乗位(プレイヤー・挿れる側。要ペニス)
;-------------------------------------------------
IF SELECTCOM == 35
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;主人公が処女
			IF CFLAG:253 == 1
				;恋慕 or 服従
				IF TALENT:恋慕 || TALENT:服従
					;PRINTFORMW
				;それ以外
				ELSE
					;PRINTFORMW
				ENDIF
			;主人公が非処女
			ELSE
				;恋慕 or 服従
				IF TALENT:恋慕 || TALENT:服従
					;PRINTFORMW
				;それ以外
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;主人公が処女
			IF CFLAG:253 == 1
				;恋慕 or 服従
				IF TALENT:恋慕 || TALENT:服従
					;PRINTFORMW
				;それ以外
				ELSE
					;PRINTFORMW
				ENDIF
			;主人公が非処女
			ELSE
				;恋慕 or 服従
				IF TALENT:恋慕 || TALENT:服従
					;PRINTFORMW
				;それ以外
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;二本挿入(プレイヤー・挿れる側。要ペニス)
;-------------------------------------------------
IF SELECTCOM == 36
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;側位(プレイヤー・挿れる側。要ペニス)
;-------------------------------------------------
IF SELECTCOM == 37
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;主人公が処女
			IF CFLAG:253 == 1
				;恋慕 or 服従
				IF TALENT:恋慕 || TALENT:服従
					;PRINTFORMW
				;それ以外
				ELSE
					;PRINTFORMW
				ENDIF
			;主人公が非処女
			ELSE
				;恋慕 or 服従
				IF TALENT:恋慕 || TALENT:服従
					;PRINTFORMW
				;それ以外
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;主人公が処女
			IF CFLAG:253 == 1
				;恋慕 or 服従
				IF TALENT:恋慕 || TALENT:服従
					;PRINTFORMW
				;それ以外
				ELSE
					;PRINTFORMW
				ENDIF
			;主人公が非処女
			ELSE
				;恋慕 or 服従
				IF TALENT:恋慕 || TALENT:服従
					;PRINTFORMW
				;それ以外
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;屈曲位(プレイヤー・挿れる側。要ペニス)
;-------------------------------------------------
IF SELECTCOM == 38
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;主人公が処女
			IF CFLAG:253 == 1
				;恋慕 or 服従
				IF TALENT:恋慕 || TALENT:服従
					;PRINTFORMW
				;それ以外
				ELSE
					;PRINTFORMW
				ENDIF
			;主人公が非処女
			ELSE
				;恋慕 or 服従
				IF TALENT:恋慕 || TALENT:服従
					;PRINTFORMW
				;それ以外
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;主人公が処女
			IF CFLAG:253 == 1
				;恋慕 or 服従
				IF TALENT:恋慕 || TALENT:服従
					;PRINTFORMW
				;それ以外
				ELSE
					;PRINTFORMW
				ENDIF
			;主人公が非処女
			ELSE
				;恋慕 or 服従
				IF TALENT:恋慕 || TALENT:服従
					;PRINTFORMW
				;それ以外
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;駅弁(プレイヤー・挿れる側。要ペニス)
;-------------------------------------------------
IF SELECTCOM == 39
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;主人公が処女
			IF CFLAG:253 == 1
				;恋慕 or 服従
				IF TALENT:恋慕 || TALENT:服従
					;PRINTFORMW
				;それ以外
				ELSE
					;PRINTFORMW
				ENDIF
			;主人公が非処女
			ELSE
				;恋慕 or 服従
				IF TALENT:恋慕 || TALENT:服従
					;PRINTFORMW
				;それ以外
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;主人公が処女
			IF CFLAG:253 == 1
				;恋慕 or 服従
				IF TALENT:恋慕 || TALENT:服従
					;PRINTFORMW
				;それ以外
				ELSE
					;PRINTFORMW
				ENDIF
			;主人公が非処女
			ELSE
				;恋慕 or 服従
				IF TALENT:恋慕 || TALENT:服従
					;PRINTFORMW
				;それ以外
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;背面駅弁(プレイヤー・挿れる側。要ペニス)
;-------------------------------------------------
IF SELECTCOM == 52
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;主人公が処女
			IF CFLAG:253 == 1
				;恋慕 or 服従
				IF TALENT:恋慕 || TALENT:服従
					;PRINTFORMW
				;それ以外
				ELSE
					;PRINTFORMW
				ENDIF
			;主人公が非処女
			ELSE
				;恋慕 or 服従
				IF TALENT:恋慕 || TALENT:服従
					;PRINTFORMW
				;それ以外
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;主人公が処女
			IF CFLAG:253 == 1
				;恋慕 or 服従
				IF TALENT:恋慕 || TALENT:服従
					;PRINTFORMW
				;それ以外
				ELSE
					;PRINTFORMW
				ENDIF
			;主人公が非処女
			ELSE
				;恋慕 or 服従
				IF TALENT:恋慕 || TALENT:服従
					;PRINTFORMW
				;それ以外
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;立ちバック(プレイヤー・挿れる側。要ペニス)
;-------------------------------------------------
IF SELECTCOM == 160
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;主人公が処女
			IF CFLAG:253 == 1
				;恋慕 or 服従
				IF TALENT:恋慕 || TALENT:服従
					;PRINTFORMW
				;それ以外
				ELSE
					;PRINTFORMW
				ENDIF
			;主人公が非処女
			ELSE
				;恋慕 or 服従
				IF TALENT:恋慕 || TALENT:服従
					;PRINTFORMW
				;それ以外
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;主人公が処女
			IF CFLAG:253 == 1
				;恋慕 or 服従
				IF TALENT:恋慕 || TALENT:服従
					;PRINTFORMW
				;それ以外
				ELSE
					;PRINTFORMW
				ENDIF
			;主人公が非処女
			ELSE
				;恋慕 or 服従
				IF TALENT:恋慕 || TALENT:服従
					;PRINTFORMW
				;それ以外
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;Ａ正常位(プレイヤー・挿れる側。要ペニス)
;-------------------------------------------------
IF SELECTCOM == 40
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;Ａ後背位(プレイヤー・挿れる側。要ペニス)
;-------------------------------------------------
IF SELECTCOM == 41
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;Ａ対面座位(プレイヤー・挿れる側。要ペニス)
;-------------------------------------------------
IF SELECTCOM == 42
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;Ａ背面座位(プレイヤー・挿れる側。要ペニス)
;-------------------------------------------------
IF SELECTCOM == 43
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;Ａ騎乗位(プレイヤー・挿れる側。要ペニス)
;-------------------------------------------------
IF SELECTCOM == 44
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;Ａ背面騎乗位(プレイヤー・挿れる側。要ペニス)
;-------------------------------------------------
IF SELECTCOM == 45
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;Ａ二本挿入(プレイヤー・挿れる側。要ペニス)
;-------------------------------------------------
IF SELECTCOM == 46
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;Ａ側位(プレイヤー・挿れる側。要ペニス)
;-------------------------------------------------
IF SELECTCOM == 47
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;Ａ屈曲位(プレイヤー・挿れる側。要ペニス)
;-------------------------------------------------
IF SELECTCOM == 48
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;Ａ駅弁(プレイヤー・挿れる側。要ペニス)
;-------------------------------------------------
IF SELECTCOM == 49
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;Ａ背面駅弁(プレイヤー・挿れる側。要ペニス)
;-------------------------------------------------
IF SELECTCOM == 53
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;立ちＡバック(プレイヤー・挿れる側。要ペニス)
;-------------------------------------------------
IF SELECTCOM == 161
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;ペニバン装着（プレイヤー)
;-------------------------------------------------
IF SELECTCOM == 50
	;初めて(主導権を考慮しない)
	IF IS_COM_FIRST(1)
		;恋慕 or 服従
		IF TALENT:恋慕 || TALENT:服従
			;PRINTFORMW
		;それ以外
		ELSE
			;PRINTFORMW
		ENDIF
	;二回目以降
	ELSE
		;恋慕 or 服従
		IF TALENT:恋慕 || TALENT:服従
			;PRINTFORMW
		;それ以外
		ELSE
			;PRINTFORMW
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;酒恥肉倫(プレイヤー・する側)
;-------------------------------------------------
IF SELECTCOM == 51
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;順番に挿入(プレイヤー・挿れる側。要ペニス)
;-------------------------------------------------
IF SELECTCOM == 55
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;順番にＡ挿入(プレイヤー・挿れる側。要ペニス)
;-------------------------------------------------
IF SELECTCOM == 56
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;代わる代わる挿入(プレイヤー・挿れる側。要ペニス)
;-------------------------------------------------
IF SELECTCOM == 57
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;代わる代わるＡ挿入(プレイヤー・挿れる側。要ペニス)
;-------------------------------------------------
IF SELECTCOM == 58
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;ローター(プレイヤー・する側)
;-------------------------------------------------
IF SELECTCOM == 60
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;バイブ(プレイヤー・する側)
;-------------------------------------------------
IF SELECTCOM == 61
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;主人公が処女
			IF CFLAG:253 == 1
				;恋慕 or 服従
				IF TALENT:恋慕 || TALENT:服従
					;PRINTFORMW
				;それ以外
				ELSE
					;PRINTFORMW
				ENDIF
			;主人公が非処女
			ELSE
				;恋慕 or 服従
				IF TALENT:恋慕 || TALENT:服従
					;PRINTFORMW
				;それ以外
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;主人公が処女
			IF CFLAG:253 == 1
				;恋慕 or 服従
				IF TALENT:恋慕 || TALENT:服従
					;PRINTFORMW
				;それ以外
				ELSE
					;PRINTFORMW
				ENDIF
			;主人公が非処女
			ELSE
				;恋慕 or 服従
				IF TALENT:恋慕 || TALENT:服従
					;PRINTFORMW
				;それ以外
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;アナルバイブ(プレイヤー・する側)
;-------------------------------------------------
IF SELECTCOM == 62
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;クリキャップ(プレイヤー・する側)
;-------------------------------------------------
IF SELECTCOM == 63
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;ニプルキャップ(プレイヤー・する側)
;-------------------------------------------------
IF SELECTCOM == 64
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;Ｖ拡張バルーン(プレイヤー・する側)
;-------------------------------------------------
IF SELECTCOM == 65
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;主人公が処女
			IF CFLAG:253 == 1
				;恋慕 or 服従
				IF TALENT:恋慕 || TALENT:服従
					;PRINTFORMW
				;それ以外
				ELSE
					;PRINTFORMW
				ENDIF
			;主人公が非処女
			ELSE
				;恋慕 or 服従
				IF TALENT:恋慕 || TALENT:服従
					;PRINTFORMW
				;それ以外
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;主人公が処女
			IF CFLAG:253 == 1
				;恋慕 or 服従
				IF TALENT:恋慕 || TALENT:服従
					;PRINTFORMW
				;それ以外
				ELSE
					;PRINTFORMW
				ENDIF
			;主人公が非処女
			ELSE
				;恋慕 or 服従
				IF TALENT:恋慕 || TALENT:服従
					;PRINTFORMW
				;それ以外
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;Ａ拡張バルーン(プレイヤー・する側)
;-------------------------------------------------
IF SELECTCOM == 66
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;ローション(プレイヤー・する側)
;-------------------------------------------------
IF SELECTCOM == 70
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;媚薬(プレイヤー・する側)
;-------------------------------------------------
IF SELECTCOM == 71
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;排卵誘発剤(プレイヤー・する側)
;-------------------------------------------------
IF SELECTCOM == 72
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;絶倫丸(プレイヤー・する側)
;-------------------------------------------------
IF SELECTCOM == 73
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;麻薬(プレイヤー・する側)
;-------------------------------------------------
IF SELECTCOM == 74
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;コンドーム(プレイヤー・する側)
;-------------------------------------------------
IF SELECTCOM == 75
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;桃源香(プレイヤー・する側)
;-------------------------------------------------
IF SELECTCOM == 76
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;脅す(プレイヤー・する側)
;-------------------------------------------------
IF SELECTCOM == 80
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;スパンキング(プレイヤー・する側)
;-------------------------------------------------
IF SELECTCOM == 81
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;鞭(プレイヤー・する側)
;-------------------------------------------------
IF SELECTCOM == 82
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;針(プレイヤー・する側)
;-------------------------------------------------
IF SELECTCOM == 83
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;目隠し(プレイヤー・する側)
;-------------------------------------------------
IF SELECTCOM == 84
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;縄(プレイヤー・する側)
;-------------------------------------------------
IF SELECTCOM == 85
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;口枷(プレイヤー・する側)
;-------------------------------------------------
IF SELECTCOM == 86
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;フィストファック(プレイヤー・する側)
;-------------------------------------------------
IF SELECTCOM == 87
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;主人公が処女
			IF CFLAG:253 == 1
				;恋慕 or 服従
				IF TALENT:恋慕 || TALENT:服従
					;PRINTFORMW
				;それ以外
				ELSE
					;PRINTFORMW
				ENDIF
			;主人公が非処女
			ELSE
				;恋慕 or 服従
				IF TALENT:恋慕 || TALENT:服従
					;PRINTFORMW
				;それ以外
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;主人公が処女
			IF CFLAG:253 == 1
				;恋慕 or 服従
				IF TALENT:恋慕 || TALENT:服従
					;PRINTFORMW
				;それ以外
				ELSE
					;PRINTFORMW
				ENDIF
			;主人公が非処女
			ELSE
				;恋慕 or 服従
				IF TALENT:恋慕 || TALENT:服従
					;PRINTFORMW
				;それ以外
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;アナルフィスト(プレイヤー・する側)
;-------------------------------------------------
IF SELECTCOM == 88
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;平手打ち(プレイヤー・する側)
;-------------------------------------------------
IF SELECTCOM == 89
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;イラマチオ(プレイヤー・しゃぶる側)
;-------------------------------------------------
IF SELECTCOM == 90
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				IF GETBIT(TALENT:淫乱系, 素質_淫乱_マゾ)
					;PRINTFORMW
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				IF GETBIT(TALENT:淫乱系, 素質_淫乱_マゾ)
					;PRINTFORMW
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				IF GETBIT(TALENT:淫乱系, 素質_淫乱_マゾ)
					;PRINTFORMW
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				IF GETBIT(TALENT:淫乱系, 素質_淫乱_マゾ)
					;PRINTFORMW
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;ピアシング(プレイヤー・する側)
;-------------------------------------------------
IF SELECTCOM == 91
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				IF GETBIT(TALENT:淫乱系, 素質_淫乱_マゾ)
					;PRINTFORMW
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				IF GETBIT(TALENT:淫乱系, 素質_淫乱_マゾ)
					;PRINTFORMW
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				IF GETBIT(TALENT:淫乱系, 素質_淫乱_マゾ)
					;PRINTFORMW
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				IF GETBIT(TALENT:淫乱系, 素質_淫乱_マゾ)
					;PRINTFORMW
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;タトゥー(プレイヤー・する側)
;-------------------------------------------------
IF SELECTCOM == 92
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				IF GETBIT(TALENT:淫乱系, 素質_淫乱_マゾ)
					;PRINTFORMW
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				IF GETBIT(TALENT:淫乱系, 素質_淫乱_マゾ)
					;PRINTFORMW
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				IF GETBIT(TALENT:淫乱系, 素質_淫乱_マゾ)
					;PRINTFORMW
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				IF GETBIT(TALENT:淫乱系, 素質_淫乱_マゾ)
					;PRINTFORMW
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;鼻フック(プレイヤー・する側)
;-------------------------------------------------
IF SELECTCOM == 93
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;首絞め(プレイヤー・する側)
;-------------------------------------------------
IF SELECTCOM == 94
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;腹パン(プレイヤー・する側)
;-------------------------------------------------
IF SELECTCOM == 97
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;遠隔調教(プレイヤー・する側)
;-------------------------------------------------
IF SELECTCOM == 98
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				IF GETBIT(TALENT:淫乱系, 素質_淫乱_淫乱)
					;PRINTFORMW
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				IF GETBIT(TALENT:淫乱系, 素質_淫乱_淫乱)
					;PRINTFORMW
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				IF GETBIT(TALENT:淫乱系, 素質_淫乱_淫乱)
					;PRINTFORMW
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				IF GETBIT(TALENT:淫乱系, 素質_淫乱_淫乱)
					;PRINTFORMW
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;自慰(プレイヤー・する側)
;-------------------------------------------------
IF SELECTCOM == 100
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;自慰Lv2以上
				IF ABL:自慰 >= 2
					;PRINTFORMW
				;自慰Lv2未満
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;自慰Lv2以上
				IF ABL:自慰 >= 2
					;PRINTFORMW
				;自慰Lv2未満
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;自慰Lv2以上
				IF ABL:自慰 >= 2
					;PRINTFORMW
				;自慰Lv2未満
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;自慰Lv2以上
				IF ABL:自慰 >= 2
					;PRINTFORMW
				;自慰Lv2未満
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;顔面騎乗(プレイヤー・する側)
;-------------------------------------------------
IF SELECTCOM == 101
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;Ｃ感Lv3以上
				IF ABL:Ｃ感 >= 3
					;PRINTFORMW
				;Ｃ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;Ｃ感Lv3以上
				IF ABL:Ｃ感 >= 3
					;PRINTFORMW
				;Ｃ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;Ｃ感Lv3以上
				IF ABL:Ｃ感 >= 3
					;PRINTFORMW
				;Ｃ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;Ｃ感Lv3以上
				IF ABL:Ｃ感 >= 3
					;PRINTFORMW
				;Ｃ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;Ａ顔面騎乗(プレイヤー・する側)
;-------------------------------------------------
IF SELECTCOM == 102
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;Ａ感Lv3以上
				IF ABL:Ａ感 >= 3
					;PRINTFORMW
				;Ａ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;Ａ感Lv3以上
				IF ABL:Ａ感 >= 3
					;PRINTFORMW
				;Ａ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;Ａ感Lv3以上
				IF ABL:Ａ感 >= 3
					;PRINTFORMW
				;Ａ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;Ａ感Lv3以上
				IF ABL:Ａ感 >= 3
					;PRINTFORMW
				;Ａ感Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;電気按摩(プレイヤー・する側)
;-------------------------------------------------
IF SELECTCOM == 103
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;足を舐める(プレイヤー・する側)
;-------------------------------------------------
IF SELECTCOM == 104
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;亀頭責め手コキ(プレイヤー・する側)
;-------------------------------------------------
IF SELECTCOM == 106
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;亀頭責めフェラ(プレイヤー・しゃぶる側)
;-------------------------------------------------
IF SELECTCOM == 107
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;尻尾セルフ挿入(プレイヤー・する側)
;-------------------------------------------------
IF SELECTCOM == 108
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;自慰Lv2以上
				IF ABL:自慰 >= 2
					;PRINTFORMW
				;自慰Lv2未満
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;自慰Lv2以上
				IF ABL:自慰 >= 2
					;PRINTFORMW
				;自慰Lv2未満
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;自慰Lv2以上
				IF ABL:自慰 >= 2
					;PRINTFORMW
				;自慰Lv2未満
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;自慰Lv2以上
				IF ABL:自慰 >= 2
					;PRINTFORMW
				;自慰Lv2未満
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;尻尾セルフＡ挿入(プレイヤー・する側)
;-------------------------------------------------
IF SELECTCOM == 109
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;自慰Lv2以上
				IF ABL:自慰 >= 2
					;PRINTFORMW
				;自慰Lv2未満
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;自慰Lv2以上
				IF ABL:自慰 >= 2
					;PRINTFORMW
				;自慰Lv2未満
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;自慰Lv2以上
				IF ABL:自慰 >= 2
					;PRINTFORMW
				;自慰Lv2未満
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;自慰Lv2以上
				IF ABL:自慰 >= 2
					;PRINTFORMW
				;自慰Lv2未満
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;土下座(プレイヤー・する側)
;-------------------------------------------------
IF SELECTCOM == 110
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;おねだり(プレイヤー・する側)
;-------------------------------------------------
IF SELECTCOM == 111
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;露出プレイ(プレイヤー・する側)
;-------------------------------------------------
IF SELECTCOM == 112
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;陰毛管理(プレイヤー・する側)
;-------------------------------------------------
IF SELECTCOM == 113
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;身体に落書き(プレイヤー・する側)
;-------------------------------------------------
IF SELECTCOM == 114
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;放尿する(プレイヤー・する側)
;-------------------------------------------------
IF SELECTCOM == 130
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;排泄Lv2以上
				IF ABL:排泄 >= 2
					;PRINTFORMW
				;排泄Lv2未満
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;排泄Lv2以上
				IF ABL:排泄 >= 2
					;PRINTFORMW
				;排泄Lv2未満
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;排泄Lv2以上
				IF ABL:排泄 >= 2
					;PRINTFORMW
				;排泄Lv2未満
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;排泄Lv2以上
				IF ABL:排泄 >= 2
					;PRINTFORMW
				;排泄Lv2未満
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;尿を掛ける(プレイヤー・する側)
;-------------------------------------------------
IF SELECTCOM == 131
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;排泄Lv2以上
				IF ABL:排泄 >= 2
					;PRINTFORMW
				;排泄Lv2未満
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;排泄Lv2以上
				IF ABL:排泄 >= 2
					;PRINTFORMW
				;排泄Lv2未満
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;排泄Lv2以上
				IF ABL:排泄 >= 2
					;PRINTFORMW
				;排泄Lv2未満
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;排泄Lv2以上
				IF ABL:排泄 >= 2
					;PRINTFORMW
				;排泄Lv2未満
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;口内放尿(プレイヤー・する側)
;-------------------------------------------------
IF SELECTCOM == 132
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;排泄Lv2以上
				IF ABL:排泄 >= 2
					;PRINTFORMW
				;排泄Lv2未満
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;排泄Lv2以上
				IF ABL:排泄 >= 2
					;PRINTFORMW
				;排泄Lv2未満
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;排泄Lv2以上
				IF ABL:排泄 >= 2
					;PRINTFORMW
				;排泄Lv2未満
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;排泄Lv2以上
				IF ABL:排泄 >= 2
					;PRINTFORMW
				;排泄Lv2未満
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;利尿剤(プレイヤー・する側)
;-------------------------------------------------
IF SELECTCOM == 140
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;浣腸(プレイヤー・する側)
;-------------------------------------------------
IF SELECTCOM == 141
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;浣腸＆プラグ(プレイヤー・する側)
;-------------------------------------------------
IF SELECTCOM == 142
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;放屁する(プレイヤー・する側)
;-------------------------------------------------
IF SELECTCOM == 150
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;排泄Lv2以上
				IF ABL:排泄 >= 2
					;PRINTFORMW
				;排泄Lv2未満
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;排泄Lv2以上
				IF ABL:排泄 >= 2
					;PRINTFORMW
				;排泄Lv2未満
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;排泄Lv2以上
				IF ABL:排泄 >= 2
					;PRINTFORMW
				;排泄Lv2未満
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;排泄Lv2以上
				IF ABL:排泄 >= 2
					;PRINTFORMW
				;排泄Lv2未満
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;顔騎放屁(プレイヤー・する側)
;-------------------------------------------------
IF SELECTCOM == 151
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;排泄Lv2以上
				IF ABL:排泄 >= 2
					;PRINTFORMW
				;排泄Lv2未満
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;排泄Lv2以上
				IF ABL:排泄 >= 2
					;PRINTFORMW
				;排泄Lv2未満
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;排泄Lv2以上
				IF ABL:排泄 >= 2
					;PRINTFORMW
				;排泄Lv2未満
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;排泄Lv2以上
				IF ABL:排泄 >= 2
					;PRINTFORMW
				;排泄Lv2未満
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;触手召喚(プレイヤー・する側)
;-------------------------------------------------
IF SELECTCOM == 200
	;初めて(主導権を考慮しない)
	IF IS_COM_FIRST(1)
		;恋慕 or 服従
		IF TALENT:恋慕 || TALENT:服従
			;PRINTFORMW
		;それ以外
		ELSE
			;PRINTFORMW
		ENDIF
	;二回目以降
	ELSE
		;恋慕 or 服従
		IF TALENT:恋慕 || TALENT:服従
			;PRINTFORMW
		;それ以外
		ELSE
			;PRINTFORMW
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;触手挿入(プレイヤー・する側)
;-------------------------------------------------
IF SELECTCOM == 201
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;主人公が処女
			IF CFLAG:253 == 1
				;恋慕 or 服従
				IF TALENT:恋慕 || TALENT:服従
					;PRINTFORMW
				;それ以外
				ELSE
					;PRINTFORMW
				ENDIF
			;主人公が非処女
			ELSE
				;恋慕 or 服従
				IF TALENT:恋慕 || TALENT:服従
					;PRINTFORMW
				;それ以外
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;主人公が処女
			IF CFLAG:253 == 1
				;恋慕 or 服従
				IF TALENT:恋慕 || TALENT:服従
					;PRINTFORMW
				;それ以外
				ELSE
					;PRINTFORMW
				ENDIF
			;主人公が非処女
			ELSE
				;恋慕 or 服従
				IF TALENT:恋慕 || TALENT:服従
					;PRINTFORMW
				;それ以外
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;触手Ａ挿入(プレイヤー・する側)
;-------------------------------------------------
IF SELECTCOM == 202
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;触手クリ責め(プレイヤー・する側)
;-------------------------------------------------
IF SELECTCOM == 203
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;触手胸愛撫(プレイヤー・する側)
;-------------------------------------------------
IF SELECTCOM == 204
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;触手オナホ(プレイヤー・する側)
;-------------------------------------------------
IF SELECTCOM == 205
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;触手コキ(プレイヤー・する側)
;-------------------------------------------------
IF SELECTCOM == 206
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;触手搾乳(プレイヤー・する側)
;-------------------------------------------------
IF SELECTCOM == 207
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;触手緊縛(プレイヤー・する側)
;-------------------------------------------------
IF SELECTCOM == 208
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;触手口辱(プレイヤー・する側)
;-------------------------------------------------
IF SELECTCOM == 210
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;触手オナニー(プレイヤー・する側)
;-------------------------------------------------
IF SELECTCOM == 211
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;触手Lv3以上
				IF ABL:触手 >= 3
					;PRINTFORMW
				;触手Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;触手Lv3以上
				IF ABL:触手 >= 3
					;PRINTFORMW
				;触手Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;触手Lv3以上
				IF ABL:触手 >= 3
					;PRINTFORMW
				;触手Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			;それ以外
			ELSE
				;触手Lv3以上
				IF ABL:触手 >= 3
					;PRINTFORMW
				;触手Lv3未満
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;触手産卵(プレイヤー・する側)
;-------------------------------------------------
IF SELECTCOM == 212
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;触手Ａ産卵(プレイヤー・する側)
;-------------------------------------------------
IF SELECTCOM == 213
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;おまかせする
;-------------------------------------------------
IF SELECTCOM == 291
	;初めて(主導権を考慮しない)
	IF IS_COM_FIRST(1)
		;恋慕 or 服従
		IF TALENT:恋慕 || TALENT:服従
			;PRINTFORMW
		;それ以外
		ELSE
			;PRINTFORMW
		ENDIF
	;二回目以降
	ELSE
		;恋慕 or 服従
		IF TALENT:恋慕 || TALENT:服従
			;PRINTFORMW
		;それ以外
		ELSE
			;PRINTFORMW
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;スキンシップ(プレイヤー・する側)　キャラがあなたに対して行うスキンシップ
;-------------------------------------------------
IF SELECTCOM == 320
	;温泉なら裸のスキンシップ
	IF TFLAG:54 == 7
		;このキャラが主導権を持っている
		IF IS_INITIATIVE(TARGET)
			;初めて
			IF IS_COM_FIRST()
				;既成事実Lv2以上
				IF TALENT:恋人 || TALENT:合意
					;PRINTFORMW
				ELSE
					;PRINTFORMW
				ENDIF
			;二回目以降
			ELSE
				;恋慕 or 服従 or 親友
				IF TALENT:恋慕 || TALENT:服従 || TALENT:親友
					;PRINTFORMW
				;既成事実Lv2以上
				ELSEIF TALENT:恋人 || TALENT:合意
					;PRINTFORMW
				;それ以外
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		;このキャラが主導権を持っていない
		ELSE
			;初めて
			IF IS_COM_FIRST()
				;既成事実Lv2以上
				IF TALENT:恋人 || TALENT:合意
					;PRINTFORMW
				ELSE
					;PRINTFORMW
				ENDIF
			;二回目以降
			ELSE
				;恋慕 or 服従 or 親友
				IF TALENT:恋慕 || TALENT:服従 || TALENT:親友
					;PRINTFORMW
				;既成事実Lv2以上
				ELSEIF TALENT:恋人 || TALENT:合意
					;PRINTFORMW
				;それ以外
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	ELSE
		;このキャラが主導権を持っている
		IF IS_INITIATIVE(TARGET)
			;初めて
			IF IS_COM_FIRST()
				;既成事実Lv2以上
				IF TALENT:恋人 || TALENT:合意
					;PRINTFORMW
				ELSE
					;PRINTFORMW
				ENDIF
			;二回目以降
			ELSE
				;恋慕 or 服従 or 親友
				IF TALENT:恋慕 || TALENT:服従 || TALENT:親友
					;PRINTFORMW
				;既成事実Lv2以上
				ELSEIF TALENT:恋人 || TALENT:合意
					;PRINTFORMW
				;それ以外
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		;このキャラが主導権を持っていない
		ELSE
			;初めて
			IF IS_COM_FIRST()
				;既成事実Lv2以上
				IF TALENT:恋人 || TALENT:合意
					;PRINTFORMW
				ELSE
					;PRINTFORMW
				ENDIF
			;二回目以降
			ELSE
				;恋慕 or 服従 or 親友
				IF TALENT:恋慕 || TALENT:服従 || TALENT:親友
					;PRINTFORMW
				;既成事実Lv2以上
				ELSEIF TALENT:恋人 || TALENT:合意
					;PRINTFORMW
				;それ以外
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;頭を撫でる(プレイヤー・する側)
;-------------------------------------------------
IF SELECTCOM == 321
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;既成事実Lv2以上
			IF TALENT:恋人 || TALENT:合意
				;PRINTFORMW
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従 or 親友
			IF TALENT:恋慕 || TALENT:服従 || TALENT:親友
				;PRINTFORMW
			;既成事実Lv2以上
			ELSEIF TALENT:恋人 || TALENT:合意
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;既成事実Lv2以上
			IF TALENT:恋人 || TALENT:合意
				;PRINTFORMW
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従 or 親友
			IF TALENT:恋慕 || TALENT:服従 || TALENT:親友
				;PRINTFORMW
			;既成事実Lv2以上
			ELSEIF TALENT:恋人 || TALENT:合意
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;髪を梳く(プレイヤー・する側)
;-------------------------------------------------
IF SELECTCOM == 322
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;既成事実Lv2以上
			IF TALENT:恋人 || TALENT:合意
				;PRINTFORMW
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従 or 親友
			IF TALENT:恋慕 || TALENT:服従 || TALENT:親友
				;PRINTFORMW
			;既成事実Lv2以上
			ELSEIF TALENT:恋人 || TALENT:合意
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;既成事実Lv2以上
			IF TALENT:恋人 || TALENT:合意
				;PRINTFORMW
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従 or 親友
			IF TALENT:恋慕 || TALENT:服従 || TALENT:親友
				;PRINTFORMW
			;既成事実Lv2以上
			ELSEIF TALENT:恋人 || TALENT:合意
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;櫛で梳かす(プレイヤー・する側)
;-------------------------------------------------
IF SELECTCOM == 323
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;既成事実Lv2以上
			IF TALENT:恋人 || TALENT:合意
				;PRINTFORMW
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従 or 親友
			IF TALENT:恋慕 || TALENT:服従 || TALENT:親友
				;PRINTFORMW
			;既成事実Lv2以上
			ELSEIF TALENT:恋人 || TALENT:合意
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;既成事実Lv2以上
			IF TALENT:恋人 || TALENT:合意
				;PRINTFORMW
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従 or 親友
			IF TALENT:恋慕 || TALENT:服従 || TALENT:親友
				;PRINTFORMW
			;既成事実Lv2以上
			ELSEIF TALENT:恋人 || TALENT:合意
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;甘い言葉(プレイヤー・する側)
;-------------------------------------------------
IF SELECTCOM == 324
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;既成事実Lv2以上
			IF TALENT:恋人 || TALENT:合意
				;PRINTFORMW
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従 or 親友
			IF TALENT:恋慕 || TALENT:服従 || TALENT:親友
				;PRINTFORMW
			;既成事実Lv2以上
			ELSEIF TALENT:恋人 || TALENT:合意
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;既成事実Lv2以上
			IF TALENT:恋人 || TALENT:合意
				;PRINTFORMW
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従 or 親友
			IF TALENT:恋慕 || TALENT:服従 || TALENT:親友
				;PRINTFORMW
			;既成事実Lv2以上
			ELSEIF TALENT:恋人 || TALENT:合意
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;日常フェラ(プレイヤー・しゃぶる側)
;-------------------------------------------------
IF SELECTCOM == 330
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;既成事実Lv2以上
			IF TALENT:恋人 || TALENT:合意
				;PRINTFORMW
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従 or 親友
			IF TALENT:恋慕 || TALENT:服従 || TALENT:親友
				;PRINTFORMW
			;既成事実Lv2以上
			ELSEIF TALENT:恋人 || TALENT:合意
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;既成事実Lv2以上
			IF TALENT:恋人 || TALENT:合意
				;PRINTFORMW
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従 or 親友
			IF TALENT:恋慕 || TALENT:服従 || TALENT:親友
				;PRINTFORMW
			;既成事実Lv2以上
			ELSEIF TALENT:恋人 || TALENT:合意
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;バードキス(プレイヤー・する側)
;-------------------------------------------------
IF SELECTCOM == 340
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			CFLAG:250 = 1;初めてキスをしたフラグ
			;既成事実Lv2以上
			IF TALENT:恋人 || TALENT:合意
				;ファーストキス
				IF CFLAG:251 == 1
					;PRINTFORMW
				;キス経験済み
				ELSE
					;PRINTFORMW
				ENDIF
			ELSE
				;ファーストキス
				IF CFLAG:251 == 1
					;PRINTFORMW
				;キス経験済み
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従 or 親友
			IF TALENT:恋慕 || TALENT:服従 || TALENT:親友
				;PRINTFORMW
			;既成事実Lv2以上
			ELSEIF TALENT:恋人 || TALENT:合意
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			CFLAG:250 = 1;初めてキスをしたフラグ
			;既成事実Lv2以上
			IF TALENT:恋人 || TALENT:合意
				;ファーストキス
				IF CFLAG:251 == 1
					;PRINTFORMW
				;キス経験済み
				ELSE
					;PRINTFORMW
				ENDIF
			ELSE
				;ファーストキス
				IF CFLAG:251 == 1
					;PRINTFORMW
				;キス経験済み
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従 or 親友
			IF TALENT:恋慕 || TALENT:服従 || TALENT:親友
				;PRINTFORMW
			;既成事実Lv2以上
			ELSEIF TALENT:恋人 || TALENT:合意
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;ソフトキス(プレイヤー・する側)
;-------------------------------------------------
IF SELECTCOM == 341
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			CFLAG:250 = 1;初めてキスをしたフラグ
			;既成事実Lv2以上
			IF TALENT:恋人 || TALENT:合意
				;ファーストキス
				IF CFLAG:251 == 1
					;PRINTFORMW
				;キス経験済み
				ELSE
					;PRINTFORMW
				ENDIF
			ELSE
				;ファーストキス
				IF CFLAG:251 == 1
					;PRINTFORMW
				;キス経験済み
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従 or 親友
			IF TALENT:恋慕 || TALENT:服従 || TALENT:親友
				;PRINTFORMW
			;既成事実Lv2以上
			ELSEIF TALENT:恋人 || TALENT:合意
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			CFLAG:250 = 1;初めてキスをしたフラグ
			;既成事実Lv2以上
			IF TALENT:恋人 || TALENT:合意
				;ファーストキス
				IF CFLAG:251 == 1
					;PRINTFORMW
				;キス経験済み
				ELSE
					;PRINTFORMW
				ENDIF
			ELSE
				;ファーストキス
				IF CFLAG:251 == 1
					;PRINTFORMW
				;キス経験済み
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従 or 親友
			IF TALENT:恋慕 || TALENT:服従 || TALENT:親友
				;PRINTFORMW
			;既成事実Lv2以上
			ELSEIF TALENT:恋人 || TALENT:合意
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;ディープキス(プレイヤー・する側)
;-------------------------------------------------
IF SELECTCOM == 342
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			CFLAG:250 = 1;初めてキスをしたフラグ
			;既成事実Lv2以上
			IF TALENT:恋人 || TALENT:合意
				;ファーストキス
				IF CFLAG:251 == 1
					;PRINTFORMW
				;キス経験済み
				ELSE
					;PRINTFORMW
				ENDIF
			ELSE
				;ファーストキス
				IF CFLAG:251 == 1
					;PRINTFORMW
				;キス経験済み
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従 or 親友
			IF TALENT:恋慕 || TALENT:服従 || TALENT:親友
				;PRINTFORMW
			;既成事実Lv2以上
			ELSEIF TALENT:恋人 || TALENT:合意
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			CFLAG:250 = 1;初めてキスをしたフラグ
			;既成事実Lv2以上
			IF TALENT:恋人 || TALENT:合意
				;ファーストキス
				IF CFLAG:251 == 1
					;PRINTFORMW
				;キス経験済み
				ELSE
					;PRINTFORMW
				ENDIF
			ELSE
				;ファーストキス
				IF CFLAG:251 == 1
					;PRINTFORMW
				;キス経験済み
				ELSE
					;PRINTFORMW
				ENDIF
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従 or 親友
			IF TALENT:恋慕 || TALENT:服従 || TALENT:親友
				;PRINTFORMW
			;既成事実Lv2以上
			ELSEIF TALENT:恋人 || TALENT:合意
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;花冠を作る(プレイヤー・する側)
;-------------------------------------------------
IF SELECTCOM == 356
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;既成事実Lv2以上
			IF TALENT:恋人 || TALENT:合意
				;PRINTFORMW
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従 or 親友
			IF TALENT:恋慕 || TALENT:服従 || TALENT:親友
				;PRINTFORMW
			;既成事実Lv2以上
			ELSEIF TALENT:恋人 || TALENT:合意
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;既成事実Lv2以上
			IF TALENT:恋人 || TALENT:合意
				;PRINTFORMW
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従 or 親友
			IF TALENT:恋慕 || TALENT:服従 || TALENT:親友
				;PRINTFORMW
			;既成事実Lv2以上
			ELSEIF TALENT:恋人 || TALENT:合意
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;背中を流す(プレイヤー・する側)
;-------------------------------------------------
IF SELECTCOM == 358
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;初めて
		IF IS_COM_FIRST()
			;既成事実Lv2以上
			IF TALENT:恋人 || TALENT:合意
				;PRINTFORMW
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従 or 親友
			IF TALENT:恋慕 || TALENT:服従 || TALENT:親友
				;PRINTFORMW
			;既成事実Lv2以上
			ELSEIF TALENT:恋人 || TALENT:合意
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;初めて
		IF IS_COM_FIRST()
			;既成事実Lv2以上
			IF TALENT:恋人 || TALENT:合意
				;PRINTFORMW
			ELSE
				;PRINTFORMW
			ENDIF
		;二回目以降
		ELSE
			;恋慕 or 服従 or 親友
			IF TALENT:恋慕 || TALENT:服従 || TALENT:親友
				;PRINTFORMW
			;既成事実Lv2以上
			ELSEIF TALENT:恋人 || TALENT:合意
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;なりゆきまかせ
;-------------------------------------------------
IF SELECTCOM == 393
	;初めて(主導権を考慮しない)
	IF IS_COM_FIRST(1)
		;既成事実Lv2以上
		IF TALENT:恋人 || TALENT:合意
			;PRINTFORMW
		ELSE
			;PRINTFORMW
		ENDIF
	;二回目以降
	ELSE
		;恋慕 or 服従 or 親友
		IF TALENT:恋慕 || TALENT:服従 || TALENT:親友
			;PRINTFORMW
		;既成事実Lv2以上
		ELSEIF TALENT:恋人 || TALENT:合意
			;PRINTFORMW
		;それ以外
		ELSE
			;PRINTFORMW
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;性の知識を教える(プレイヤー・する側)
;-------------------------------------------------
IF SELECTCOM == 550
	;このキャラが主導権を持っている
	IF IS_INITIATIVE(TARGET)
		;恋慕 or 服従 or 親友
		IF TALENT:恋慕 || TALENT:服従 || TALENT:親友
			;PRINTFORMW
		;それ以外
		ELSE
			;PRINTFORMW
		ENDIF
	;このキャラが主導権を持っていない
	ELSE
		;恋慕 or 服従 or 親友
		IF TALENT:恋慕 || TALENT:服従 || TALENT:親友
			;PRINTFORMW
		;それ以外
		ELSE
			;PRINTFORMW
		ENDIF
	ENDIF
ENDIF

;=================================================
;●コマンド実行後(パラメータの変動処理が終わってから呼び出される)
;=================================================
@KOJO_COM_AFTER_A_K145
;[空虚]状態の場合、口が塞がっている場合は口上を表示しない
IF TALENT:空虚 || IS_EQUIP_CONTINUE(TARGET, "口装着")
	RETURN 0
ENDIF

;ファーストキスフラグ（コマンド終了時に無接吻経験じゃ無かったらCFLAG:251を0にセット）
SIF !TALENT:無接吻経験
	CFLAG:251 = 0
;処女フラグ（コマンド終了時に処女じゃ無かったらCFLAG:252を0にセット）
SIF !TALENT:処女
	CFLAG:252 = 0
;主人公が処女フラグ（コマンド終了時に主人公が処女じゃ無かったらCFLAG:253を0にセット）
SIF !TALENT:MASTER:処女
	CFLAG:253 = 0
;主人公が童貞フラグ（コマンド終了時に主人公が童貞じゃ無かったらCFLAG:254を0にセット）
SIF !TALENT:MASTER:童貞
	CFLAG:254 = 0

;-------------------------------------------------
;処女喪失
;-------------------------------------------------
;主人公とのセックスによる処女喪失
IF TCVAR:13 == 1
	;恋慕 or 服従
	IF TALENT:恋慕 || TALENT:服従
		;PRINTFORMW
	;それ以外
	ELSE
		;PRINTFORMW
	ENDIF
;主人公にセックス以外で処女を奪われる
ELSEIF TCVAR:13 == 2
	;恋慕 or 服従
	IF TALENT:恋慕 || TALENT:服従
		;PRINTFORMW
	;それ以外
	ELSE
		;PRINTFORMW
	ENDIF
;それ以外の時に処女喪失
ELSEIF TCVAR:13 == 3 || TCVAR:13 == 4
	;恋慕 or 服従
	IF TALENT:恋慕 || TALENT:服従
		;PRINTFORMW
	;それ以外
	ELSE
		;PRINTFORMW
	ENDIF
ENDIF

;-------------------------------------------------
;主人公を射精させた
;-------------------------------------------------
IF NOWEX:MASTER:射精 > 0
	SELECTCASE GET_STACK_SPERM_TO(MASTER, TARGET)
		;膣に射精
		CASE 1
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;アナルに射精
		CASE 2
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;手に射精
		CASE 3
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;口に射精
		CASE 4
			;恋慕 or 服従 かつ精愛Lv2以上
			IF (TALENT:恋慕 || TALENT:服従) && ABL:精愛 >= 2
				;PRINTFORMW
			;恋慕 or 服従
			ELSEIF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;胸に射精
		CASE 5
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;足に射精
		CASE 6
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;尻尾に射精
		CASE 7
		;顔に射精
		CASE 8
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;髪に射精
		CASE 9
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;腋に射精
		CASE 10
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
		;その他の場所に射精
		CASEELSE
			;恋慕 or 服従
			IF TALENT:恋慕 || TALENT:服従
				;PRINTFORMW
			;それ以外
			ELSE
				;PRINTFORMW
			ENDIF
	ENDSELECT

;-------------------------------------------------
;主人公を射精させずに絶頂させた
;-------------------------------------------------
ELSEIF NOWEX:MASTER:Ｃ絶頂 + NOWEX:MASTER:Ｖ絶頂 + NOWEX:MASTER:Ａ絶頂 + NOWEX:MASTER:Ｂ絶頂 + NOWEX:MASTER:Ｍ絶頂 > 0
	;恋慕 or 服従
	IF TALENT:恋慕 || TALENT:服従
		;PRINTFORMW
	;それ以外
	ELSE
		;PRINTFORMW
	ENDIF
ENDIF

;-------------------------------------------------
;おもらしした
;-------------------------------------------------
SELECTCASE TCVAR:0
	;尿意がある状態で絶頂強度の合計が4以上
	CASE 1
		;PRINTFORMW
	;尿意が高い状態で絶頂
	CASE 2
		;PRINTFORMW
	;尿意の限界
	CASE 3
		;PRINTFORMW
	;恐怖による失禁
	CASE 4
		;PRINTFORMW
ENDSELECT

