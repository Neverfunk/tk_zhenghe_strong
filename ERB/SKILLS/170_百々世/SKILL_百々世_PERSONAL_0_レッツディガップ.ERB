;-----------------------------------
;基本値計算に先んじて処理するもの
;-----------------------------------
@SKILL_170_PERSONAL_0_EXIST
RETURN 1

@SKILL_170_PERSONAL_0_NAME
RESULTS = Let's dig up！
;TranslatedMark レッツディガップ

;レベルは1-5まで
@SKILL_170_PERSONAL_0_LEVEL
RETURN 3

;対象選択
@SKILL_170_PERSONAL_0_SETTARGET(発動者, 発動番号, 発動側, 発動勢力, 発動部隊, 対象勢力, 対象部隊)
#DIM 発動者
#DIM 発動番号
#DIM スキル
#DIMS ジャンル
#DIM 発動側
#DIM 発動勢力
#DIM 発動部隊
#DIM 対象側
#DIM 対象勢力
#DIM 対象部隊
#DIM 能力, 3
VARSET 能力

COMBAT_SKILL_TARGET = 発動番号
RETURN 1

;発動判定
@SKILL_170_PERSONAL_0_CAN_INVOKE(発動者, 発動番号, 発動側, 発動勢力, 発動部隊, 対象勢力, 対象部隊)
#DIM 発動者
#DIM 発動番号
#DIM スキル
#DIMS ジャンル
#DIM 発動側
#DIM 発動勢力
#DIM 発動部隊
#DIM 対象側
#DIM 対象勢力
#DIM 対象部隊
対象側 = !発動側
RETURN 1

;効果をここに記述
@SKILL_170_PERSONAL_0_INVOKE(発動者, 発動番号, 発動側, 発動勢力, 発動部隊, 対象勢力, 対象部隊)
#DIM 発動者
#DIM 発動番号
#DIM スキル
#DIMS ジャンル
#DIM 発動側
#DIM 発動勢力
#DIM 発動部隊
#DIM 対象側
#DIM 対象勢力
#DIM 対象部隊
対象側 = !発動側
PRINTFORML %ANAME(発動者)%的武闘・料理増加、防衛・知略下降了！
;TranslatedMark %ANAME(発動者)%の武闘・料理が増加、防衛・知略が低下した！
TIMES BATTLE_武闘パワー:発動側:COMBAT_SKILL_TARGET, 1.3
TIMES BATTLE_料理パワー:発動側:COMBAT_SKILL_TARGET, 1.3
TIMES BATTLE_知略パワー:発動側:COMBAT_SKILL_TARGET, 0.7
TIMES BATTLE_防衛パワー:発動側:COMBAT_SKILL_TARGET, 0.7
RETURN 1

;
@SKILL_170_PERSONAL_0_EXPLANATION
RESULTS = 使自身的武闘・料理增加、防衛・知略下降。
;TranslatedMark 自身の武闘・料理を増加させ、防衛・知略を低下させる。




@SKILL_170_PERSONAL_0_RATE(発動者, 発動番号, 発動側, 発動勢力, 発動部隊, 対象勢力, 対象部隊)
#DIM 発動者
#DIM 発動番号
#DIM スキル
#DIMS ジャンル
#DIM 発動側
#DIM 発動勢力
#DIM 発動部隊
#DIM 対象側
#DIM 対象勢力
#DIM 対象部隊
RETURN 200
