@UPDATE_CSV_CHARA_RESOLUTION_CONFLICT(CSVNO)
	#DIM CSVNO
	#DIM NEW_CHARA_ID
	#DIM DYNAMIC 角色已经存在
	CALL ADD_CHARA_FROM_CSV(CSVNO)
	NEW_CHARA_ID = RESULT
	FOR LOCAL,1,CHARANUM
		IF CSTR:NEW_CHARA_ID:99 == CSTR:LOCAL:99
			NO:LOCAL = NO:NEW_CHARA_ID
			角色已经存在 = 1
		ENDIF
	NEXT
	SIF 角色已经存在
		DELCHARA NEW_CHARA_ID
; @UPDATE_CSV_CHARA_CONFLICT_RESOLUTE(OLD_CHARA_ID,NEW_CHARA_ID)
; 	#DIM OLD_CHARA_ID
; 	#DIM NEW_CHARA_ID
; 	PRINTFORMW 角色%ANAME(OLD_CHARA_ID)%已经有一个存在了
; 	PRINTFORMW

@自动
