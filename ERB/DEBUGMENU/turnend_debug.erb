@TURN_END_DEBUG
    TRYCALL DEBUG1()

@DEBUG1
    FOR LOCAL,0,CHARANUM
        IF !INRANGE(CFLAG:LOCAL:所属,0,MAX_COUNTRY)
            SETCOLOR カラー_警告
            PRINTFORML 角色%ANAME(LOCAL)%,id:{LOCAL}的“CFLAG:所属”值:{CFLAG:LOCAL:所属} 错误的超出了范围
            PRINTFORML 现在我们将其设置为了0，继续游戏将不会出错
            PRINTFORMW 如果你收到此消息，希望你能向制作者反馈这一消息并提供最近的存档，以便我们寻找此bug的来源
            RESETCOLOR
            FORCEWAIT
            CFLAG:LOCAL:所属 = 0
        ENDIF
        IF !INRANGE(CFLAG:LOCAL:捕虜先,0,MAX_COUNTRY)
            SETCOLOR カラー_警告
            PRINTFORML 角色%ANAME(LOCAL)%,id:{LOCAL}的“CFLAG:捕虜先”值:{CFLAG:LOCAL:捕虜先} 错误的超出了范围
            PRINTFORML 现在我们将其设置为了0，继续游戏将不会出错
            PRINTFORMW 如果你收到此消息，希望你能向制作者反馈这一消息并提供最近的存档，以便我们寻找此bug的来源
            RESETCOLOR
            FORCEWAIT
            CFLAG:LOCAL:捕虜先 = 0
        ENDIF
    NEXT
