;武術を教える

;-------------------------------------------------
;コマンド名称
;-------------------------------------------------
@COM_NAME500
IF LOCAL:0 >= 0 && CFLAG:(MTAR:0):子の成長度 <= 2
	RESULTS = 外出遊玩
ELSE
	RESULTS = 教導武術
ENDIF

;-------------------------------------------------
;選択可否判定
;-------------------------------------------------
@COM_ABLE500
;共通部分
CALL COM_ABLE_COMMON(500)
SIF RESULT == 0
	RETURN 0
;主人公がプレイヤーのとき限定
SIF MPLY:0 != MASTER
	RETURN 0
RETURN 1

;-------------------------------------------------
;メイン処理
;-------------------------------------------------
@COM500
;外で遊ぶ
CALL COM_SLG_TRAIN_MAIN_PROCESS(GETNUM(ABL, "武闘"), 1)

RETURN 1

;-------------------------------------------------
;地の文(前文)
;-------------------------------------------------
@COM_TEXT_BEFORE500
;外で遊ぶ
IF LOCAL:0 >= 0 && CFLAG:(MTAR:0):子の成長度 <= 2
	PRINTFORMW %ANAME(MPLY:0)%和%ANAME(MTAR:0)%一起在山野里奔跑遊戲

;武術を教える
ELSE
	PRINTFORMW %ANAME(MPLY:0)%対%ANAME(MTAR:0)%進行了武術特訓
ENDIF

;--------------------------------------------------------
;地の文(パラメータ・刻印変動後)
;--------------------------------------------------------
@COM_TEXT_LAST500

;-------------------------------------------------

;コマンド区分
;-------------------------------------------------

@COM_AVAILABLE_WHEN500
RETURN コマンド_育児
