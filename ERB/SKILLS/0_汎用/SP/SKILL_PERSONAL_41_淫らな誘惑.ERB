;-----------------------------------
;基本値計算に先んじて処理するもの
;-----------------------------------
@SKILL_0_PERSONAL_41_EXIST
RETURN 1

@SKILL_0_PERSONAL_41_NAME
RESULTS = 淫らな誘惑

;レベルは1-5まで
@SKILL_0_PERSONAL_41_LEVEL
RETURN 5

;対象選択
@SKILL_0_PERSONAL_41_SETTARGET(発動者, 発動番号, 発動側, 発動勢力, 発動部隊, 対象勢力, 対象部隊)
#DIM 発動者
#DIM 発動番号
#DIM スキル
#DIMS ジャンル
#DIM 発動側
#DIM 発動勢力
#DIM 発動部隊
#DIM 対象側
#DIM 対象勢力
#DIM 対象部隊
対象側 = !発動側
SIF BATTLE_COMMANDER_NUM:対象側 == 0
	RETURN 0
COMBAT_SKILL_TARGET = RAND:(BATTLE_COMMANDER_NUM:対象側)
RETURN 1

;発動判定
@SKILL_0_PERSONAL_41_CAN_INVOKE(発動者, 発動番号, 発動側, 発動勢力, 発動部隊, 対象勢力, 対象部隊)
#DIM 発動者
#DIM 発動番号
#DIM スキル
#DIMS ジャンル
#DIM 発動側
#DIM 発動勢力
#DIM 発動部隊
#DIM 対象側
#DIM 対象勢力
#DIM 対象部隊
対象側 = !発動側
RETURN 1

;効果をここに記述
@SKILL_0_PERSONAL_41_INVOKE(発動者, 発動番号, 発動側, 発動勢力, 発動部隊, 対象勢力, 対象部隊)
#DIM 発動者
#DIM 発動番号
#DIM スキル
#DIMS ジャンル
#DIM 発動側
#DIM 発動勢力
#DIM 発動部隊
#DIM 対象側
#DIM 対象勢力
#DIM 対象部隊
対象側 = !発動側

SELECTCASE RAND:3
	CASE 0
		CALL COLOR_PRINTL(@"%ANAME(発動者)%「和我一起做好事吧？」",カラー_ピンク)
		;TranslatedMark @"%ANAME(発動者)%「私と、イイことしましょう？」", カラー_ピンク
	CASE 1
		CALL COLOR_PRINTL("%ANAME(発動者)%「哼哼♥把你的身体和心全部……涂满♥」",カラー_ピンク)
		;TranslatedMark @"%ANAME(発動者)%「うふっ♥　貴方の身体も心も全部……塗りつぶしてアゲル♥」", カラー_ピンク
	CASE 2
		CALL COLOR_PRINTL("%ANAME(発動者)%「会让好好你享用一个美梦的…嘻嘻…♥」",カラー_ピンク)
		;TranslatedMark @"%ANAME(発動者)%「キモチいい夢、見せてあげるわよ…クスクス…♥」", カラー_ピンク
ENDSELECT
PRINTFORML %ANAME(BATTLE_COMMANDER:対象側:COMBAT_SKILL_TARGET)%被诱惑迷住了！
;TranslatedMark %ANAME(BATTLE_COMMANDER:対象側:COMBAT_SKILL_TARGET)%は誘惑に魅了された！
PRINTFORML 不能参加这场战斗！
;TranslatedMark この戦闘に参加できない！
CALL BATTLE_KNOCKOUT(対象側, COMBAT_SKILL_TARGET)

RETURN 1

@SKILL_0_PERSONAL_41_EXPLANATION
RESULTS = 使敌方一人战斗不能
;TranslatedMark この戦闘に参加できない！

@SKILL_0_PERSONAL_41_NO_LEARN_INIT
@SKILL_0_PERSONAL_41_CANT_LEARN_FROM_SHOP
@SKILL_0_PERSONAL_41_CANT_TELL


@SKILL_0_PERSONAL_41_RATE(発動者, 発動番号, 発動側, 発動勢力, 発動部隊, 対象勢力, 対象部隊)
#DIM 発動者
#DIM 発動番号
#DIM スキル
#DIMS ジャンル
#DIM 発動側
#DIM 発動勢力
#DIM 発動部隊
#DIM 対象側
#DIM 対象勢力
#DIM 対象部隊
RETURN 110
