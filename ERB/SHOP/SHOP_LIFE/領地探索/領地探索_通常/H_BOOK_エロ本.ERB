;-------------------
;存在判定
;なにもさせないこと
;-------------------
@TERRITORY_EVENT_H_BOOK_EXISTS()
#DIM 対象
RETURN 1

;-------------------
;発生判定
;-------------------
@TERRITORY_EVENT_H_BOOK_DECISION(対象)
#DIM 対象
RETURN 1

;-------------------
;本体
;-------------------
@TERRITORY_EVENT_H_BOOK(対象)
#DIM 対象

PRINTFORML 傍晚時分、在河灘上撿到了一本舊書
PRINTFORML 在意裡面什麼内容読了一下、是色情雜誌

IF (CONFIG:202 == 1 && TALENT:対象:0 == 1)
	PRINTFORMW 两个全裸的少女相拥在一起，一边摩擦着下体，一边叫喊着对方的名字
ELSE
	PRINTFORMW 女性被戴上了項圈、像狗一樣被侵犯著
ENDIF

IF IS_MALE(対象)
	PRINTFORML 開心地把撿到的好東西帯回去了
	PRINTFORML 在自己房間仔細地讀著
	CALL FUCK(対象, "性技")
ELSE
	PRINTFORML 雖然感到非常羞恥但是目光卻無法移開
	PRINTFORML 偷偷帯了回去、在自己房間沉溺于自慰
	CALL FUCK(対象, "性技, 自慰, Ｃ, Ｖ, Ｂ" + (HAS_PENIS(対象) ? ", 射精" # ""))
ENDIF

RETURN 1