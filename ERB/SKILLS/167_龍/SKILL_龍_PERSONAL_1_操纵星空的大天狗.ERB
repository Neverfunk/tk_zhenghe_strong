;-----------------------------------
;基本値計算に先んじて処理するもの
;-----------------------------------
@SKILL_167_PERSONAL_1_EXIST
RETURN 1

@SKILL_167_PERSONAL_1_NAME
RESULTS = 操纵星空的大天狗

;レベルは1-5まで
@SKILL_167_PERSONAL_1_LEVEL
RETURN 5

;対象選択
@SKILL_167_PERSONAL_1_SETTARGET(発動者, 発動番号, 発動側, 発動勢力, 発動部隊, 対象勢力, 対象部隊)
#DIM 発動者
#DIM 発動番号
#DIM スキル
#DIMS ジャンル
#DIM 発動側
#DIM 発動勢力
#DIM 発動部隊
#DIM 対象側
#DIM 対象勢力
#DIM 対象部隊
対象側 = !発動側
RETURN 1

;発動判定
@SKILL_167_PERSONAL_1_CAN_INVOKE(発動者, 発動番号, 発動側, 発動勢力, 発動部隊, 対象勢力, 対象部隊)
#DIM 発動者
#DIM 発動番号
#DIM スキル
#DIMS ジャンル
#DIM 発動側
#DIM 発動勢力
#DIM 発動部隊
#DIM 対象側
#DIM 対象勢力
#DIM 対象部隊
対象側 = !発動側
RETURN 1

;発動テキストをオーバライドしたいときに。
;「誰それのスキル発動！　○○した！」の「○○した！」の部分を実装したい場合は、
;これじゃなくてINVOKEで書けばいいです。
;@SKILL_167_PERSONAL_1_INVOKE_TEXT(発動者, スキル, ジャンル)
;#DIM 発動者
;#DIM スキル
;#DIMS ジャンル

;効果をここに記述
@SKILL_167_PERSONAL_1_INVOKE(発動者, 発動番号, 発動側, 発動勢力, 発動部隊, 対象勢力, 対象部隊)
#DIM 発動者
#DIM 発動番号
#DIM スキル
#DIMS ジャンル
#DIM 発動側
#DIM 発動勢力
#DIM 発動部隊
#DIM 対象側
#DIM 対象勢力
#DIM 対象部隊
対象側 = !発動側
PRINTFORML --「星火燎原之舞」--
CALL DECREASE_SOLDIER(対象勢力, 対象部隊, RAND(800, 1000), 1);期望900
SIF RAND:100 >= 80
	RETURN 0
PRINTFORML --「虹彩陆离之舞」--
CALL DECREASE_SOLDIER(対象勢力, 対象部隊, RAND(800, 1000), 1);期望720
SIF RAND:100 >= 40
	RETURN 0
PRINTFORML --「天马行空之舞」--
CALL DECREASE_SOLDIER(対象勢力, 対象部隊, RAND(800, 1000), 1);期望288

@SKILL_167_PERSONAL_1_EXPLANATION
RESULTS = 按照一定概率对敌方部队造成多次伤害

@SKILL_167_PERSONAL_1_CANT_TELL

@SKILL_167_PERSONAL_1_RATE(発動者, 発動番号, 発動側, 発動勢力, 発動部隊, 対象勢力, 対象部隊)
#DIM 発動者
#DIM 発動番号
#DIM スキル
#DIMS ジャンル
#DIM 発動側
#DIM 発動勢力
#DIM 発動部隊
#DIM 対象側
#DIM 対象勢力
#DIM 対象部隊
RETURN 150
