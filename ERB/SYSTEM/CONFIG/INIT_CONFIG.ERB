@INIT_CUSTOM_CONFIG_BLANK(ARG)
CLEARLINE LINECOUNT - ARG
REDRAW 0


@INIT_CUSTOM_CONFIG
PRINTFORML .
PRINTFORML .
PRINTFORML .
PRINTFORML .
PRINTFORML .
PRINTFORML .
LOCAL = LINECOUNT
PRINTFORML 要打开初始的设定页面么？
CALL ASK_MULTI("好","读取之前保存的配置","还是直接开始游戏罢")
SIF RESULT == 2
    RETURN
IF RESULT == 1
    CALL CONFIG_LOAD(1)
    RETURN
ENDIF
CALL COLOR_PRINTL("配置中，**默认表明这是默认的选项。在不知道该选什么的时候就这样选吧？",カラー_注意)
PRINTFORML 来开始配置SLG部分的内容吧：
PRINTFORML 关于难度的选择：
PRINTFORML 难度只对SLG的部分产生影响
CALL ASK_MULTI("EASY（想軽鬆愉快的玩遊戲的選択）","NORMAL（普通的難度） **默认","HARD（需要有好的戦略）","LUNATIC（多周目前提下的難度）","NIGHTMARE（無理）")
CONFIG:314 = RESULT
CALL INIT_CUSTOM_CONFIG_BLANK(LOCAL)

PRINTFORML 角色在被捕后会因为对方势力君主的厌恶而被处决嗎？
CALL ASK_MULTI("全部角色都会被处决","不会发生处决 **默认","非东方的角色可以被处决")
CONFIG:304 = RESULT
CALL INIT_CUSTOM_CONFIG_BLANK(LOCAL)

PRINTFORML 角色所在部队如果被全歼了，会战死吗？
CALL ASK_MULTI("会","不会！**默认")
IF RESULT == 1
    CONFIG:305 = 0
ELSE
    PRINTFORML 那种情况下，战死的概率?
    CALL ASK_MULTI("我后悔了","低","中 **不推荐","高 **不推荐")
    CONFIG:305 = RESULT
    SIF RESULT == 0
        GOTO skip_config1
    PRINTFORML 那种情况下，哪些角色会战死?
    CALL ASK_MULTI("全部 **默认","只有随机生成的角色")
    CONFIG:309 = RESULT
ENDIF
$skip_config1
CALL INIT_CUSTOM_CONFIG_BLANK(LOCAL)

PRINTFORML 角色所在部队被全歼时被捕的概率？
CALL ASK_MULTI("中 **默认","低")
CONFIG:306 = RESULT
CALL INIT_CUSTOM_CONFIG_BLANK(LOCAL)

PRINTFORML 其他君主提出外交性要求的概率？
CALL ASK_MULTI("欲望Lv3＋性知識Lv3时总是提出","欲望Lv3＋性知識Lv3時概率提出","性知識Lv3時常時提出","無視条件保持100％")
CONFIG:308 = RESULT
CALL INIT_CUSTOM_CONFIG_BLANK(LOCAL)

PRINTFORML 当被对方君主提出外交性要求时，谁会成为对象？
CALL ASK_MULTI("随机 **默认","总是主人公","总是势力的君主")
CONFIG:307 = RESULT
CALL INIT_CUSTOM_CONFIG_BLANK(LOCAL)

PRINTFORMW 然后是交流部分的设定：
PRINTFORML 允許怀孕吗
CALL ASK_MULTI("可以 **默认","不可以")
CONFIG:1 = RESULT
CALL INIT_CUSTOM_CONFIG_BLANK(LOCAL)

PRINTFORML 孩子的性别？
CALL ASK_MULTI("总是男孩","总是女孩","完全随机")
IF RESULT == 0
    CONFIG:4 = 100
ELSEIF RESULT == 1
    CONFIG:4 = 0
ELSE
    CONFIG:4 = 50
ENDIF
CALL INIT_CUSTOM_CONFIG_BLANK(LOCAL)

PRINTFORML 随机角色的性别？
CALL ASK_MULTI("总是男性","总是女性","完全随机")
IF RESULT == 0
    CONFIG:3 = 100
ELSEIF RESULT == 1
    CONFIG:3 = 0
ELSE
    CONFIG:3 = 50
ENDIF
CALL INIT_CUSTOM_CONFIG_BLANK(LOCAL)


PRINTFORML 关于特殊性癖的设定：
PRINTFORML 允许特殊势力调教角色吗？
CALL ASK_MULTI("不允许 **荐给不喜欢女性角色被侵犯的人","只允许对女性角色这么做","允许 **默认")
CONFIG:80 = RESULT
CALL INIT_CUSTOM_CONFIG_BLANK(LOCAL)
SIF CONFIG:80 == 0
    GOTO skip_config2
PRINTFORML 有哪些特殊势力会调教角色？
CALL ASK_MULTI("所有特殊势力 **默认","只有主人公所在的特殊势力","主人公所在*以外*的特殊势力")
CONFIG:81 = RESULT
CALL INIT_CUSTOM_CONFIG_BLANK(LOCAL)

PRINTFORML 每个特殊势力会调教哪些角色？
CALL ASK_MULTI("以下全部 **默认","只有被该特殊势力俘虏的角色","只有加入该特殊势力的角色")
CONFIG:82 = RESULT
CALL INIT_CUSTOM_CONFIG_BLANK(LOCAL)

$skip_config2
PRINTFORML 被调教的角色会排尿吗？
CALL ASK_MULTI("不会 **默认","会")
CONFIG:90 = RESULT
CONFIG:91 = RESULT
CALL INIT_CUSTOM_CONFIG_BLANK(LOCAL)


PRINTFORML 探索事件百合化（女性角色在领地探索遇到色情事件时，会触发百合事件）
CALL ASK_MULTI("不启用","启用")
CONFIG:202 = RESULT
CALL INIT_CUSTOM_CONFIG_BLANK(LOCAL)

PRINTFORML 每期日常事件与口上事件发生数量的上限與概率
PRINTFORML 推薦將倍率設置為1-2，將個數設置為5-10
LOCAL:1 = LINECOUNT
$SHOW_LOOP
CURRENTREDRAW
LOCAL:2 = RESULT
REDRAW 0
PRINTFORML [9990] 日常事件発生倍率 - {DAILY_EVENT_ODDS}
PRINTFORML [9991] 日常事件発生個数 - {DAILY_EVENT_COUNT}
PRINTFORML [9992] 口上日常事件倍率 - {KOJO_DAILY_EVENT_ODDS}
PRINTFORML [9993] 口上日常事件個数 - {KOJO_DAILY_EVENT_COUNT}
PRINTFORML [9999] 完成
INPUT
IF RESULT == 9999
	RETURN
ELSEIF GROUPMATCH(RESULT, 9990, 9991, 9992, 9993)
	SELECTCASE RESULT
		CASE 9990
			DAILY_EVENT_ODDS = ROUND_INCREMENT(DAILY_EVENT_ODDS, 0, 10)
		CASE 9991
			DAILY_EVENT_COUNT = ROUND_INCREMENT(DAILY_EVENT_COUNT, 0, 10)
		CASE 9992
			KOJO_DAILY_EVENT_ODDS = ROUND_INCREMENT(KOJO_DAILY_EVENT_ODDS, 0, 10)
		CASE 9993
			KOJO_DAILY_EVENT_COUNT = ROUND_INCREMENT(KOJO_DAILY_EVENT_COUNT, 0, 10)
	ENDSELECT
    CLEARLINE LINECOUNT - LOCAL:1
    REDRAW LOCAL:2
    GOTO SHOW_LOOP
ENDIF

PRINTFORML 要保存這個配置嗎？
CALL ASK_MULTI("是","否")
SIF RESULT == 0
    CALL CONFIG_SAVE(1)