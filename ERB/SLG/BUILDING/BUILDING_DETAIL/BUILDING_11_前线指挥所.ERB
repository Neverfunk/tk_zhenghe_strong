@BUILDING_GET_NAME_前线指挥所()
    #FUNCTIONS
    RETURNF "前线指挥所"

@BUILDING_GET_EXP_前线指挥所()
    #FUNCTIONS
    RETURNF "驻扎在本城市的我方部队武斗，防卫提高。"

@BUILDING_SET_INFO_ID_11()
    BUILDING_TYPE:建造物_前线指挥所 = 建造物_类型_军事类
    BUILDING_LEVEL:建造物_前线指挥所 = 建造物_等级_二级建筑
    BUILDING_LIMIT_NUM:建造物_前线指挥所 = 建造物_数量限制_无
    BUILDING_LIMIT_UNI:建造物_前线指挥所 = 0
    BUILDING_COUNTRY_ID:建造物_前线指挥所:特殊勢力_普通势力 = 1
    BUILDING_HAVE_SKILL:建造物_前线指挥所 = 0
    BUILDING_TOHO_REQ_ID:建造物_前线指挥所:0 = 0

@CHECK_BUILDING_FUNCTION_前线指挥所(CITY_ID)
    #FUNCTION
    #DIM CITY_ID
    RESULT:1 = GET_CITY_BUILDING_NUM(CITY_ID,建造物_前线指挥所)*10
    IF RESULT:1 > 0
        RESULT:0 = 1
    ENDIF
    RETURNF RESULT:0

@BUILDING_TEXT_前线指挥所_1(COUNTRY_ID,CITY_ID)
    #FUNCTION
    #DIM COUNTRY_ID
    #DIM CITY_ID
    SIF !CHECK_BUILDING_TEXT_OUTPUT(COUNTRY_ID)
        RETURNF 0
    BUILDING_TEXT = PRINT_BUILDING_TEXT_TITLE_F()
    PRINTFORM 因为《前线指挥所》的存在，
    PRINTFORML 位于%CITY_NAME:CITY_ID%的%ANAME(GET_COUNTRY_BOSS(COUNTRY_ID))%的军队武斗提高了！
    RETURNF 1

@BUILDING_TEXT_前线指挥所_2(COUNTRY_ID,CITY_ID)
    #FUNCTION
    #DIM COUNTRY_ID
    #DIM CITY_ID
    SIF !CHECK_BUILDING_TEXT_OUTPUT(COUNTRY_ID)
        RETURNF 0
    BUILDING_TEXT = PRINT_BUILDING_TEXT_TITLE_F()
    PRINTFORM 因为《前线指挥所》的存在，
    PRINTFORML 位于%CITY_NAME:CITY_ID%的%ANAME(GET_COUNTRY_BOSS(COUNTRY_ID))%的军队防卫提高了！
    RETURNF 1
