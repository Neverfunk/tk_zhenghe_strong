;-----------------------------------
;基本値計算に先んじて処理するもの
;-----------------------------------
@SKILL_30_PERSONAL_0_EXIST
RETURN 1

@SKILL_30_PERSONAL_0_NAME
RESULTS = 永遠与須臾的罪人

;レベルは1-5まで
@SKILL_30_PERSONAL_0_LEVEL
RETURN 5

;対象選択
@SKILL_30_PERSONAL_0_SETTARGET(発動者, 発動番号, 発動側, 発動勢力, 発動部隊, 対象勢力, 対象部隊)
#DIM 発動者
#DIM 発動番号
#DIM スキル
#DIMS ジャンル
#DIM 発動側
#DIM 発動勢力
#DIM 発動部隊
#DIM 対象側
#DIM 対象勢力
#DIM 対象部隊
;自部隊対象
RETURN 1

;発動判定
@SKILL_30_PERSONAL_0_CAN_INVOKE(発動者, 発動番号, 発動側, 発動勢力, 発動部隊, 対象勢力, 対象部隊)
#DIM 発動者
#DIM 発動番号
#DIM スキル
#DIMS ジャンル
#DIM 発動側
#DIM 発動勢力
#DIM 発動部隊
#DIM 対象側
#DIM 対象勢力
#DIM 対象部隊
対象側 = !発動側
RETURN 1

;効果をここに記述
@SKILL_30_PERSONAL_0_INVOKE(発動者, 発動番号, 発動側, 発動勢力, 発動部隊, 対象勢力, 対象部隊)
#DIM 発動者
#DIM 発動番号
#DIM スキル
#DIMS ジャンル
#DIM 発動側
#DIM 発動勢力
#DIM 発動部隊
#DIM 対象側
#DIM 対象勢力
#DIM 対象部隊
対象側 = !発動側
PRINTFORML 在，距离现在的百兆年后
PRINTFORML 全宇宙的恒星将会停止行动，宇宙，将会迎来黑暗时代
PRINTFORML 然后，又是三涧年
PRINTFORML 质子崩坏发生，宇宙中存在的所有物全部毁减
CALL COLOR_PRINTL("「即使如此，宇宙也不会毁灭」", カラー_黄)
PRINTFORML 剩下的蓬莱人将会在22个无量大数年中，度过所谓的永远
PRINTFORML 这是永远的罪孽，那么须臾呢？
PRINTFORML 须臾，相对于永远，便是一瞬的时间
PRINTFORML 集合了须臾的时间也是在时间中流动，人类们却完全无法感知。
PRINTFORML 利用这时间就能弄出复数不同的历史来。
PRINTFORML 也就是，对无限未来的观测
PRINTFORML 这就是妾身，永远与须臾的罪人
PRINTFORML 知晓了这一切的你，如何敢在这一宇宙，与妾身开战呢？
CALL COLOR_PRINTL("「此为，视宇宙之死的不死罪人！」", カラー_黄)
BATTLE_RATE_GRD:発動側 += 100
FOR LOCAL, 0, BATTLE_COMMANDER_NUM:発動側
	IF LOCAL != 発動番号
		CALL BATTLE_KNOCKOUT(発動側, LOCAL)
	ENDIF
NEXT
FOR LOCAL, 0, BATTLE_COMMANDER_NUM:対象側
	CALL BATTLE_KNOCKOUT(対象側, LOCAL)
NEXT
@SKILL_30_PERSONAL_0_EXPLANATION
RESULTS = 我方部隊受到的傷害無效化。除辉夜外，双方其他角色均不可行动

@SKILL_30_PERSONAL_0_NO_LEARN_INIT
@SKILL_30_PERSONAL_0_CANT_LEARN_FROM_SHOP
@SKILL_30_PERSONAL_0_CANT_TELL


@SKILL_30_PERSONAL_0_RATE(発動者, 発動番号, 発動側, 発動勢力, 発動部隊, 対象勢力, 対象部隊)
#DIM 発動者
#DIM 発動番号
#DIM スキル
#DIMS ジャンル
#DIM 発動側
#DIM 発動勢力
#DIM 発動部隊
#DIM 対象側
#DIM 対象勢力
#DIM 対象部隊
RETURN 1000
