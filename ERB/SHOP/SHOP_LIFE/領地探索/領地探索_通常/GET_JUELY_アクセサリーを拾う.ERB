;-------------------
;存在判定
;なにもさせないこと
;-------------------
@TERRITORY_EVENT_GET_JUERY_EXISTS()
#DIM 対象
RETURN 1

;-------------------
;発生判定
;-------------------
@TERRITORY_EVENT_GET_JUERY_DECISION(対象)
#DIM 対象
RETURN 1

;-------------------
;本体
;-------------------
@TERRITORY_EVENT_GET_JUERY(対象)
#DIM 対象

PRINTFORML 探索的時候、揀到了一個遺失物
PRINTFORML 是一個閃閃発光的珠宝飾品
PRINTFORML 
CALL ASK_YN("賣給商店", "交給衛兵")

IF RESULT == 0
	PRINTFORML 打算賣掉賺點小錢、走到了附近的珠寶店
	PRINTFORML 但是、被店主氣勢洶洶的怒罵了
	PRINTFORML 那好像是幾天前這個店裡被偷走的東西
	PRINTFORML 筋疲力盡後總算是解開了誤會
	CALL ADD_COOLTIME(対象, 1)
ELSEIF RESULT == 1
	PRINTFORML 私藏不太好
	PRINTFORML 交給衛兵了
ENDIF

RETURN 1